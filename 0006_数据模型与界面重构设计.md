# 数据模型与界面重构设计

> 文档版本：v1.4
> 创建日期：2026-06-01
> 修订日期：2026-06-02
> 状态：⏳ 待用户审核
> 关联文档：0001_项目设计 / 0002_技术设计 / 0004_数据源设计 / 0005_海龟信号监控设计 / 0016_通达信海龟策略详解

> **v1.4 说明**：v1.4 是独立版本，**完整保留 v1.3 全部内容**（在前面章节），v1.4 新增内容追加在末尾：
> - §4.3.5（海龟 session 特有事实）、§4.5.5（事件流水）
> - §6.3 重新设计 + §6.5 操作入口统一说明
> - §7.5（Session 生命周期与触发源）+ §7.6（用户操作流程）
> - §8.1 新增 6 个 API、§9 Phase 3 扩展、§10/§11/§13 增量
> - 修订记录首条为 v1.4 总览

---

## 修订记录

### v1.5（2026-06-02）— 海龟交易过程数据子表

**背景**：v1.4 缺"子策略交易过程数据"子表。用户指出：
1. 子策略会话数据已有（`turtle_session_data`）✅
2. 但"过程数据"无表存储 ❌

**核心新增**：
1. 新增 `turtle_trade_process` 表：海龟策略逐根 K 线决策流水
2. 10 个 event_type 枚举（entry_signal / entry_filled / add_signal / add_filled / add_skipped_gap / stop_loss_check / stop_loss_triggered / check_exit / exit_20_triggered / unit_closed）
3. check 类事件优化存储（C 方案：触发存前后 5 根，平时不存）
4. 退仓优先顺序明确：止损 > 加仓（同根 K 线）
5. 加仓次数确认：4 Unit = 1 首仓 + 3 加仓

**4 项决策**（用户全部按推荐默认）：
1. 加仓次数：4 Unit（1+3）
2. 退仓优先：止损优先
3. check 类存储：C 方案（触发存前后 5 根）
4. dual_ma_trade_process：v1.5 不做

**表数变化**：21 → 22（+1 turtle_trade_process）

### v1.4（2026-06-02）— 子策略特有事实落库 + Session 生命周期显式化 + 用户入口补全

**背景**：用户在 v1.3 基础上指出 2 块缺失：

1. **缺子策略特有字段的存储**：v1.3 的通用表 `trade_sessions` / `position_units` 被海龟的 `entry_basis_price` / `entry_locked_atr` / `stop_price` 字段污染，将来加 `dual_ma` / `breakout` 等其他子策略时无解
2. **session_id 触发与复用规则没说清楚**：v1.3 仅说"建仓瞬间建 session"，没说手动操作（建仓/加仓/减仓/平仓）怎么复用同一 session_id，也没说"信号触发后 session 与"手动建仓" session 的差异

**8 项决策**：

| # | 决策点 | 决定 | 备选 |
|---|--------|------|------|
| 1 | `turtle_session_data` 字段范围 | v1.4 草案 7 字段（不加 `max_units_reached`）| 8 字段（含 `max_units_reached`）|
| 2 | `dual_ma_session_data` 占位 | 不预留 | 预留空表 |
| 3 | `session_event_log.event_type` 枚举 | 8 个（见 §4.5.5）| 6 个精简版 |
| 4 | 老 session 归档 | v1.4 不实施，靠索引兜底 | 30 天归档到 `trade_sessions_archive` |
| 5 | `position_units` 海龟字段 | 继续放，加注释 | 拆到 `turtle_unit_data` |
| 6 | 手动 vs 信号并发 | 应用层互斥锁（`POST /session` 拿 `session_lock`，HTTP 423 拒绝冲突）| DB 事务锁 / 不处理 |
| 7 | 用户入口分散 3 处 | 保持分散 | 合并为 1 个总控按钮 |
| 8 | 版本策略 | v1.4 独立版本（保留 v1.3 全部内容）| 在 v1.3 上修改 |

**核心修订**：

1. **新增 `turtle_session_data` 表**（§4.3.5）：海龟策略 session 级特有事实（7 字段），通用表不再被海龟字段污染
2. **新增 `session_event_log` 表**（§4.5.5）：session 生命周期事件流水（8 个 `event_type` 枚举）
3. **新增 `strategy_event_log` 表**（§4.5.5）：策略执行引擎事件流水（与 `session_event_log` 分离）
4. **`trade_sessions` 注释调整**：`entry_basis_price` / `entry_locked_atr` 注释明确"通用字段，含义由策略决定"；新增 `idx_ts_status_open` 部分索引加速 open session 查询
5. **`position_units` 注释追加**：海龟字段（`entry_basis_price` / `entry_atr` / `stop_price`）注释说明"目前主要是海龟使用，其他策略可忽略"
6. **新增 §7.5 Session 生命周期与触发源**：6 个触发源场景矩阵 + DB / 应用层双层唯一性约束 + 完整流程图（触发源 → API 路由层 → 撮合引擎 → 价格线 API）
7. **新增 §7.6 用户操作流程**：手动建仓 / 手动加仓 / 手动减仓 / 全部平仓 4 个完整流程图
8. **改造 §6 界面设计**：5 个 Tab（信号/模拟/回测/策略/Session）+ 顶部"操作"组（建仓/加仓/减仓/全部平仓）+ 4 个模态框设计
9. **新增 6 个 API**（§8.1）：`POST /session`（建仓）/ `POST /session/{id}/orders`（加仓/减仓）/ `POST /session/{id}/close`（全部平仓）/ `GET /session/{id}/events`（事件流）/ `GET /sessions?status=open`（open session 列表）
10. **Phase 3 扩展**（§9）：6-7 天 → 10-12 天，新增 14 个子任务
11. **新增 4 项待确认**（§10）：并发冲突处理 / 事件流保留期 / 手动操作权限校验 / session_event_log 数据量预估
12. **新增 2 项风险**（§11）：手动操作并发风险 / 事件流水膨胀风险

**表数量变化**：16 → 20（+4 新增主表，-0）
- 新增：19. `turtle_session_data` / 20. `session_event_log` / 21. `strategy_event_log` / 22. `~~dual_ma_session_data~~`（v1.4 不预留，删除占位）
- 调整：9. `trade_sessions` 注释补充 + 1 索引 / 14. `position_units` 注释补充

**实施计划调整**：
- Phase 3：6-7 天 → **10-12 天**（新增 14 个子任务：3 张新表 / 6 个新 API / 互斥锁 / 事件流水写入 / 模态框前端）
- 总计：~19-22 → **~24-28 工作日**

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.4（预计 90KB / 1800+ 行 / 14 ## / 60+ ###）

**飞书同步**：
- 同步方式：lark SDK 路径（v1.3 路径）
- 飞书 doc 终态：14 H2 + ?? H3 + ?? Sheet + ?? Code = ?? 顶层块
- 文档链接未变：https://feishu.cn/wiki/OMwUwjCDAi0QOyk6gINcDZVUnlc

### v1.3（2026-06-01）— 多账户 + 价格线实时计算 + 用户调价 + 策略代码组织 + K 线保留期 + ATR 合并 + date 关联 + 主力合约代码 + 删除 signal_states + 精简信号表 + 外部平台信息

**5. K 线数据保留期调整**（v1.3 补充）：
- `futures_daily`：14 天 → **3 年**（1095 天）
- `futures_min`：7 天 → **半年**（180 天）
- 原因：策略回测需要长期历史数据；半年分时支持人工复盘近期行情

**6. ATR 整合**（v1.3 补充）：
- `futures_daily` 新增 `atr` 字段（吸纳原 `futures_atr` 表）
- `futures_atr` 表删除（数据迁移到 `futures_daily.atr`）
- 减少表数量，简化查询

**7. 分时与日线关联**（v1.3 补充）：
- `futures_min` 新增 `date` 字段（从 `datetime` 派生，冗余但高效）
- 建立索引 `idx_fm_symbol_date` 用于 (symbol, date) 关联查询
- 常见场景：策略执行时合并当日日线 + 当根分线（减少查询次数）

**8. `futures_daily` 加主力合约代码**（v1.3 补充）：
- 新增 `main_contract_code TEXT` 字段（每日对应的主力合约）
- 每日同步时计算并写入（基于成交量+持仓量动态识别）
- 策略回测时直接用 daily 的 `main_contract_code` 锁定当日主力，不用再去 `futures_contracts` 查
- 加索引 `idx_fd_main_contract(main_contract_code)`

**9. 精简信号表 + 删除 `strategy_signal_states`**（v1.3 补充）：
- **`strategy_signals` 只记开仓事件**：`signal_type` 限制为 `entry_long` / `entry_short` 两个枚举值
  - 加仓/止损/退出信号**不存**（依赖实际成交价上下文，实时算）
  - 调整后信号表更轻量，只记录纯粹的"开仓决策事件"
- **删除 `signal_session_id` 字段**：信号与会话是 1 对多关系（同 1 个 entry 信号可能产生多次部分成交）
  - 会话应从 `sim_trades.session_id` 反向查询，不应从 signal 反向查询
- **删除 `strategy_signal_states` 表**：派生数据不持久化
  - 跟"价格线实时算"是同一套思想
  - 前端"当前信号" = 后端 API 实时 JOIN `strategy_signals` + `trade_sessions` + `sim_positions` 算
  - `phase` 字段 → 实时算（idle / in_position 由是否有 open trade_session 判定）

**10. 交易/账户/订单表加外部平台信息**（v1.3 补充）：
- `sim_account` / `live_account`：`platform_name` / `external_account_no` / `broker_id`
- `sim_orders` / `live_orders`：`platform_name` / `external_order_id` / `ctp_order_ref` / `ctp_front_id` / `ctp_session_id`
- `sim_trades` / `live_trades`：`platform_name` / `external_trade_id`
- `platform_name` 枚举：`sim` / `ctp` / `tiger` / `ib` / `futu` 等
- 原因：未来接 CTP/老虎证券/盈透时，能精确追踪外部委托号 / 成交号，便于对账

**11. `trade_sessions` 业务唯一性约束**（v1.3 补充）：
- 加 UNIQUE 约束 `(account_id, strategy_id, direction, entry_time)`
- 含义：同一 (账户, 策略, 方向, 建仓日期) 只能有 1 个 session
- 不同日期可以多次会话；跨账户/策略/方向不冲突
- PK 仍为 `session_id`（UUID），UNIQUE 是业务唯一性额外保证
- 原因：一天内的开仓是"一次完整交易"（即使中间平仓重开也是同一 session）

### v1.3（2026-06-01）— 多账户 + 价格线实时计算 + 用户调价 + 策略代码组织

**1. 多账户支持**：所有交易表加 `account_id` 字段，支持多账户并存

**2. 价格线实时计算（不持久化）**：
- 撤回 v1.3 旧版提议的 `trade_session_lines` 表
- 价格线**实时算**：前端根据 K线 + trade_sessions 锁定值 + position_units + 策略参数动态计算
- 后端提供 `GET /session/{id}/lines` 实时计算 API

**3. 用户调价支持**：
- `trade_sessions.price_overrides_json`（轻量级 JSON）保存用户调整的埋点
- 调价 API：`PUT /session/{id}/line/{line_type}`
- 算法值与 override 优先级：override > 算法

**4. 策略代码组织约定**：
- `strategies/<type>/V<版本号>/strategy.py` 目录结构
- 算法命中时通过 `strategy_id`（`type_vN`）动态导入路径

### v1.2（2026-06-01）— 三大基础领域重构

- **领域划分**：明确划分 3 个基础领域（合约行情 / 策略 / 交易账户）
- **删除**：`turtle_states` / `dual_ma_states`（状态属于交易事实）
- **新增**：`trade_sessions`（交易领域主表）
- **核心原则**：**策略是无状态规则引擎**，锁定值在交易领域落库

### v1.1（2026-06-01）— 5 项关键修订

- 表注释补全 / 策略版本字段 / 信号主表 / 交易会话 / 策略状态表拆分（v1.2 已替代第 5 项）

### 表数量变化

- v1.0：10 张
- v1.1：11 张
- v1.2：16 张
- v1.3：**16 张**（字段调整 + 增加 price_overrides_json 字段，不增表）

---

## 一、背景与目标

### 1.1 现状痛点

经过 `www/index.html` + `data/data_loader.py` + `api/main.py` + `strategies/turtle/position.py` 全链路梳理，发现当前架构有 **4 个结构性缺陷**：

1. **合约维度缺失**：品种（AG）和合约（ag2607）混用
2. **策略写死**：`strategies/turtle/` 硬编码路径
3. **交易层缺失**：没有 `orders` / `trades` / `account` 表
4. **真实持仓未设计**：CTP 未对接，持仓模型未预留

### 1.2 改造目标

| # | 目标 | 用户场景 |
|---|------|---------|
| 1 | 看到品种下所有合约报价 + 主力合约高亮 | "AG 下有 4 个合约，ag2607 是主力" |
| 2 | 策略可切换 / 可并行 + 参数迭代可追溯 + 多版本代码隔离 | "海龟 V1 / V2 共存，V1 跑实盘 V2 跑回测" |
| 3 | 模拟交易全程落盘 + 回测结果持久化 | "每个成交都存，重启还能看" |
| 4 | 真实持仓模型完整 + 与 0016 通达信策略字段对齐 | "4 Unit 详情 + 锁定初始 ATR + 跳空标志" |
| 5 | 多账户支持 | "sim 账户 + live 账户并存，不同策略可挂不同账户" |
| 6 | 价格线实时计算 + 用户调价 | "K 线图上画价格线，用户拖动调整后实时重算" |

---

## 二、当前现状分析

### 2.1 当前数据表（4 张）

| 表 | 主键 | 关键字段 | 数据量级 | 备注 |
|----|------|---------|---------|------|
| `futures_daily` | (symbol, date) | OHLC, volume, hold, settle, **atr** (v1.3 新增), cached_at | ~1万行/品种 | **3 年滚动**（v1.3 调整）|
| `futures_min` | (symbol, datetime) | OHLC, volume, period, contract_code, name, **date** (v1.3 新增), cached_at | ~1万行/品种/周期 | **半年滚动**（v1.3 调整）|
| `turtle_signals` | id (自增) | symbol, signal_type, trigger_price, ... | 通常 0-数百 | v1.3 升级为 strategy_signals |
| `futures_atr` | (symbol, date) | atr, n_value | 极小 | **★ v1.3 合并到 futures_daily，删除此表** |

### 2.2 数据表与界面对应关系矩阵

| 界面功能 | 当前数据源 | 数据流 | 现状问题 |
|---------|-----------|--------|---------|
| 品种网格（价格/涨跌） | `futures_daily` 最后一条 | `GET /signals/all` 返回 | 实时价用日线收盘 |
| 品种网格（信号指示点） | `turtle_signals` 最新 | `GET /turtle/signals/active` | 只有海龟，多策略无法区分 |
| K 线图 | `futures_min` / `futures_daily` | `GET /minute/{sym}` / `GET /daily/{sym}` | 没有"按合约切换"，没有"价格线" |
| 信号时间线 | `turtle_signals` | `GET /turtle/signals/{sym}` | 写死 turtle |
| 持仓卡片 | 内存 `PositionManager` | `GET /position/{sym}` | 进程重启即丢 |
| 回测结果 | 内存 job | `POST /backtest/run` | 不落盘 |

**核心矛盾**：界面已经在展示"策略信号 + 持仓 + 回测"三件套，但数据层只有"行情 + 单一策略信号"两张半表，缺一半。

---

## 三、领域划分（v1.2 + v1.3 强化）

### 3.1 三大基础领域总览

```
┌──────────────────────────────────────────────────────────────┐
│  领域 1：合约行情（Contract Market）                          │
│  基础事实：合约 + 价格走势                                    │
│  写入方：数据同步服务（Sina / AkShare）                       │
│  读出方：所有需要行情的领域                                   │
└──────────────────────────────────────────────────────────────┘
                              ▲
                              │ 提供走势数据
                              │
┌──────────────────────────────────────────────────────────────┐
│  领域 2：策略（Strategy）                                     │
│  本质：规则 + 参数，无状态                                    │
│  输入：合约走势（领域 1）+ 持仓事实（领域 3）                 │
│  输出：信号（待转化为委托）                                   │
│  代码组织：strategies/<type>/V<版本>/strategy.py             │
└──────────────────────────────────────────────────────────────┘
                              ▲
                              │ 信号触发 / 持仓反查
                              │
┌──────────────────────────────────────────────────────────────┐
│  领域 3：交易 / 账户（Trading & Account）                     │
│  基础事实：账户 + 实际买了什么                                │
│  写入方：撮合引擎 / CTP 回报                                 │
│  读出方：策略（下一次决策）+ 账户中心 + 回测 + 价格线计算    │
│  v1.3 新增：account_id 关联 + price_overrides_json 用户调价  │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 关键设计原则（v1.2 保持）

- 策略是**无状态**的——不存"当前 phase"、"锁定值"等中间状态
- "锁定值"（基准价/锁定 ATR/跳空标志）在建仓瞬间由撮合引擎写死到 `trade_sessions` / `position_units`
- 策略下次执行时直接**读这些事实**，不需要"中间状态表"

### 3.3 价格线是"实时计算"不是"持久化"（v1.3 新原则）

**反例（v1.3 旧版提议）**：

```
存储 trade_session_lines 表：
  - entry_actual @ 18810
  - add_unit_1_predicted @ 18860
  - add_unit_2_predicted @ 18910
  - stop_loss_predicted @ 18710
```

**问题**：
- 价格线是**派生数据**，不是事实
- 派生数据持久化会导致"双写一致性问题"（基础事实变了，派生数据没更新）
- 加仓后 stop_loss 会重算，写库就过期了
- 浪费存储

**v1.3 修正**：

```
不存储价格线，每次需要时实时计算：
  inputs: trade_sessions 锁定值 + position_units 实际成交价
         + price_overrides_json 用户覆盖 + 策略参数
  output: 一组价格线 JSON
```

**收益**：
- 单一数据源：所有价格线都从基础事实计算
- 用户调价即时生效（改 JSON 后下次计算就用新值）
- 加仓后子线自动重算（无过期问题）

---

## 四、新数据模型设计（v1.3 按领域组织）

### 4.1 设计原则

1. **领域驱动**：3 大领域，每领域内表强聚合
2. **策略无状态**：所有事实落库到交易领域
3. **策略版本可追溯**：`strategies.version` + 代码目录 `strategies/<type>/V<N>/`
4. **信号状态 vs 流水分离（v1.3 调整）**：只保留 `strategy_signals`（开仓事件流水），`strategy_signal_states` 已被删除（派生数据实时 JOIN 算）
5. **多账户支持**：所有交易表带 `account_id`（v1.3 新增）
6. **价格线实时计算**：不持久化，通过 API 实时算（v1.3 关键决策）
7. **用户调价支持**：`trade_sessions.price_overrides_json`（v1.3 新增）
8. **兼容老数据**：现有 4 张表保留，新表增量创建

### 4.2 领域 1：合约行情（2 张新表 + 2 张保留）

**`futures_contracts`** — 合约基础信息

```sql
-- 表用途：存储每个品种下所有合约的静态信息（代码/交易所/到期日/主力标记）
-- 所属领域：合约行情
-- 写入方：每次 sync_all_minute_data 时通过 Sina 接口刷新
-- 读取方：策略运行时识别主力合约 / 合约抽屉展示
CREATE TABLE futures_contracts (
    contract_code TEXT PRIMARY KEY,
    symbol        TEXT NOT NULL,
    name          TEXT,
    exchange      TEXT NOT NULL,
    list_date     TEXT,
    expire_date   TEXT,
    is_main       INTEGER DEFAULT 0,
    main_rank     INTEGER DEFAULT 99,
    contract_size REAL,
    point_value   REAL,
    tick          REAL,
    margin_ratio  REAL,
    last_volume   REAL,
    last_oi       REAL,
    updated_at    TEXT,
    status        TEXT DEFAULT 'active'
);
CREATE INDEX idx_fc_symbol ON futures_contracts(symbol);
CREATE INDEX idx_fc_main ON futures_contracts(symbol, is_main);
```

**`futures_quotes`** — 合约实时行情

```sql
-- 表用途：存储每个合约的最新行情快照（每 30s 刷新一次）
-- 所属领域：合约行情
CREATE TABLE futures_quotes (
    contract_code TEXT PRIMARY KEY,
    symbol        TEXT NOT NULL,
    trade         REAL, change_abs REAL, change_pct REAL,
    open          REAL, high REAL, low REAL,
    preclose      REAL, presettlement REAL, settlement REAL,
    volume        REAL, position REAL,
    bidprice1     REAL, askprice1 REAL,
    bidvol1       REAL, askvol1 REAL,
    ticktime      TEXT, updated_at TEXT
);
```

**`futures_daily`** — 日 K 线（**v1.3 调整**）

```sql
-- 表用途：存储每个品种的日 K 线数据 + 当日 ATR
-- 所属领域：合约行情
-- 写入方：data_loader.fetch_daily_data 每日同步
-- 读取方：策略（55日高/低、20日反向）+ 前端 K 线图 + 价格线计算
-- v1.3 关键调整：
--   1. 保留期：14 天 → 3 年（策略回测需长期历史）
--   2. 新增字段：atr（从原 futures_atr 表合并，吸纳后删除该表）
--   3. high / low 字段已存在，确保数据采集时正确填充
-- 性能估算：3 年 × 33 品种 × 250 交易日/年 ≈ 24,750 行（SQLite 轻松扛）
CREATE TABLE futures_daily (
    symbol        TEXT NOT NULL,         -- 品种代码 AG
    date          TEXT NOT NULL,         -- 日期 2026-05-30
    open          REAL,                  -- 开盘价
    high          REAL,                  -- ★ 最高价（用于 55日高）
    low           REAL,                  -- ★ 最低价（用于 55日低 / 20日反向）
    close         REAL,                  -- 收盘价
    volume        REAL,                  -- 成交量
    hold          REAL,                  -- 持仓量
    settle        REAL,                  -- 结算价
    atr           REAL,                  -- ★ v1.3 新增：当日 ATR（20 日 EMA）
    -- ★ v1.3 补充：当日对应的主力合约代码（根据成交量+持仓量动态识别）
    main_contract_code TEXT,             -- 例 ag2607（主力 / 次主力随日期可能切换）
    cached_at     TEXT,
    PRIMARY KEY (symbol, date)
);
CREATE INDEX idx_fd_symbol ON futures_daily(symbol, date);
CREATE INDEX idx_fd_main_contract ON futures_daily(main_contract_code);
```

**`futures_min`** — 分钟 K 线（**v1.3 调整**）

```sql
-- 表用途：存储每个品种的分钟 K 线数据
-- 所属领域：合约行情
-- 写入方：data_loader.fetch_minute_data 增量同步
-- 读取方：策略执行（每根 K 线 tick）+ 前端 K 线图
-- v1.3 关键调整：
--   1. 保留期：7 天 → 半年（180 天）
--   2. 新增字段：date（从 datetime 派生，便于与 futures_daily 关联）
-- 性能估算：半年 × 33 品种 × 240 根/天 = 1,425,600 行（需谨慎加索引）
CREATE TABLE futures_min (
    symbol        TEXT NOT NULL,
    datetime      TEXT NOT NULL,         -- 完整时间 2026-05-30 14:30:00
    date          TEXT NOT NULL,         -- ★ v1.3 新增：日期 2026-05-30（从 datetime 派生）
    open          REAL, high REAL, low REAL, close REAL,
    volume        REAL,
    period        TEXT DEFAULT '5min',   -- 周期 1min/5min/15min/30min/60min
    contract_code TEXT DEFAULT '',
    name          TEXT DEFAULT '',
    cached_at     TEXT,
    PRIMARY KEY (symbol, datetime)
);
CREATE INDEX idx_fm_symbol_date ON futures_min(symbol, date);  -- ★ v1.3 关键索引
CREATE INDEX idx_fm_symbol ON futures_min(symbol, datetime);
```

**`futures_daily` 与 `futures_min` 关联查询**（v1.3 新增）

```sql
-- 场景1：拿某天的日线 + 当天的分时
SELECT * FROM futures_daily WHERE symbol='AG' AND date='2026-05-30';
SELECT * FROM futures_min WHERE symbol='AG' AND date='2026-05-30';

-- 场景2：拿 55 日高/低（基于 futures_daily 3 年数据）
SELECT MAX(high), MIN(low)
FROM futures_daily
WHERE symbol='AG' AND date <= '2026-05-30'
ORDER BY date DESC LIMIT 55;

-- 场景3：JOIN 查询（按日期+品种关联）
SELECT
    d.date, d.open, d.high, d.low, d.close, d.atr,
    m.datetime, m.open AS m_open, m.close AS m_close, m.volume
FROM futures_daily d
LEFT JOIN futures_min m ON m.symbol = d.symbol AND m.date = d.date
WHERE d.symbol='AG' AND d.date BETWEEN '2026-05-25' AND '2026-05-30'
ORDER BY d.date DESC, m.datetime ASC;

-- 场景4：策略执行时获取某根分线 + 当日日线（合并指标）
SELECT d.*, m.* FROM futures_min m
LEFT JOIN futures_daily d ON d.symbol = m.symbol AND d.date = m.date
WHERE m.symbol='AG' AND m.datetime='2026-05-30 14:30:00';
```

**`futures_atr` 表删除**（v1.3 整合到 `futures_daily.atr`）

```sql
-- 原 futures_atr 表已废弃，其数据迁移到 futures_daily.atr
-- 迁移脚本：INSERT INTO futures_daily (symbol, date, atr) SELECT symbol, date, atr FROM futures_atr;
-- 迁移完成后：DROP TABLE futures_atr;
```

### 4.3 领域 2：策略（4 张 + 代码组织约定）

**`strategies`** — 策略注册表（主表）

```sql
-- 表用途：策略主表，记录每个策略的元信息 + 当前生效参数
-- 所属领域：策略
-- 关键设计：name + version 联合唯一，支持参数迭代（v1 → v2 → v3）
-- v1.3 新增：type 字段对应代码目录 strategies/<type>/V<version>/
CREATE TABLE strategies (
    strategy_id  TEXT PRIMARY KEY,        -- 主键 = name_vN 形式（如 turtle_v1）
    name         TEXT NOT NULL,           -- 策略名 海龟T1
    version      INTEGER NOT NULL DEFAULT 1,  -- 版本号
    type         TEXT NOT NULL,           -- 策略类型 turtle / dual_ma / breakout
    description  TEXT,
    params_json  TEXT,                   -- 当前生效参数
    is_active    INTEGER DEFAULT 1,
    is_paper     INTEGER DEFAULT 1,
    created_at   TEXT,
    updated_at   TEXT,
    UNIQUE (name, version)
);

-- 预置种子数据
INSERT INTO strategies VALUES
    ('turtle_v1', '海龟T1', 1, 'turtle',
     '经典海龟系统二，55日突破入场/20日反向离市/0.5N加仓/2N止损',
     '{"entry_n":55,"exit_n":20,"add_interval":0.5,"stop_loss":2,"atr_n":20,"max_units":4,"risk_per_trade":2000}',
     1, 1, datetime('now'), datetime('now')),
    ('dual_ma_v1', '双均线', 1, 'dual_ma',
     '快慢均线交叉信号',
     '{"fast":10,"slow":20}', 0, 1, datetime('now'), datetime('now'));
```

**`strategy_param_history`** — 策略参数迭代历史

```sql
-- 表用途：记录策略每次参数变更（v1 → v2 的参数对比）
-- 所属领域：策略
CREATE TABLE strategy_param_history (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    strategy_id  TEXT NOT NULL,
    name         TEXT NOT NULL,
    version      INTEGER NOT NULL,
    params_json  TEXT NOT NULL,
    changed_at   TEXT NOT NULL,
    changed_by   TEXT,
    change_reason TEXT,
    notes        TEXT
);
CREATE INDEX idx_sph_strategy ON strategy_param_history(strategy_id, version);
```

**`strategy_signals`** — 开仓信号流水（★ v1.3 精简）

```sql
-- 表用途：仅记录策略产生的"开仓信号点"（entry_long / entry_short）
-- 所属领域：策略
-- 写入方：策略执行引擎（仅在 K线检测到开仓信号时 INSERT）
-- 读取方：信号时间线（只显示开仓历史）
--
-- ★ v1.3 重要调整：
--   1. 删除 signal_session_id 字段：信号与会话是 1 对多关系（同 1 个 entry 信号可能产生多次部分成交）
--      会话应从 sim_trades.session_id 反向查询， 不应从 signal 反向查询
--   2. signal_type 限制为只 2 个枚举：entry_long / entry_short
--      加仓 / 止损 / 退出 信号 不存：这些信号需要依赖实际成交价（建仓价 / 加仓价）才能计算
--      依赖持仓上下文 实时算，不持久化
--   3. 不需要 phase 字段：phase 属于 trade_session 上下文，不属于信号本身
-- 调整后信号表更轻量，只记录纯粹的"开仓决策事件"
CREATE TABLE strategy_signals (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    strategy_id       TEXT NOT NULL,
    symbol            TEXT NOT NULL,
    name              TEXT,
    contract_code     TEXT,
    signal_type       TEXT NOT NULL,      -- 仅 entry_long / entry_short 两个枚举值
    direction         TEXT NOT NULL,      -- long / short
    trigger_price     REAL NOT NULL,
    reference_price   REAL NOT NULL,
    reference_n       REAL,
    bar_time          TEXT NOT NULL,
    period            TEXT DEFAULT '5min',
    alert_level       TEXT DEFAULT 'normal',
    -- ★ v1.3 删除以下字段：
    --   signal_session_id TEXT,           -- 错误的外键：信号与会话是 1 对多
    --   session_phase     TEXT,            -- 错位：phase 属于 session 不是 signal
    created_at        TEXT DEFAULT (datetime('now')),
    UNIQUE(strategy_id, symbol, signal_type, bar_time)
);
CREATE INDEX idx_ss_strategy ON strategy_signals(strategy_id, bar_time);
CREATE INDEX idx_ss_active ON strategy_signals(strategy_id, symbol, direction, alert_level);

-- 旧 turtle_signals 表保留 + 加 strategy_id
ALTER TABLE turtle_signals ADD COLUMN strategy_id TEXT DEFAULT 'turtle_v1';
CREATE VIEW turtle_signals_v AS
    SELECT * FROM strategy_signals WHERE strategy_id = 'turtle_v1';
```

**为什么不存加仓 / 止损 / 退出信号**（v1.3 核心思想）：

```
【开仓信号】 基于 K线检测（不需要持仓上下文）
  - 可以独立计算
  - 写库（strategy_signals）
  - 不可变的历史记录

【加仓/止损/退出信号】 依赖实际成交价上下文
  - 加仓价 = 建仓价 + n×0.5N（需要知道建仓价）
  - 止损价 = 实际入场价 - 2N（需要知道实际入场价）
  - 依赖 trade_session + position_units 实时算
  - 实时返回（通过 GET /session/{id}/lines）
  - 不存流水（避免重复存、加仓后重算、无法反映 override）
```

// ★ v1.3 删除 strategy_signal_states 表
// 原因：派生数据不持久化。信号状态 = JOIN strategy_signals + trade_sessions 实时算
// 代替方案：后端提供 GET /strategy/signal/state/{sym} 实时 JOIN 查询

### 4.3.5 子策略会话数据（★ v1.4 新增）

**背景**：v1.3 的 `trade_sessions` 表被海龟的 `entry_basis_price` / `entry_locked_atr` 字段污染，将来加 `dual_ma` / `breakout` 等其他子策略时这些字段含义不明确。

**v1.4 解决方案**：将子策略特有的 session 级事实下沉到独立的子策略表（`turtle_session_data`），`trade_sessions` 只保留通用字段。

**`turtle_session_data`** — 海龟策略 session 级特有事实

```sql
-- 表用途：海龟策略 session 级的特有事实（55日高/低、20日EMA、加仓计数、跳空事件等）
-- 所属领域：策略（子策略专属）
-- 写入方：撮合引擎（建仓时 1 次 INSERT + 每次加仓 UPDATE 计数器）
-- 读取方：价格线计算 / 用户操作 / 策略回测
-- ★ v1.4 新增：解决通用表被海龟字段污染的问题
--
-- 设计要点：
--   1. 1:1 关联 trade_sessions.session_id
--   2. 7 字段：3 锁定值 + 2 计数器 + 2 事件
--   3. 不加 max_units_reached（v1.4 决策点 1：改由 trade_sessions.current_units 实时算）
CREATE TABLE turtle_session_data (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id          TEXT NOT NULL UNIQUE,  -- 1:1 关联 trade_sessions
    -- 3 个锁定值（建仓瞬间写死）
    -- 这 3 个字段原本在 trade_sessions 通用表中（v1.3），v1.4 下沉到子策略专属表
    entry_55d_high      REAL,         -- 建仓时 55日最高价（多头入场信号价）
    entry_55d_low       REAL,         -- 建仓时 55日最低价（空头入场信号价）
    entry_atr_20        REAL,         -- 建仓时 20日 ATR（EMA，锁定 N 值）
    -- 2 个计数器（实时 UPDATE）
    add_count           INTEGER DEFAULT 0,  -- 已加仓次数
    skip_add_count      INTEGER DEFAULT 0,  -- 跳空加仓次数（is_gap=1 的 unit 数）
    -- 2 个事件标志（建仓瞬间写死）
    is_gap_entry        INTEGER DEFAULT 0,  -- 是否跳空建仓
    is_breakout_confirm INTEGER DEFAULT 0,  -- 是否突破确认（55日高/低后第 2 根 K 线确认）
    -- 元数据
    created_at          TEXT DEFAULT (datetime('now')),
    updated_at          TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_tsd_session ON turtle_session_data(session_id);
-- 可选：按品种查最新 session
CREATE INDEX idx_tsd_created ON turtle_session_data(created_at);
```

**字段说明**：

| 字段 | 类型 | 含义 | 写入时机 | 来源（v1.3 → v1.4 映射）|
|------|------|------|---------|------------------------|
| `entry_55d_high` | REAL | 建仓时 55日最高价 | 建仓瞬间 | `trade_sessions.entry_basis_price`（多头时）|
| `entry_55d_low` | REAL | 建仓时 55日最低价 | 建仓瞬间 | `trade_sessions.entry_basis_price`（空头时）|
| `entry_atr_20` | REAL | 建仓时 20日 ATR | 建仓瞬间 | `trade_sessions.entry_locked_atr` |
| `add_count` | INTEGER | 已加仓次数 | 每次加仓 +1 | 新增（v1.3 没记）|
| `skip_add_count` | INTEGER | 跳空加仓次数 | 跳空时 +1 | 新增（v1.3 没记）|
| `is_gap_entry` | INTEGER | 跳空建仓标志 | 建仓瞬间 | `position_units.is_gap` 聚合（v1.3 unit 级）|
| `is_breakout_confirm` | INTEGER | 突破确认标志 | 建仓瞬间 | 新增（v1.3 没记）|

**v1.4 决策点 1（7 字段 vs 8 字段）**：

- v1.4 选 7 字段：去掉 `max_units_reached`（最大持仓次数标志，到 4 Unit 后置 1）
- 理由：`trade_sessions.current_units` 已经是 Unit 计数（0-4），前端要"是否到 4 Unit" 实时算即可
- 不需要单独字段

**`trade_sessions` vs `turtle_session_data` 边界**：

```
┌────────────────────────────────┬──────────────────────────────────────┐
│ trade_sessions（通用，16 张）  │ turtle_session_data（子策略专属）     │
├────────────────────────────────┼──────────────────────────────────────┤
│ session_id                     │ session_id（FK）                      │
│ account_id / account_type      │ entry_55d_high / entry_55d_low        │
│ strategy_id / symbol           │ entry_atr_20                          │
│ direction / entry_time         │ add_count / skip_add_count            │
│ first_entry_price              │ is_gap_entry / is_breakout_confirm    │
│ current_units / total_units    │                                       │
│ status / exit_*                │                                       │
│ price_overrides_json           │                                       │
└────────────────────────────────┴──────────────────────────────────────┘
```

**对其他子策略的影响**：

- `dual_ma`：将来加 `dual_ma_session_data` 表时套同样模式（`session_id` + 快慢线值 + 交叉状态）
- `breakout`：套同样模式
- v1.4 决策点 2：**`dual_ma_session_data` 不预留**（等真要加时再创建，避免空表维护成本）

**`dual_ma_session_data` 暂不预留**（v1.4 决策点 2）：

```sql
-- v1.4 不实施：等 dual_ma 真要加时再创建
-- CREATE TABLE dual_ma_session_data (
--     session_id      TEXT PRIMARY KEY,
--     fast_ma_value   REAL,
--     slow_ma_value   REAL,
--     cross_state     TEXT  -- 'above' / 'below' / 'crossed_up' / 'crossed_down'
-- );
```

### 4.3.6 海龟交易过程数据（★ v1.5 新增）

**背景**：`turtle_session_data` 是 session 级"事实快照"（55日高/低、ATR、参数），但**过程数据**（每根 K 线决策流：何时检测加仓信号、跳空跳过、止损触发）无表存储。

**v1.5 解决方案**：新增 `turtle_trade_process` 表，专门记录海龟策略的逐根 K 线决策流水。

**`turtle_trade_process`** — 海龟交易过程流水

```sql
-- 表用途：海龟策略逐根 K 线决策流（含信号检测 + 跳空 + 止损检查 + 20日反向检查）
-- 所属领域：策略（子策略专属过程流水）
-- 写入方：海龟策略执行引擎（每根 K 线 tick 后批量写）
-- 读取方：详情页策略执行回放 / 调试 / 审计
-- ★ v1.5 新增：解决"子策略过程数据无表存储"的问题
--
-- 设计要点：
--   1. 1:N 关联 trade_sessions（一个 session 多个过程事件）
--   2. 10 个 event_type 枚举
--   3. check 类事件优化存储：触发时存前后 5 根 K 线，平时不存（C 方案）
--   4. unit_id 可空（不是所有事件都关联 unit）

CREATE TABLE turtle_trade_process (
    id                  INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id          TEXT NOT NULL,
    account_id          TEXT NOT NULL,
    -- 事件类型（10 个，见下表）
    event_type          TEXT NOT NULL,
    -- K 线上下文
    bar_time            TEXT NOT NULL,
    bar_open            REAL,
    bar_high            REAL,
    bar_low             REAL,
    bar_close           REAL,
    -- 信号相关
    signal_type         TEXT,                 -- entry_long / entry_short / add_long / add_short / stop_loss / exit_20
    signal_price        REAL,                 -- 信号价
    reference_price     REAL,                 -- 参考价
    n_value             REAL,                 -- 当时 N 值
    -- 单元关联
    unit_id             INTEGER,              -- 关联 position_units.unit_id
    -- 执行结果
    exec_status         TEXT,                 -- pending / filled / skipped / failed
    exec_price          REAL,
    exec_hand_count     INTEGER,
    exec_time           TEXT,
    slippage            REAL,
    -- 跳空
    is_gap              INTEGER DEFAULT 0,
    gap_size            REAL,
    -- 决策原因
    decision_reason     TEXT,
    -- 元数据
    created_at          TEXT DEFAULT (datetime('now')),
    FOREIGN KEY (session_id) REFERENCES trade_sessions(session_id) ON DELETE CASCADE
);
CREATE INDEX idx_ttp_session ON turtle_trade_process(session_id, bar_time);
CREATE INDEX idx_ttp_type ON turtle_trade_process(event_type, bar_time);
CREATE INDEX idx_ttp_bar ON turtle_trade_process(bar_time);
```

**event_type 枚举（10 个，v1.5 决策）**：

| # | event_type | 含义 | 触发时机 | 频率 |
|---|-----------|------|---------|------|
| 1 | `entry_signal` | 检测到 55日突破 | 策略每根 K 线 | 一 session 1-2 次 |
| 2 | `entry_filled` | 建仓成交 | 撮合回报 | 一 session 1 次 |
| 3 | `add_signal` | 检测到加仓信号（0.5N 间隔）| 策略每根 K 线 | 经常（多半跳过）|
| 4 | `add_filled` | 加仓成交 | 撮合回报 | 0-3 次/session |
| 5 | `add_skipped_gap` | 加仓跳空跳过 | 策略检测 | 罕见 |
| 6 | `stop_loss_check` | 止损价位检查 | 触发时存前后 5 根 | 触发时 10 条/session |
| 7 | `stop_loss_triggered` | 止损触发 | 撮合回报 | 0-1 次/session |
| 8 | `check_exit` | 20日反向检查 | 触发时存前后 5 根 | 触发时 10 条/session |
| 9 | `exit_20_triggered` | 20日反向触发 | 撮合回报 | 0-1 次/session |
| 10 | `unit_closed` | 减仓/全部平仓 | 撮合回报 | 0-4 次/session |

**v1.5 决策点 3：check 类事件 C 方案**：

- `stop_loss_check` / `check_exit` 是每根 K 线都检查
- 平时不写 `turtle_trade_process`（避免数据量爆炸）
- **触发时**：存触发前 5 根 + 触发 + 触发后 5 根 = 共 10 条 check 事件
- 触发事件本身（`stop_loss_triggered` / `exit_20_triggered`）正常写
- **效果**：定位"为何触发"够用（看前 5 根价格走势），数据量不爆炸

### 4.4 策略代码组织约定（v1.3 核心实施约束）

**目录结构**：

```
strategies/
├── __init__.py
├── base.py                          # BaseStrategy 抽象基类
├── loader.py                        # 策略动态加载器
├── turtle/
│   ├── __init__.py
│   ├── V1/
│   │   ├── __init__.py
│   │   ├── strategy.py              # TurtleV1Strategy(BaseStrategy)
│   │   ├── factors.py               # calc_atr / calc_breakout_55 等
│   │   ├── position.py              # 持仓管理（未来替代 strategies/turtle/position.py）
│   │   ├── signal.py                # 信号检测
│   │   └── default_params.json      # 默认参数
│   └── V2/
│       ├── __init__.py
│       ├── strategy.py              # TurtleV2Strategy（优化版）
│       └── ...
├── dual_ma/
│   ├── __init__.py
│   └── V1/
│       ├── __init__.py
│       ├── strategy.py              # DualMAV1Strategy
│       └── ...
└── breakout/
    └── V1/
        └── ...
```

**约束规则**：

| 规则 | 说明 |
|------|------|
| 文件夹命名 | `<strategy_type>/V<版本号>/` 全大写 V + 数字（如 `V1` / `V2`）|
| 主类命名 | `<StrategyType>V<版本号>Strategy`（如 `TurtleV1Strategy`）|
| strategy_id 命名 | `<type>_v<n>` 小写（如 `turtle_v1`，与 strategies.strategy_id 对应）|
| 参数文件 | 每个版本自带 `default_params.json`，可被 strategies.params_json 覆盖 |
| 动态加载 | `loader.py` 根据 `strategy_id` 解析路径 → importlib 动态导入 |
| 独立性 | 不同版本之间不共享代码（避免改动 V1 意外影响 V2）|

**动态加载器伪代码**：

```python
# strategies/loader.py
import importlib
from strategies.base import BaseStrategy

def load_strategy(strategy_id: str) -> BaseStrategy:
    """
    根据 strategy_id 动态加载策略类
    例: 'turtle_v1' → strategies/turtle/V1/strategy.py → TurtleV1Strategy
    """
    # 解析 strategy_id: "turtle_v1" → type="turtle", version="1"
    parts = strategy_id.rsplit('_v', 1)
    if len(parts) != 2:
        raise ValueError(f"Invalid strategy_id: {strategy_id}")
    type_name, version = parts[0], parts[1]
    
    # 构建模块路径
    module_path = f"strategies.{type_name}.V{version}.strategy"
    
    # 动态导入
    module = importlib.import_module(module_path)
    
    # 类名约定: TurtleV1Strategy
    class_name = f"{type_name.capitalize()}V{version}Strategy"
    strategy_class = getattr(module, class_name)
    
    # 实例化（参数从 strategies 表读取后传入）
    return strategy_class()


# 使用示例
def execute_strategy(strategy_id: str, kline: dict, session_context: dict):
    strategy = load_strategy(strategy_id)
    signal = strategy.on_bar(kline, session_context)
    return signal
```

**回测时多版本并存**：

```python
# 同时加载海龟 V1（实盘）和 V2（回测）
v1 = load_strategy('turtle_v1')  # → strategies/turtle/V1/strategy.py
v2 = load_strategy('turtle_v2')  # → strategies/turtle/V2/strategy.py
# 两者完全隔离，互不干扰
```

### 4.5 领域 3：交易 / 账户（v1.3 强化多账户）

#### 4.5.1 账户表（v1.3 新增 user_id / display_name）

**`sim_account`** — 模拟账户

```sql
-- 表用途：模拟账户资金/权益快照
-- 所属领域：交易
-- v1.3 新增：user_id / display_name 支持多账户
CREATE TABLE sim_account (
    account_id          TEXT PRIMARY KEY,   -- 'sim_default' / 'sim_user1' / 'sim_user2' 等
    user_id             TEXT,               -- 所属用户 ID（未来多用户系统）
    display_name        TEXT,               -- 账户显示名
    -- ★ v1.3 新增：外部平台信息
    platform_name       TEXT DEFAULT 'sim',  -- sim / ctp / tiger / ib / futu
    external_account_no TEXT,               -- 外部账户号（如 CTP 资金账号）
    broker_id           TEXT,               -- broker 标识
    initial_capital     REAL NOT NULL,
    cash                REAL NOT NULL,
    margin_used         REAL DEFAULT 0,
    equity              REAL NOT NULL,
    frozen              REAL DEFAULT 0,
    total_pnl           REAL DEFAULT 0,
    daily_pnl           REAL DEFAULT 0,
    last_settle_date    TEXT,
    created_at          TEXT,
    updated_at          TEXT
);

-- 种子数据
INSERT INTO sim_account VALUES
    ('sim_default', 'system', '默认模拟账户', 'sim',
     1000000, 1000000, 0, 1000000, 0, 0, 0, NULL,
     datetime('now'), datetime('now'));
```

#### 4.5.2 核心：trade_sessions（v1.3 新增 account_id + price_overrides_json）

```sql
-- 表用途：记录一次完整交易（从 entry 到 exit）的元数据
-- 所属领域：交易
-- v1.3 新增：account_id（多账户关联）+ price_overrides_json（用户调价）
CREATE TABLE trade_sessions (
    session_id         TEXT PRIMARY KEY,  -- UUID 业务唯一标识
    -- ★ v1.3 新增：账户关联
    account_id         TEXT NOT NULL,     -- 关联 sim_account.account_id
    account_type       TEXT NOT NULL,     -- 'sim' / 'live'
    -- 策略与品种
    strategy_id        TEXT NOT NULL,     -- 关联 strategies.strategy_id
    symbol             TEXT NOT NULL,
    contract_code      TEXT NOT NULL,
    direction          INTEGER NOT NULL,  -- 1=多/-1=空
    -- 建仓瞬间锁定的值（交易事实）
    -- ★ v1.4 注释调整：
    --   entry_basis_price / entry_locked_atr 是通用字段，含义由策略决定
    --   海龟=55日高/低、20日EMA，详见 turtle_session_data
    --   其他子策略（dual_ma / breakout）可能有不同含义
    entry_basis_price  REAL,              -- 建仓时锁定的基准价（通用字段，含义由策略决定；海龟=55日高/低，详见 turtle_session_data）
    entry_locked_atr   REAL,              -- 建仓时锁定的 ATR（策略特有；海龟=20日EMA，详见 turtle_session_data）
    entry_time         TEXT NOT NULL,     -- 建仓日期（YYYY-MM-DD）
    first_entry_price  REAL NOT NULL,     -- 首 Unit 实际成交价
    -- 状态
    status             TEXT DEFAULT 'open',  -- pending/open/closed
    current_units      INTEGER DEFAULT 0,
    total_units        INTEGER DEFAULT 0,
    -- 离场
    exit_time          TEXT,
    exit_price         REAL,
    exit_reason        TEXT,
    realized_pnl       REAL DEFAULT 0,
    -- ★ v1.3 新增：用户调价覆盖（轻量级 JSON）
    -- 内容示例：{"add_unit_1": 18830, "stop_loss": 18750}
    -- 含义：用户把这几个埋点价格从算法值改成自定义值
    -- 读取时：算法值 + override 覆盖（override 优先）
    price_overrides_json TEXT DEFAULT '{}',
    -- 元数据
    created_at         TEXT,
    last_update        TEXT,
    -- ★ v1.3 补充：业务唯一性约束
    -- 同一 (账户, 策略, 方向, 建仓日期) 只能有 1 个 session
    -- 不同日期可以多次会话；跨账户/策略/方向不冲突
    -- 例：AG 在 2026-05-30 turtle_v1 多头 sim_default 只能 1 个 session
    --     但 2026-05-31 又可以开新 session
    -- 原因：一天内的开仓是"一次完整交易"（即使中间平仓重开也是同一 session）
    UNIQUE (account_id, strategy_id, direction, entry_time)
);
CREATE INDEX idx_ts_account ON trade_sessions(account_id, strategy_id, status);
CREATE INDEX idx_ts_symbol ON trade_sessions(symbol, status);
-- ★ v1.4 新增：open session 部分索引（账户中心页高频查询）
-- 只索引 status='open' 的行，体积小、查询快
CREATE INDEX idx_ts_status_open ON trade_sessions(status) WHERE status='open';
```

**price_overrides_json 字段说明**：

| line_type | 含义 | 调价场景 |
|-----------|------|---------|
| `entry` | 入场价 | 用户想提前/延后入场 |
| `add_unit_1` ~ `add_unit_4` | 加仓价 | 用户想提前/延后加仓 |
| `stop_loss` | 止损价 | 用户想调整止损位 |
| `exit_20` | 20日反向离市价 | 罕见 |

**使用示例**：

```python
# 用户拖动加仓 1 价格线
PUT /session/abc-123/line/add_unit_1
{"new_price": 18830}

# 后端处理
def override_line_price(account_id, session_id, line_type, new_price):
    session = get_trade_session(account_id, session_id)
    overrides = parse_json(session.price_overrides_json) or {}
    overrides[line_type] = new_price
    UPDATE trade_sessions
    SET price_overrides_json = json.dumps(overrides),
        last_update = datetime('now')
    WHERE account_id=? AND session_id=?
```

#### 4.5.3 委托 / 成交 / 持仓（v1.3 新增 account_id）

**`sim_orders`** — 模拟委托单

```sql
-- 表用途：记录模拟委托单（manual + auto + 回测）
-- 所属领域：交易
-- v1.3 新增：account_id
CREATE TABLE sim_orders (
    order_id        TEXT PRIMARY KEY,
    account_id      TEXT NOT NULL,        -- ★ v1.3 多账户
    strategy_id     TEXT NOT NULL,
    symbol          TEXT NOT NULL,
    contract_code   TEXT NOT NULL,
    direction       INTEGER NOT NULL,
    offset_flag     TEXT NOT NULL,
    order_type      TEXT NOT NULL,
    hand_count      INTEGER NOT NULL,
    price           REAL,
    status          TEXT NOT NULL,
    signal_id       INTEGER,
    session_id      TEXT,
    filled_price    REAL,
    filled_at       TEXT,
    reject_reason   TEXT,
    -- ★ v1.3 新增：外部平台信息
    platform_name       TEXT,              -- sim / ctp / tiger / ib / futu
    external_order_id   TEXT,              -- 外部订单号（CTP 委托号 / Tiger order_id）
    ctp_order_ref       TEXT,              -- CTP 特定字段：报单引用
    ctp_front_id        INTEGER,           -- CTP 前置机编号
    ctp_session_id      INTEGER,           -- CTP 会话编号
    created_at      TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_so_account ON sim_orders(account_id, strategy_id, created_at);
CREATE INDEX idx_so_status ON sim_orders(status);
CREATE INDEX idx_so_session ON sim_orders(session_id);
```

**`sim_trades`** — 模拟成交记录

```sql
-- 表用途：模拟成交明细（含手续费/滑点/已实现盈亏）
-- 所属领域：交易
-- v1.3 新增：account_id
CREATE TABLE sim_trades (
    trade_id         TEXT PRIMARY KEY,
    account_id       TEXT NOT NULL,
    order_id         TEXT NOT NULL,
    strategy_id      TEXT NOT NULL,
    symbol           TEXT NOT NULL,
    contract_code    TEXT NOT NULL,
    direction        INTEGER NOT NULL,
    offset_flag      TEXT NOT NULL,
    hand_count       INTEGER NOT NULL,
    price            REAL NOT NULL,
    fee              REAL DEFAULT 0,
    slippage         REAL DEFAULT 0,
    realized_pnl     REAL DEFAULT 0,
    backtest_run_id  TEXT,
    session_id       TEXT,
    traded_at        TEXT NOT NULL,
    created_at       TEXT DEFAULT (datetime('now')),
    -- ★ v1.3 新增：外部平台信息
    platform_name       TEXT,              -- sim / ctp / tiger / ib / futu
    external_trade_id   TEXT               -- 外部成交号（CTP 成交号 / Tiger trade_id）
);
CREATE INDEX idx_st_account ON sim_trades(account_id, strategy_id, traded_at);
CREATE INDEX idx_st_backtest ON sim_trades(backtest_run_id);
CREATE INDEX idx_st_session ON sim_trades(session_id);
```

**`sim_positions`** — 模拟持仓汇总

```sql
-- 表用途：模拟持仓的汇总视图
-- 所属领域：交易
-- v1.3 新增：account_id
CREATE TABLE sim_positions (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id      TEXT NOT NULL,
    strategy_id     TEXT NOT NULL,
    symbol          TEXT NOT NULL,
    contract_code   TEXT NOT NULL,
    direction       INTEGER NOT NULL,
    session_id      TEXT,
    total_hand      INTEGER NOT NULL,
    avg_price       REAL NOT NULL,
    realized_pnl    REAL DEFAULT 0,
    unrealized_pnl  REAL DEFAULT 0,
    margin_used     REAL DEFAULT 0,
    open_time       TEXT,
    last_update     TEXT,
    UNIQUE(account_id, strategy_id, symbol, contract_code, direction)
);
```

**`position_units`** — 持仓单元详情（4 Unit 持久化）

```sql
-- 表用途：持仓的 4 Unit 详情
-- 所属领域：交易
-- v1.3 新增：account_id（必填）
CREATE TABLE position_units (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    account_id        TEXT NOT NULL,     -- ★ v1.3 新增
    account_type      TEXT NOT NULL,
    session_id        TEXT NOT NULL,
    strategy_id       TEXT NOT NULL,
    symbol            TEXT NOT NULL,
    contract_code     TEXT NOT NULL,
    unit_id           INTEGER NOT NULL,
    direction         INTEGER NOT NULL,
    hand_count        INTEGER NOT NULL,
    entry_price       REAL NOT NULL,     -- 该 Unit 实际成交价
    -- ★ v1.4 注释补充：entry_basis_price / entry_atr / stop_price 目前主要是海龟使用
    --   其他策略（dual_ma / breakout 等）可忽略这 3 个字段，或不填
    entry_basis_price REAL NOT NULL,
    entry_atr         REAL NOT NULL,
    stop_price        REAL NOT NULL,
    status            TEXT DEFAULT 'open',
    is_gap            INTEGER DEFAULT 0,
    open_time         TEXT NOT NULL,
    close_time        TEXT,
    close_price       REAL,
    close_reason      TEXT,
    created_at        TEXT DEFAULT (datetime('now')),
    UNIQUE(account_id, strategy_id, symbol, contract_code, unit_id, open_time)
);
CREATE INDEX idx_pu_account ON position_units(account_id, strategy_id, symbol, status);
CREATE INDEX idx_pu_session ON position_units(session_id);
```

#### 4.5.4 真实持仓层（3 张，预留）

```sql
-- 现阶段不实施，预留给 CTP 实盘对接
-- 同构于 sim_*，所有表加 account_id
CREATE TABLE live_orders    (...);
CREATE TABLE live_trades    (...);
CREATE TABLE live_positions (...);
```

#### 4.5.5 事件流水表（★ v1.4 新增）

**背景**：v1.3 没有统一的事件流水表，session 生命周期、策略执行状态变化只能通过对比 trade_sessions/sim_trades 前后状态反推，查询慢且容易出错。

**v1.4 解决方案**：新增 2 张事件流水表，分别记录"session 生命周期"和"策略执行引擎"两类事件。

**`session_event_log`** — session 生命周期事件流水

```sql
-- 表用途：记录 trade_session 生命周期中的关键事件
-- 所属领域：交易
-- 写入方：撮合引擎 / API 路由层 / 调度任务
-- 读取方：详情页 Session Tab / 审计 / 调试
-- ★ v1.4 新增：8 个 event_type 枚举
CREATE TABLE session_event_log (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    session_id        TEXT NOT NULL,       -- FK → trade_sessions
    account_id        TEXT NOT NULL,
    event_type        TEXT NOT NULL,       -- 8 个枚举（见下表）
    event_time        TEXT NOT NULL,       -- 事件时间（UTC+8）
    -- 事件详情（JSON，灵活）
    event_data_json   TEXT,                -- 事件详情：价格/手数/原因等
    -- 触发源追溯
    trigger_source    TEXT,                -- signal / manual / system（信号/手动/系统）
    trigger_ref_id    TEXT,                -- 触发源 ID：signal_id / user_id / cron_name
    notes             TEXT,
    created_at        TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_sel_session ON session_event_log(session_id, event_time);
CREATE INDEX idx_sel_account_type ON session_event_log(account_id, event_type);
CREATE INDEX idx_sel_event_time ON session_event_log(event_time);
```

**`event_type` 枚举（v1.4 决策点 3：8 个）**：

| # | event_type | 含义 | 触发时机 | 典型 event_data |
|---|------------|------|---------|----------------|
| 1 | `session_created` | session 创建 | 撮合首 Unit 成交 | `{entry_price, basis_price, atr}` |
| 2 | `order_submitted` | 委托提交 | API 接收委托 | `{order_id, price, hand_count, direction}` |
| 3 | `order_filled` | 委托成交 | 撮合成交回报 | `{trade_id, filled_price, slippage, fee}` |
| 4 | `units_changed` | Unit 数变化 | 每次加仓/减仓 | `{from_units, to_units, unit_id, reason}` |
| 5 | `line_overridden` | 价格线被用户调价 | PUT /session/{id}/line API | `{line_type, old_price, new_price}` |
| 6 | `status_changed` | session 状态变化 | open/pending/closed 切换 | `{from_status, to_status, reason}` |
| 7 | `stop_loss_triggered` | 止损触发 | 撮合检测到止损价位 | `{stop_price, fill_price, exit_reason}` |
| 8 | `exit_20_triggered` | 20日反向触发 | 撮合检测到 20日反向 | `{reverse_price, fill_price, exit_reason}` |

**v1.4 决策点 4：老 session 归档不实施**：

- v1.4 暂不实施 `session_event_log_archive` 表
- 靠索引 `idx_sel_event_time` 兜底（按时间范围查询避免全表扫）
- 30 天后看数据量再决定是否归档

**`strategy_event_log`** — 策略执行引擎事件流水

```sql
-- 表用途：记录策略执行引擎的运行事件（与 session 生命周期解耦）
-- 所属领域：策略
-- 写入方：策略执行引擎（每根 K 线 tick 后）
-- 读取方：详情页策略 Tab / 调试 / 性能分析
-- ★ v1.4 新增：与 session_event_log 分离（session 是交易事实，策略是规则）
CREATE TABLE strategy_event_log (
    id                INTEGER PRIMARY KEY AUTOINCREMENT,
    strategy_id       TEXT NOT NULL,
    symbol            TEXT NOT NULL,
    bar_time          TEXT NOT NULL,       -- K 线时间
    -- 事件类型
    event_type        TEXT NOT NULL,       -- signal_detected / signal_filtered / on_bar_called / error_occurred / params_loaded
    event_level       TEXT DEFAULT 'info', -- info / warn / error
    -- 详情
    event_data_json   TEXT,                -- JSON：信号价/原因/参数/异常堆栈
    duration_ms       REAL,                -- 策略 on_bar 执行耗时（毫秒）
    created_at        TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_strel_strategy ON strategy_event_log(strategy_id, bar_time);
CREATE INDEX idx_strel_level ON strategy_event_log(event_level);
CREATE INDEX idx_strel_time ON strategy_event_log(created_at);
```

**`strategy_event_log` vs `session_event_log` 边界**：

```
┌──────────────────────────────┬────────────────────────────────────┐
│ strategy_event_log           │ session_event_log                  │
├──────────────────────────────┼────────────────────────────────────┤
│ 粒度：每根 K 线              │ 粒度：每个 session 生命周期事件    │
│ 触发方：策略执行引擎         │ 触发方：撮合引擎 / API 路由        │
│ 关注点：信号检测 / 过滤      │ 关注点：session 状态机变化          │
│ 数据量：每根 K 线 1 行       │ 数据量：每个 session 5-20 行      │
│ 用途：策略调试 / 性能分析    │ 用途：详情页 Session Tab / 审计   │
└──────────────────────────────┴────────────────────────────────────┘
```

**`turtle_trade_process` vs `session_event_log` / `strategy_event_log` 边界**（v1.5 强化）：

| 维度 | turtle_trade_process | session_event_log | strategy_event_log |
|------|----------------------|-------------------|---------------------|
| 粒度 | 海龟每根 K 线决策 | session 生命周期事件 | 策略引擎每根 K 线 tick |
| 写入方 | 海龟策略执行 | 撮合引擎 / API 路由 | 策略执行引擎 |
| 关注点 | 海龟特有决策（55日/0.5N/20日/止损）| session 状态机 | 信号检测 / 引擎事件 |
| 频率 | 一 session 30-50 条 | 一 session 5-20 条 | 每根 K 线 1 条 |
| 用途 | 海龟回放 / 调试 | 详情页 Session Tab | 策略调试 / 性能分析 |
| check 类 | C 方案（触发存前后 5 根）| 不适用 | 不适用 |

**`position_units` 字段说明补充**（v1.4 决策点 5）：

- v1.3 在 `position_units` 表中保留了 `entry_basis_price` / `entry_atr` / `stop_price` 3 个海龟专属字段
- v1.4 决策：**不拆分到 `turtle_unit_data` 表**，继续放 `position_units`，但加注释说明
- 理由：unit 级的事实用得频繁（价格线计算每根线都要查），拆表增加 JOIN 开销
- 注释原文：
  > `entry_basis_price` / `entry_atr` / `stop_price` 目前主要是海龟使用，其他策略可忽略

### 4.6 跨领域协同关系

| 协同方向 | 触发条件 | 数据流 | 关键字段 |
|---------|---------|--------|---------|
| 合约 → 策略 | K线更新 | 读 `futures_min` / `futures_daily` | OHLC |
| 合约 → 交易 | 行情刷新 | 读 `futures_quotes` 写 `sim_trades.price` | trade |
| 交易 → 策略 | 策略执行 | 读 `trade_sessions` / `position_units` | locked_atr, basis_price, current_units |
| 策略 → 交易 | 信号触发 | 写 `sim_orders` | signal_type, direction, hand_count |
| 撮合 → 交易 | 成交发生 | 写 `sim_trades` + `trade_sessions` + `position_units` + `sim_positions` | 价格/手续费 |
| 策略 → 信号表 | 信号触发（仅开仓） | 写 `strategy_signals` | signal_type |
| 交易 → 合约 | 价格线计算 | 读 `trade_sessions` + `position_units` 实时算 | entry_basis_price, locked_atr, current_units |
| 合约 → 价格线 | 价格线计算 | 读 `futures_daily` / `futures_min` | 20日高/低 |

---

## 五、价格线实时计算（v1.3 核心新增）

### 5.1 核心思想

价格线是**派生数据**，由基础事实（trade_sessions + position_units + 策略参数 + K线）实时计算得出。

**不持久化价格线表**，每次需要时调用算法重新计算。

### 5.2 计算 API

**`GET /session/{session_id}/lines?account_id=...`**

请求：
```
GET /session/abc-123/lines?account_id=sim_default
```

响应：
```json
{
  "session_id": "abc-123",
  "account_id": "sim_default",
  "symbol": "AG",
  "contract_code": "ag2607",
  "direction": 1,
  "current_units": 2,
  "lines": [
    {
      "line_type": "entry",
      "price": 18810,
      "basis": "actual_fill",
      "is_triggered": true,
      "triggered_at": "2026-05-30 14:30:00",
      "triggered_by_trade_id": "trade-xxx"
    },
    {
      "line_type": "add_unit_1",
      "price": 18870,
      "basis": "actual_fill",
      "is_triggered": true,
      "triggered_at": "2026-05-30 14:35:00",
      "triggered_by_trade_id": "trade-yyy"
    },
    {
      "line_type": "add_unit_2",
      "price": 18920,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": false
    },
    {
      "line_type": "add_unit_3",
      "price": 18970,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": false
    },
    {
      "line_type": "add_unit_4",
      "price": 19020,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": false
    },
    {
      "line_type": "stop_loss_unit_1",
      "price": 18720,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": true,
      "original_algo_price": 18710
    },
    {
      "line_type": "stop_loss_unit_2",
      "price": 18770,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": false
    },
    {
      "line_type": "exit_20",
      "price": 18600,
      "basis": "actual_fill",
      "is_triggered": false,
      "is_overridden": false
    }
  ]
}
```

### 5.3 计算算法

```python
def compute_session_lines(account_id, session_id) -> dict:
    """
    实时计算 trade_session 的所有价格线
    """
    # 1. 读基础事实
    session = get_trade_session(account_id, session_id)
    if session is None:
        return {"error": "session not found"}
    
    strategy = get_strategy(session.strategy_id)
    params = parse_json(strategy.params_json)
    overrides = parse_json(session.price_overrides_json) or {}
    
    direction = session.direction
    locked_atr = session.entry_locked_atr
    max_units = params.get('max_units', 4)
    add_interval = params.get('add_interval', 0.5) * locked_atr  # 0.5N
    add_step = add_interval  # 每个加仓档位的间距
    
    # 2. 已有 unit 的实际入场价
    open_units = get_open_position_units(account_id, session_id)
    last_unit = open_units[-1] if open_units else None
    
    lines = []
    
    # 3. 入场线（已触发则用实际成交价，未触发则用信号预测价或 override）
    if session.first_entry_price:
        lines.append({
            "line_type": "entry",
            "price": session.first_entry_price,
            "basis": "actual_fill",
            "is_triggered": True,
            "triggered_at": session.entry_time,
            "triggered_by_trade_id": get_entry_trade_id(account_id, session_id),
        })
    else:
        # 信号触发但未成交：展示信号价
        algo_entry = session.entry_basis_price  # 实际是 55日高/低
        lines.append({
            "line_type": "entry",
            "price": overrides.get('entry', algo_entry),
            "basis": "signal",
            "is_triggered": False,
            "is_overridden": 'entry' in overrides,
            "original_algo_price": algo_entry,
        })
    
    # 4. 加仓线（4 个）
    for n in range(1, max_units + 1):
        line_type = f"add_unit_{n}"
        if n <= len(open_units):
            # 已成交：实际入场价
            actual_unit = open_units[n - 1]
            lines.append({
                "line_type": line_type,
                "price": actual_unit.entry_price,
                "basis": "actual_fill",
                "is_triggered": True,
                "triggered_at": actual_unit.open_time,
                "triggered_by_trade_id": actual_unit.created_trade_id,
            })
        else:
            # 未成交：基于最后 unit 计算（或基于基准价）
            if last_unit is None:
                base_price = session.entry_basis_price
            else:
                base_price = last_unit.entry_price
            algo_price = base_price + direction * n * add_step
            price = overrides.get(line_type, algo_price)
            lines.append({
                "line_type": line_type,
                "price": price,
                "basis": "actual_fill" if last_unit else "signal",
                "is_triggered": False,
                "is_overridden": line_type in overrides,
                "original_algo_price": algo_price,
            })
    
    # 5. 止损线（每个 unit 一条，动态计算）
    for i, unit in enumerate(open_units):
        # 止损阶梯：entry_price - (len(open_units) - i) * 0.5N - 1.5N
        # 实际上海龟的止损是 entry - 2N，加仓后上调
        # 简化：每个 unit 的止损 = entry_price - 2N（基于自己的入场价）
        # 实际上海龟的 stop 是 锁定 ATR × 2
        # 这里用"动态阶梯"模式：unit_n 的 stop = (最后 unit 的 entry) - (max_units - n + 1) * 0.5N
        # 简化版：直接每 unit 一条线
        if last_unit is None:
            base = unit.entry_price
        else:
            base = last_unit.entry_price
        
        # 阶梯止损：本 unit 的 stop = (n - 1) * 0.5N 之下
        ladder_offset = (i + 1) * add_step
        algo_stop = unit.entry_price - direction * (2 * locked_atr - ladder_offset)
        # 实际：基础止损 2N - (i+1)*0.5N（即每个加仓后止损上调 0.5N）
        
        line_type = f"stop_loss_unit_{unit.unit_id}"
        price = overrides.get(line_type, algo_stop)
        lines.append({
            "line_type": line_type,
            "price": price,
            "basis": "actual_fill",
            "is_triggered": False,
            "is_overridden": line_type in overrides,
            "original_algo_price": algo_stop,
        })
    
    # 6. 20日反向离市线（基于当前 K线）
    if open_units:
        if direction == 1:
            algo_exit_20 = get_20_day_low(session.symbol)  # 多头：跌破 20日低
        else:
            algo_exit_20 = get_20_day_high(session.symbol)  # 空头：涨破 20日高
        price = overrides.get('exit_20', algo_exit_20)
        lines.append({
            "line_type": "exit_20",
            "price": price,
            "basis": "actual_fill",
            "is_triggered": False,
            "is_overridden": 'exit_20' in overrides,
            "original_algo_price": algo_exit_20,
        })
    
    return {
        "session_id": session_id,
        "account_id": account_id,
        "symbol": session.symbol,
        "contract_code": session.contract_code,
        "direction": direction,
        "current_units": session.current_units,
        "lines": lines,
    }
```

### 5.4 用户调价 API

**`PUT /session/{session_id}/line/{line_type}?account_id=...`**

请求：
```
PUT /session/abc-123/line/add_unit_1?account_id=sim_default
Content-Type: application/json

{
  "new_price": 18830
}
```

后端处理：
```python
def override_line_price(account_id, session_id, line_type, new_price):
    # 校验
    if not is_valid_line_type(line_type):
        raise ValueError(f"Invalid line_type: {line_type}")
    
    session = get_trade_session(account_id, session_id)
    if session is None:
        raise ValueError("Session not found")
    
    # 校验：仅允许 in_position / pending 状态下调价
    if session.status not in ('open', 'pending'):
        raise ValueError(f"Cannot override line in status {session.status}")
    
    # 更新 price_overrides_json
    overrides = parse_json(session.price_overrides_json) or {}
    overrides[line_type] = new_price
    
    UPDATE trade_sessions
    SET price_overrides_json = json.dumps(overrides),
        last_update = datetime('now')
    WHERE account_id=? AND session_id=?
    
    return {"success": True, "line_type": line_type, "new_price": new_price}
```

### 5.5 前端 K 线图集成

```javascript
// K 线图组件初始化
async function initKLChart(symbol, sessionId, accountId) {
    // 1. 加载 K线
    const klines = await fetch(`/minute/${symbol}`).then(r => r.json());
    
    // 2. 加载价格线
    const linesData = await fetch(`/session/${sessionId}/lines?account_id=${accountId}`).then(r => r.json());
    
    // 3. 渲染 K线
    chart.setData(klines);
    
    // 4. 叠加价格线
    for (const line of linesData.lines) {
        const color = line.basis === 'actual_fill' && line.is_triggered
            ? 'red'   // 已成交
            : line.is_overridden
                ? 'purple'  // 用户调价
                : 'blue';  // 算法预测
        const style = line.is_triggered ? LineStyle.Solid : LineStyle.Dashed;
        
        chart.addHorizontalLine({
            price: line.price,
            color: color,
            lineStyle: style,
            text: line.line_type,
        });
    }
}

// 用户拖动水平线
chart.onHorizontalLineDrag((lineType, newPrice) => {
    // 调价
    fetch(`/session/${sessionId}/line/${lineType}?account_id=${accountId}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ new_price: newPrice })
    });
    
    // 立即重算所有线（递归影响）
    refreshLines();
});
```

### 5.6 价格线生命周期

```
[空仓阶段]
  trade_sessions 还没建，不显示价格线

[信号触发 entry_long @ 18800，尚未成交]
  status = 'pending'
  ├─ entry @ 18800 (signal, dashed blue)
  └─ 其他加仓/止损线 暂不显示（因为还没建仓）

[实际成交 @ 18810]
  status = 'open', current_units = 1
  ├─ entry @ 18810 (actual_fill, solid red) ← 红色实线
  ├─ add_unit_1 @ 18860 (actual_fill, solid blue)  ← 基于 actual_fill 重算
  ├─ add_unit_2 @ 18910
  ├─ add_unit_3 @ 18960
  ├─ add_unit_4 @ 19010
  ├─ stop_loss_unit_1 @ 18710
  └─ exit_20 @ 18600（基于 K线）

[用户拖动 add_unit_1 到 18830]
  PUT /session/abc/line/add_unit_1
  → price_overrides_json = '{"add_unit_1": 18830}'
  → 重算 add_unit_2（如果基于 add_unit_1 计算）：18900 - 30 = 18880
  → 重算 add_unit_3, 4 同样偏移

[加仓 1 成交 @ 18870（市场跳过 18830）]
  current_units = 2
  ├─ add_unit_1 @ 18870 (actual_fill, solid red) ← 实际成交价（不是用户 override 价）
  ├─ add_unit_2 重新计算（基于 last_unit=add_unit_1）
  │   注意：用户 override 是信号线，实际成交后 override 可能不再准确
  │   设计：override 仅在"未成交的信号线"上生效，已成交后用 actual_fill
  ├─ add_unit_3 / 4 同样
  └─ stop_loss_unit_2 重新计算

[止损触发 @ 18705]
  status = 'closed', exit_reason = 'stop'
  → 所有 add_unit_* is_active=0（前端的"激活"过滤）
  → stop_loss_unit_1 @ 18705 (is_triggered=true)
```

### 5.7 override 优先级

**规则**：
- 算法值：基于 trade_sessions 锁定值 + 策略参数 + position_units 实际成交价实时计算
- override 值：从 `trade_sessions.price_overrides_json` 读取
- **override 优先**（仅在"信号线"上生效，已成交的 line 不应用 override）

**示例**：
- `add_unit_2` 还没成交，用户拖到 18880 → override 生效，K 线图显示 18880
- `add_unit_2` 实际成交 @ 18860 → 用 18860（actual_fill），忽略之前的 override
- 子线（add_unit_3, 4）重新计算：基于 last_unit=add_unit_2 实际成交价，不再基于 override

---

## 六、界面改造设计

### 6.1 顶部新增：策略切换器 + 账户切换器

```
┌────────────────────────────────────────────────────────────────────┐
│ 📈 Stock Quant 期货行情  [账户: sim_default ▼] [策略: 海龟V1 ▼]   │
└────────────────────────────────────────────────────────────────────┘
```

- 账户切换：`GET /accounts` → `sim_default` / `sim_user1` / `live_xxx`
- 策略切换：`GET /strategies?account_id=...&is_active=1`
- URL 持久化：`?account=sim_default&strategy=turtle_v1`

### 6.2 品种网格 + 详情面板（与 v1.2 类似，略）

### 6.3 账户中心页（v1.3 强化）

```
http://localhost:8000/account?account=sim_default&strategy=turtle_v1

┌─────────────────────────────────────────────┐
│  🏦 账户中心 (sim_default / 海龟V1)         │
├─────────────────────────────────────────────┤
│  总览（来自 sim_account）                   │
│  初始资金:    1,000,000.00                  │
│  当前权益:    1,123,450.00   (+12.34%)      │
│  ...                                         │
│                                             │
│  当前 Session（来自 trade_sessions）        │
│  account: sim_default                       │
│  session_id: abc-123-...                    │
│  status: open  current_units: 2/4           │
│  entry_basis_price: 18850  locked_atr: 1036 │
│  price_overrides: {add_unit_1: 18830, stop_loss: 18750}  │
│                                             │
│  K 线图 + 价格线（来自 GET /session/{id}/lines）│
│  ┌──────────────────────────────────────┐  │
│  │   K线 + 6 条水平线：                  │  │
│  │   - entry (红实线)   @ 18810          │  │
│  │   - add_unit_1 (红实线) @ 18870      │  │
│  │   - add_unit_2 (蓝实线) @ 18920      │  │
│  │   - add_unit_3 (蓝实线) @ 18970      │  │
│  │   - add_unit_4 (蓝实线) @ 19020      │  │
│  │   - stop_loss (紫实线) @ 18720       │  │
│  │   - exit_20 (蓝虚线) @ 18600         │  │
│  │                                       │  │
│  │   用户可拖动任意线调整价格           │  │
│  │   调整后通过 PUT API 实时生效        │  │
│  └──────────────────────────────────────┘  │
│                                             │
│  持仓列表（来自 sim_positions）             │
│  ...                                         │
│                                             │
│  Unit 明细（来自 position_units）            │
│  ...                                         │
│                                             │
│  成交记录（来自 sim_trades）                │
│  ...                                         │
└─────────────────────────────────────────────┘
```

### 6.4 策略历史页（同 v1.2）

### 6.5 回测中心页（同 v1.2）

### 6.6 界面与数据表对应关系（v1.3）

| 界面区块 | 涉及数据表 | 领域 | 主要 API |
|---------|-----------|------|---------|
| 顶部账户切换 | `sim_account` | 交易 | `GET /accounts` |
| 顶部策略切换 | `strategies` | 策略 | `GET /strategies?account_id=...` |
| 品种网格（信号点） | `strategy_signals`（仅最新开仓事件） | 策略 | `GET /signals/all?strategy_id=...` |
| 合约抽屉 | `futures_contracts`, `futures_quotes` | 合约 | `GET /contracts/{symbol}`, `GET /quotes/{contract_code}` |
| 详情·信号 Tab（当前） | 实时 JOIN（`trade_sessions` + `strategy_signals`） | 策略 | `GET /strategy/signal/state/{sym}` |
| 详情·模拟 Tab | `sim_*`（带 account_id 过滤） | 交易 | `GET /sim/positions?account_id=...` |
| 详情·真实 Tab | `live_*` | 交易 | `GET /live/...` |
| 详情·回测 Tab | `backtest_runs` | 交易 | `POST /backtest/run` |
| K 线图 + 价格线 | 实时计算（trade_sessions + position_units + K线） | 跨领域 | `GET /session/{id}/lines` |
| 用户调价 | `trade_sessions.price_overrides_json` | 交易 | `PUT /session/{id}/line/{line_type}` |
| 账户中心 | 所有 sim_* | 交易 | `GET /account/overview?account_id=...` |
| 策略历史 | `strategies` + `strategy_param_history` | 策略 | `GET /strategies/{id}/history` |
| 回测中心 | `backtest_runs` + 关联 `sim_trades` | 交易 | `GET /backtest/runs` |

---

## 七、核心处理流程

### 7.1 策略执行循环（v1.3）

```
[每根 K 线 tick]
    ↓
[领域 1 合约行情更新] fetch_minute_data → 写 futures_min
    ↓
[领域 2 策略执行引擎] 遍历 (account_id, strategy_id, symbol, contract_code)
    ↓
    for each combination:
        ↓
        # 1. 动态加载策略
        strategy = load_strategy(strategy_id)  # → strategies/<type>/V<N>/strategy.py
        ↓
        # 2. 读 K线（领域 1）
        kline = SELECT * FROM futures_min WHERE symbol=? AND datetime=?
        ↓
        # 3. 读持仓事实（领域 3）
        open_session = SELECT * FROM trade_sessions 
                       WHERE account_id=? AND strategy_id=? AND symbol=? AND status='open' 
                       LIMIT 1
        open_units = SELECT * FROM position_units 
                     WHERE account_id=? AND session_id=?
        ↓
        # 4. 计算 phase
        if open_session is None: phase = 'idle'
        else: phase = 'in_position'
        ↓
        # 5. 策略执行
        signal = strategy.on_bar(kline, open_session, open_units, overrides)
        ↓
        # 6. 写信号
        if signal:
            INSERT INTO strategy_signals (...)  // 只记开仓事件
            create_sim_order(signal, current_price, account_id, strategy_id)
    ↓
[领域 3 撮合引擎]
    match_order(order, current_price)
    ↓
    INSERT INTO sim_trades (account_id, ..., session_id)
    ↓
    if offset == 'open':
        INSERT INTO trade_sessions (account_id, entry_basis_price, entry_locked_atr, ...)
        INSERT INTO position_units (account_id, unit_id=1, ...)
    elif offset == 'add':
        UPDATE trade_sessions SET current_units = current_units + 1
        INSERT INTO position_units (account_id, unit_id=N, ...)
    elif offset == 'close':
        UPDATE trade_sessions SET status='closed', exit_time=now
        UPDATE position_units SET status='closed'
```

### 7.2 撮合核心代码（同 v1.2，伪代码略）

### 7.3 价格线计算（v1.3 核心新增）

见 §五

### 7.4 用户调价流程

```
[用户在 K 线图上拖动水平线]
    ↓
[前端计算新价格 + line_type]
    ↓
PUT /session/{id}/line/{line_type}?account_id=...
{"new_price": 18830}
    ↓
[后端] override_line_price()
  - 校验 line_type
  - 校验 session.status（仅 open / pending 可改）
  - 解析 price_overrides_json
  - 设置 overrides[line_type] = new_price
  - 写库
    ↓
[前端立即重新调用] GET /session/{id}/lines
  - 服务端实时重算所有线
  - 包括子线（add_unit_2/3/4/stop_loss）的级联重算
    ↓
[K 线图刷新所有水平线]
```

### 7.5 Session 生命周期与触发源（★ v1.4 核心新增）

**v1.3 遗留问题**：

- v1.3 只说"建仓瞬间建 session"，没说手动操作（建仓/加仓/减仓/平仓）怎么复用同一 session_id
- v1.3 没说"信号触发 session"与"手动建仓 session"的差异
- v1.3 没说并发场景（信号 + 手动同时）怎么处理

**v1.4 解决方案**：6 个触发源场景 + DB / 应用层双层唯一性约束 + 完整流程图。

#### 7.5.1 触发源矩阵（6 个场景）

| 场景 | 触发源 | session 来源 | 唯一性保证 | 备注 |
|------|-------|-------------|-----------|------|
| 1. 信号触发开仓 | `strategy_signals`（entry_long/entry_short）| 新建 | DB UNIQUE | 主动建仓 |
| 2. 手动建仓 | `POST /session` | 新建 | DB UNIQUE + 应用锁 | 手动建仓 |
| 3. 信号触发加仓 | `strategy_signals`（add_unit）| 复用 open session | DB UNIQUE | 加仓到现有 session |
| 4. 手动加仓/减仓 | `POST /session/{id}/orders` | 复用指定 session | 应用锁 | 必须传 session_id |
| 5. 信号触发止损/退出 | `strategy_signals`（stop/exit）| 复用 open session | DB UNIQUE | 触发后改 status='closed' |
| 6. 全部平仓（手动）| `POST /session/{id}/close` | 复用指定 session | 应用锁 | 一次平所有 Unit |

#### 7.5.2 Session 唯一性约束（DB 层 + 应用层）

**DB 层**（`trade_sessions` 表 v1.3 已有）：

```sql
-- 同一 (账户, 策略, 方向, 建仓日期) 只能有 1 个 session
UNIQUE (account_id, strategy_id, direction, entry_time)
-- 例：AG 在 2026-05-30 turtle_v1 多头 sim_default 只能 1 个 session
-- 例：AG 在 2026-05-31 turtle_v1 多头 sim_default 又可以开 1 个新 session
-- 不同日期可以多次会话
```

**应用层**（v1.4 决策点 6：互斥锁）：

```python
# api/session_lock.py
# 同一 (account_id, strategy_id, symbol, contract_code, direction) 串行化
_session_locks: dict[tuple, asyncio.Lock] = {}

async def acquire_session_lock(account_id, strategy_id, symbol, contract_code, direction) -> Lock:
    key = (account_id, strategy_id, symbol, contract_code, direction)
    if key not in _session_locks:
        _session_locks[key] = asyncio.Lock()
    lock = _session_locks[key]
    
    # 非阻塞获取，失败返 HTTP 423
    if lock.locked():
        raise HTTPException(
            status_code=423,  # Locked
            detail=f"Session for {symbol}/{direction} is being processed, please retry"
        )
    
    await lock.acquire()
    return lock

# 使用示例：POST /session（手动建仓）
async def create_session_handler(req: CreateSessionRequest):
    lock = await acquire_session_lock(
        req.account_id, req.strategy_id, req.symbol, req.contract_code, req.direction
    )
    try:
        session = create_trade_session(...)
        return session
    finally:
        lock.release()
```

**应用层互斥锁 vs DB 事务锁**：

- v1.4 选应用层互斥锁（`asyncio.Lock`）
- 理由：信号 + 手动并发的概率低，应用层锁足够；DB 事务锁会拖慢撮合
- 决策点 6：明确选应用层锁

#### 7.5.3 完整流程图（v1.4 重构版）

```
┌─────────────────────────────────────────────────────────────────────┐
│                       触发源（6 个场景）                            │
├─────────────────────────────────────────────────────────────────────┤
│  [信号] 策略执行引擎每 K 线 tick                                    │
│      ↓ entry_long/entry_short 检测                                  │
│      ↓ add_unit_1/2/3/4 触发                                        │
│      ↓ stop_loss / exit_20 触发                                      │
│                                                                      │
│  [手动] 用户点击操作按钮                                              │
│      ↓ POST /session（建仓）                                         │
│      ↓ POST /session/{id}/orders（加仓/减仓）                        │
│      ↓ POST /session/{id}/close（全部平仓）                          │
└───────────────────────────────┬─────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       API 路由层                                    │
│  - 校验账户/策略/品种                                                │
│  - acquire_session_lock() 应用层互斥锁                              │
│  - 冲突返 HTTP 423                                                   │
└───────────────────────────────┬─────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       撮合引擎                                       │
│  - INSERT sim_orders (订单委托)                                      │
│  - INSERT sim_trades (成交)                                          │
│  - INSERT/UPDATE trade_sessions (session 状态机)                     │
│  - INSERT position_units (Unit 详情)                                 │
│  - INSERT/SELECT turtle_session_data (海龟特有)                      │
│  - INSERT session_event_log (事件流水)                               │
└───────────────────────────────┬─────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       前端 5 个 Tab + 操作组                          │
│  - [信号] Tab：显示当前/历史信号                                      │
│  - [模拟] Tab：当前持仓 + Unit 详情 + Session 详情 + 价格线          │
│  - [回测] Tab：历史回测列表                                           │
│  - [策略] Tab：参数 + 历史 + strategy_event_log                       │
│  - [Session] Tab：open/closed session 列表 + session_event_log       │
└───────────────────────────────┬─────────────────────────────────────┘
                                ↓
┌─────────────────────────────────────────────────────────────────────┐
│                       价格线 API                                    │
│  - GET /session/{id}/lines（实时计算）                                │
│  - PUT /session/{id}/line/{line_type}（用户调价）                     │
│  - GET /session/{id}/events（事件流）                                │
└─────────────────────────────────────────────────────────────────────┘
```

### 7.6 用户操作流程（★ v1.4 核心新增）

**背景**：v1.3 的 7.1 流程图只覆盖"信号触发"路径，没说"手动建仓/加仓/减仓/平仓"。

**v1.4 解决方案**：4 个用户操作流程图，与信号路径形成完整闭环。

#### 7.6.1 手动建仓流程

```
[用户在详情页点击"📈 建仓"按钮]
    ↓
[前端弹出"建仓参数模态框"]
  - 品种（自动带入当前品种）
  - 合约代码（下拉选择 / 自动选主力）
  - 方向（多 / 空）
  - 手数（默认 1 Unit）
  - 策略（下拉：海龟V1 / V2）
  - 账户（下拉：sim_default / sim_user1 / sim_user2）
  - 确认按钮
    ↓
[用户点确认 → POST /session]
{
  "account_id": "sim_default",
  "strategy_id": "turtle_v1",
  "symbol": "AG",
  "contract_code": "ag2607",
  "direction": "long",
  "hand_count": 1,
  "basis_price": 18850,    // 用户填的入场参考价（可选，默认 55日高/低）
  "atr": 1036,             // 用户填的 ATR（可选，默认 20日EMA）
  "current_price": 18810   // 当前市价（前端从 /quotes/{contract_code} 拿）
}
    ↓
[后端 create_session_handler()]
  - acquire_session_lock() 应用层互斥锁
  - 校验 account/strategy/symbol 合法
  - 校验：同 (account, strategy, symbol, direction, today) 不能已有 open session
  - INSERT trade_sessions (status='pending', entry_basis_price=basis_price, entry_locked_atr=atr)
  - INSERT turtle_session_data (entry_55d_high, entry_55d_low, entry_atr_20)
  - INSERT session_event_log (event_type='session_created', trigger_source='manual')
  - 创建 sim_orders (order_type='market', offset_flag='open')
    ↓
[撮合引擎]
  - match_order(order, current_price)
  - INSERT sim_trades (offset='open', price=current_price)
  - UPDATE trade_sessions SET status='open', first_entry_price=current_price, current_units=1
  - INSERT position_units (unit_id=1, entry_price=current_price)
  - INSERT session_event_log (event_type='order_filled' + 'status_changed')
    ↓
[前端]
  - 关闭模态框
  - 模拟 Tab 自动刷新：持仓 + Unit + Session + 价格线
  - 顶部状态徽章更新：持仓 = 1
```

#### 7.6.2 手动加仓流程

```
[用户在详情页模拟 Tab 点击"➕ 加仓"按钮]
    ↓
[前端弹出"加仓参数模态框"]
  - 当前 Session（自动带入）
  - 手数（默认 1 Unit）
  - 加仓价格（默认 add_unit_N 算法价，可手动改）
  - 确认按钮
    ↓
[用户点确认 → POST /session/{session_id}/orders]
{
  "account_id": "sim_default",
  "hand_count": 1,
  "price": 18870,    // 用户填的加仓价（默认 add_unit_N 算法价）
  "offset_flag": "add",
  "current_price": 18865
}
    ↓
[后端 add_position_handler()]
  - acquire_session_lock() 应用层互斥锁
  - 校验 session 存在 + status='open'
  - 校验 current_units < max_units（默认 4）
  - 创建 sim_orders
  - 撮合成交
  - UPDATE trade_sessions SET current_units=current_units+1
  - INSERT position_units (unit_id=current_units+1)
  - UPDATE turtle_session_data SET add_count=add_count+1
  - INSERT session_event_log (event_type='order_filled' + 'units_changed')
    ↓
[前端]
  - 关闭模态框
  - 模拟 Tab 自动刷新：持仓 + Unit + Session + 价格线（add_unit_N 变实际成交价）
  - 子线（add_unit_N+1, N+2, ...）自动级联重算
```

#### 7.6.3 手动减仓流程

```
[用户在详情页模拟 Tab 点击"➖ 减仓"按钮]
    ↓
[前端弹出"减仓参数模态框"]
  - 当前 Session（自动带入）
  - 减仓哪个 Unit（默认最后 Unit，下拉可改）
  - 手数（默认该 Unit 全部手数）
  - 减仓价格（默认市价，可手动改）
  - 确认按钮
    ↓
[用户点确认 → POST /session/{session_id}/orders]
{
  "account_id": "sim_default",
  "unit_id": 2,           // 减仓第 2 个 Unit
  "hand_count": 1,
  "price": 18950,         // 减仓价
  "offset_flag": "reduce",
  "current_price": 18940
}
    ↓
[后端 reduce_position_handler()]
  - acquire_session_lock() 应用层互斥锁
  - 校验 session 存在 + status='open'
  - 校验 unit_id 在 open units 中
  - 创建 sim_orders
  - 撮合成交
  - UPDATE position_units SET status='closed', close_time=now, close_price=price
  - UPDATE trade_sessions SET current_units=current_units-1
  - INSERT session_event_log (event_type='order_filled' + 'units_changed')
    ↓
[前端]
  - 关闭模态框
  - 模拟 Tab 自动刷新：Unit 状态变化
  - 价格线子线（add_unit_3, 4, stop_loss）级联重算
```

#### 7.6.4 全部平仓流程

```
[用户在详情页模拟 Tab 点击"🛑 全部平仓"按钮]
    ↓
[前端弹出"平仓确认模态框"]
  - 当前 Session（自动带入）
  - 当前 Units：N/4
  - 预估盈亏（来自 sim_positions）
  - 二次确认（必勾选"我已知晓盈亏"）
  - 确认按钮
    ↓
[用户点确认 → POST /session/{session_id}/close]
{
  "account_id": "sim_default",
  "current_price": 18850
}
    ↓
[后端 close_session_handler()]
  - acquire_session_lock() 应用层互斥锁
  - 校验 session 存在 + status='open'
  - 对每个 open unit 创建 sim_orders（offset='close'）
  - 撮合全部成交
  - UPDATE position_units SET status='closed', close_time=now
  - UPDATE trade_sessions SET status='closed', exit_time=now, exit_price=avg, realized_pnl=sum
  - INSERT session_event_log (event_type='status_changed' from 'open' to 'closed')
    ↓
[前端]
  - 关闭模态框
  - 模拟 Tab 自动刷新：持仓清空 + Session 移到 closed
  - 顶部状态徽章更新：持仓 = 无，信号 = 无
```

### 7.7 海龟交易过程流程（v1.5 新增）

```
[每根 K 线 tick 后]
    ↓
[海龟策略 on_bar 流程]
    ↓
    1. 是否在持仓？(查 trade_sessions.status='open')
       - 否：扫 55日突破 → 触发 entry_signal
       - 是：进入持仓管理 ↓
    ↓
    2. 持仓管理（v1.5 强化退仓优先）：
       a. 20日反向检查（check_exit，触发存前后 5 根）
          - 多头：bar_low < 20日低 → exit_20_triggered → 全部平仓
          - 空头：bar_high > 20日高 → exit_20_triggered → 全部平仓
       b. 止损检查（stop_loss_check，每个 unit，触发存前后 5 根）
          - unit_1.stop_price > bar_low → stop_loss_triggered → 减仓 1 unit
          - unit_2.stop_price > bar_low → stop_loss_triggered → 减仓 1 unit
          - ...
       c. 加仓信号检查（add_signal）
          - 多头：bar_high > last_unit.entry_price + 0.5N
          - 空头：bar_low < last_unit.entry_price - 0.5N
          - 检查跳空：bar_open > 加仓价 + min_unit → add_skipped_gap
                     否则 → add_filled
    ↓
    3. **退仓优先顺序（v1.5 决策点 2）**：
       - 同根 K 线同时触发止损 + 加仓时，**止损优先**（保守策略）
       - 实现：先扫 exit_20 → 再扫 stop_loss → 最后扫 add
    ↓
    4. 写 turtle_trade_process（每个决策按 C 方案写）
    ↓
    5. 写 strategy_event_log（重要决策才写）
    ↓
    6. 触发 entry/exit/add 时同步写 session_event_log
```

**4 Unit 加仓模型**（v1.5 决策点 1）：

| Unit | 类型 | 间隔 | 累计手数 | 止损价（基于建仓价）|
|------|------|------|---------|------------------|
| Unit 1 | 首仓 | 0 | 1 | entry - 2N |
| Unit 2 | 加仓 1 | 0.5N | 2 | entry_2 - 2N |
| Unit 3 | 加仓 2 | 0.5N | 3 | entry_3 - 2N |
| Unit 4 | 加仓 3 | 0.5N | 4 | entry_4 - 2N |

**最多加仓 3 次 = 4 Unit**（v1.5 决策点 1 确认）

---

## 八、API 改造清单

### 8.1 新增 API

| 方法 | 路径 | 用途 | 涉及领域 |
|------|------|------|---------|
| GET | `/accounts` | 列出所有账户 | 交易 |
| POST | `/accounts` | 创建新账户 | 交易 |
| GET | `/strategies?account_id=...` | 列出策略 | 策略 |
| GET | `/strategies/{id}/history` | 策略参数历史 | 策略 |
| POST | `/strategies` | 注册新策略 | 策略 |
| PUT | `/strategies/{id}/params` | 更新参数（自动写 history） | 策略 |
| GET | `/contracts/{symbol}` | 品种下所有合约 | 合约 |
| GET | `/quotes/{contract_code}` | 合约实时行情 | 合约 |
| GET | `/strategy/signal/state/{sym}?strategy_id=...` | 当前信号状态 | 策略 |
| GET | `/strategy/signals/{sym}?strategy_id=...` | 历史信号 | 策略 |
| GET | `/sim/positions?account_id=...` | 模拟持仓 | 交易 |
| GET | `/sim/orders?account_id=...` | 模拟委托 | 交易 |
| GET | `/sim/trades?account_id=...` | 模拟成交 | 交易 |
| GET | `/sim/account/{account_id}` | 模拟账户 | 交易 |
| GET | `/sim/sessions?account_id=...&strategy_id=...` | 模拟 session | 交易 |
| POST | `/sim/order` | 手动模拟下单 | 交易 |
| **GET** | **`/session/{id}/lines?account_id=...`** | **★ 实时计算价格线** | **跨领域** |
| **PUT** | **`/session/{id}/line/{line_type}?account_id=...`** | **★ 用户调价** | **交易** |
| GET | `/account/overview?account_id=...&strategy_id=...` | 账户总览 | 交易 |
| GET | `/backtest/runs` | 回测列表 | 交易 |
| GET | `/backtest/{run_id}` | 回测详情 | 交易 |
| GET | `/backtest/{run_id}/trades` | 回测成交 | 交易 |
| **GET** | **`/session/{id}/trade-process`** | **★ v1.5 新增 — 海龟交易过程流水** | **策略** |

### 8.2 改造/废弃 API

| 旧 API | 处理 |
|--------|------|
| `/turtle/signals/{sym}` | 保留，重定向到 `/strategy/signals/{sym}?strategy_id=turtle_v1` |
| `/turtle/signals/active` | 重定向到 `/strategy/signal/state/all?strategy_id=turtle_v1` |
| `/position/{sym}` | 保留（兼容），重定向到 `/sim/positions?symbol=...&account_id=...` |
| `/position/{sym}/add` | 保留，重定向到 `/sim/order` |

---

## 九、实施计划（v1.3）

### Phase 1：合约行情领域（3-4 天）
- [ ] `futures_contracts` 表 + `init_db` 自动迁移
- [ ] `futures_daily` 表迁移（v1.3 调整）：
  - 新增 `atr` 字段（从 `futures_atr` 合并）
  - 保留期从 14 天改为 3 年（1095 天）
  - 初始化时全量回填近 3 年日线
  - 索引 `(symbol, date)`
- [ ] `futures_min` 表迁移（v1.3 调整）：
  - 新增 `date` 字段（从 `datetime` 派生，便于与 `futures_daily` 关联）
  - 保留期从 7 天改为半年（180 天）
  - 初始化时全量回填近半年分时
  - 索引 `(symbol, date)` 关键用于关联查询
- [ ] `futures_atr` 表数据迁移 + 删除（原数据 INSERT 到 `futures_daily.atr`）
- [ ] 数据采集：每次 `sync_all_minute_data` 时更新合约列表
- [ ] 主力合约识别逻辑
- [ ] API：`GET /contracts/{symbol}`、`GET /quotes/{contract_code}`
- [ ] 前端：品种网格加"📋 N 合约"角标 + 合约抽屉
- **验收**：点 AG 卡片能弹出 4 个合约的报价列表；日线/分时表 retention 分别是 3 年/半年

### Phase 2：策略领域（含多版本代码组织）（3 天）
- [ ] **代码组织重构**：
  - 建立 `strategies/turtle/V1/` 目录
  - 把现有 `strategies/turtle/position.py` 移到 `strategies/turtle/V1/`
  - 实现 `strategies/loader.py`（动态加载器）
  - 实现 `strategies/base.py`（BaseStrategy 抽象基类）
- [ ] `strategies` 表 + `version` 字段 + 种子数据
- [ ] `strategy_param_history` 表
- [ ] `strategy_signal_states` 表
- [ ] `strategy_signals` 表升级
- [ ] API：`GET /strategies`
- [ ] 前端：顶部策略下拉
- [ ] 新增"策略历史"页
- **验收**：
  - 切换 turtle_v1 / turtle_v2 都能加载对应算法
  - 修改 V1 代码不影响 V2
  - 策略历史能查看参数迭代

### Phase 3：交易 / 账户领域（v1.3 强化多账户）（6-7 天）
- [ ] `sim_account` 表 + `user_id` / `display_name` 字段
- [ ] `trade_sessions` 表 + `account_id` + `price_overrides_json`
- [ ] `sim_orders` / `sim_trades` / `sim_positions` / `position_units` 加 `account_id`
- [ ] 撮合引擎 `sim_engine.py`
- [ ] 把 `PositionManager` 改造为持久化版
- [ ] **价格线计算 API**：`GET /session/{id}/lines`（v1.3 核心）
- [ ] **用户调价 API**：`PUT /session/{id}/line/{line_type}`（v1.3 核心）
- [ ] 前端 K 线图集成价格线
- [ ] API：`/sim/*` 加 `account_id` 参数
- [ ] 账户切换器（顶部）
- [ ] 前端：详情"模拟"Tab + 模拟下单按钮
- **验收**：
  - 多账户并存（sim_default / sim_user1 / sim_user2）
  - K 线图显示价格线（红/蓝/紫三色区分）
  - 用户拖动水平线调价，实时生效
  - 手动模拟开仓/加仓/平仓，重启服务后持仓仍在
- [ ] **3.15** ★ **新增 `turtle_trade_process` 表 + `init_db`**（v1.5）
- [ ] **3.16** ★ **策略执行引擎：`on_bar` 流程改造**（v1.5 退仓优先顺序：止损 > 加仓）
- [ ] **3.17** ★ **check 类事件按 C 方案实现**（触发存前后 5 根 K 线）
- [ ] **3.18** ★ **新增 API：`GET /session/{id}/trade-process`**（v1.5）

### Phase 4：回测持久化（3 天）
- [ ] `backtest_runs` 表
- [ ] `BacktestEngine` 改造：每个成交写 `sim_trades`（带 `backtest_run_id` / `session_id` / `account_id`）
- [ ] API：`/backtest/runs`、`/backtest/{run_id}/trades`
- [ ] 前端：详情"回测"Tab 改为"历史回测列表"
- **验收**：跑一次回测，重启后历史回测列表能列出

### Phase 5：账户中心页（2 天）
- [ ] API：`/account/overview`（聚合 sim_account + sim_positions + position_units + trade_sessions）
- [ ] 前端：`/account` 页面（总览/持仓列表/Unit 明细/当前 Session/价格线/成交记录）
- [ ] URL 参数 `?account=&strategy=` 全局贯通
- **验收**：账户中心能看到完整的 4 Unit 明细 + 当前 Session 锁定值 + 价格线

### Phase 6：单元 + 集成测试（2-3 天）
- [ ] 撮合引擎单测
- [ ] 价格线计算单测（含 override 优先级）
- [ ] trade_session 生命周期单测
- [ ] 多账户隔离单测
- [ ] 策略版本切换单测（V1 vs V2）
- [ ] 端到端测试：信号 → 模拟下单 → 成交 → 持仓 → 价格线更新
- [ ] 与 0016 字段对齐测试

### Phase 7（可选）：CTP 接入（后续）
- 阶段二-C 的实盘对接工作

**总计**：~19-22 工作日，分 6 个交付里程碑

---

## 十、待确认项

| # | 问题 | 当前默认 | 备选 |
|---|------|---------|------|
| 1 | 策略切换是否影响历史数据？ | 不影响（只影响新数据查询） | 切换时清空前端状态 |
| 2 | 默认策略资金/默认品种范围 | 全品种 + 100万 | 用户可配置 |
| 3 | 模拟撮合：市价单 / 限价单默认 | 全部市价单（信号价成交） | 按策略可配 |
| 4 | 模拟手续费率 | 用 instruments.yaml 的费率（待补） | 固定 2 元/手 |
| 5 | 是否废弃 `turtle_signals` 表 | 保留 + 视图兼容 | 迁移后删除 |
| 6 | 真实持仓表是否在本期实施 | 不实施，仅建表预留 | 直接接 CTP |
| 7 | 是否需要"多账户"概念 | 是，v1.3 默认 sim_default + sim_user1 + sim_user2 | 简化为单一 default |
| 8 | 持仓 Unit 超过 4 个的处理 | 拒绝（对齐原版海龟） | 允许但报警 |
| 9 | 策略参数历史是否保留全部 | 全部保留（无上限） | 仅最近 N 版 |
| 10 | 信号主表刷新频率 | 每次新信号触发时 UPSERT | 每 30s 批量刷新 |
| 11 | trade_session.status='closed' 后保留多久 | 永久保留（用于审计） | 保留 N 天后归档 |
| 12 | 回测是否产生 trade_sessions | 是，但用 account_type 区分 | 用 backtest_run_id 字段 |
| 13 | 用户调价是否限定仅 in_position 阶段 | 是 | 全程可改 |
| 14 | 用户调价后子线是否级联重算 | 是 | 调哪个改哪个 |
| 15 | 策略 V1 / V2 是否完全隔离（不共享代码） | 是 | 允许部分共享（utils/） |
| 16 | `turtle_trade_process` 数据量预估与归档策略 | 半年（180 天）后归档 | 1 年后归档 / 不归档 |
| 17 | check 类事件 C 方案触发存 5 根是否够？ | 5 根够调试用 | 10 根上下文更全 |
| 18 | `dual_ma_trade_process` 何时启动设计？ | v1.5 暂不做，等加 dual_ma 时 | v1.6 一起做 |

---

## 十一、风险与回退

### 11.1 风险

1. **数据迁移风险**：新表与旧表共存
2. **API 兼容风险**：旧 `/turtle/signals/*` 保留重定向
3. **性能风险**：16 张表，单次回测可能产生数千行 trades（SQLite 够用）
4. **撮合逻辑风险**：与实盘 CTP 撮合规则有差异
5. **领域边界风险**：策略误写交易事实
   - 缓解：交易领域的写操作封装在 sim_engine.py / 撮合引擎内
6. **价格线计算开销**：每次 K 线更新都重算所有 session 的线
   - 缓解：缓存（仅在 trade_sessions 变化时失效）
7. **策略代码组织**：V1 / V2 物理隔离可能代码重复
   - 缓解：允许 utils/ 共享目录
8. **海龟过程数据量**：单 session 30-50 条 × 1000 session/年 ≈ 30-50k 条/年，可接受
9. **check 类事件 C 方案**：触发前 5 根 + 触发 + 后 5 根 = 10 条，调试够用

### 11.2 回退方案

- 所有新表独立，旧表不动 → 任何阶段可丢弃新表回到 v0.8.5
- API 路由保留兼容 → 旧前端继续工作
- 数据库备份：`futures_akshare.db` 每次 Phase 实施前 `cp` 备份
- 策略代码：保留 v0.8.5 路径 `strategies/turtle/`，新代码放 `strategies/turtle/V1/`

---

## 十二、附录：0016 通达信字段映射（v1.3 强化）

| 0016 字段 | 中文 | 新表对应字段 | 领域 |
|-----------|------|-------------|------|
| 开仓周期 / 离场周期 / 加仓间隔倍数 / 止损倍数 | 策略参数 | `strategies.params_json` | 策略 |
| 真实波幅 / ATR波动率 | N 值 | 内存计算 | — |
| 单笔最大亏损 | 风控参数 | `strategies.params_json.risk_per_trade` | 策略 |
| 持多 / 持空 | 持仓方向 | `trade_sessions.direction` | 交易 |
| 加1价 / 加2价 / 加3价 | 加仓点 | 实时计算 + `price_overrides_json` | 交易 |
| 距多开 / 距空开 | 持仓天数 | `trade_sessions.entry_time` + 当前时间 | 交易 |
| 实际加仓次数 | 单元数 | `trade_sessions.current_units` | 交易 |
| 总持仓 | 总手数 | `sim_positions.total_hand` | 交易 |
| 初始锁定 ATR | 锁定 N 值 | `trade_sessions.entry_locked_atr` | 交易 |
| 固定55高_开仓时 | 基准价 | `trade_sessions.entry_basis_price` | 交易 |
| 加仓信号 | 信号事件 | `strategy_signals.signal_type='add_unit'` | 策略 |
| 止损价 | 止损位 | 实时计算 + `price_overrides_json` | 交易 |
| 行为阶段 | phase | 实时算（基于 `trade_sessions.status`） | 策略 |
| 信号点 / 埋点 | 预测价 | 实时计算（不持久化） | — |

**v1.3 关键变化**：
- 加仓价 / 止损价 不再是存储字段，而是"实时计算 + 用户 override"
- `price_overrides_json` 唯一持久化用户调整

---

## 十三、附录：表清单速查（v1.4）

### 领域 1：合约行情（4 张）

| # | 表名 | 状态 | 主要字段 |
|---|------|------|---------|
| 1 | futures_contracts | v1.2 新增 | 合约静态信息 + is_main |
| 2 | futures_quotes | v1.2 新增 | 合约实时行情 |
| 3 | futures_daily | 保留 | 日 K 线 |
| 4 | futures_min | 保留 | 分钟 K 线 |

### 领域 2：策略（6 张 + 代码组织 + 事件流水）

| # | 表名 / 文件 | 状态 | 说明 |
|---|-----------|------|------|
| 5 | strategies | v1.2 +version | 策略主表 |
| 6 | strategy_param_history | v1.1 新增 | 参数迭代历史 |
| 7 | strategy_signals | v1.0 +strategy_id | 信号流水（仅开仓） |
| 8 | ~~strategy_signal_states~~ | v1.3 删除 | 派生数据不持久化，实时 JOIN 算 |
| **9** | **`strategy_event_log`** | **★ v1.4 新增** | **策略执行引擎事件流水（每根 K 线）** |
| **10** | **`turtle_trade_process`** | **★ v1.5 新增** | **海龟交易过程流水** |
| 代码 | `strategies/<type>/V<N>/strategy.py` | v1.3 新增 | 多版本代码组织 |

### 领域 3：交易 / 账户（8 张 + 1 预留 + 1 子策略扩展 + 1 事件流水）

| # | 表名 | 状态 | 主要字段 |
|---|------|------|---------|
| 11 | trade_sessions | v1.2 +account_id +price_overrides_json | 一次完整交易主表（通用） |
| 12 | sim_account | v1.0 +user_id +display_name +platform_name | 模拟账户 |
| 13 | sim_orders | v1.0 +account_id +platform_name +external_order_id | 模拟委托 |
| 14 | sim_trades | v1.0 +account_id +platform_name +external_trade_id | 模拟成交 |
| 15 | sim_positions | v1.0 +account_id | 模拟持仓汇总 |
| 16 | position_units | v1.0 +account_id（v1.4 注释：海龟字段为主） | 4 Unit 详情 |
| **17** | **`turtle_session_data`** | **★ v1.4 新增** | **海龟子策略 session 特有事实（7 字段）** |
| **18** | **`session_event_log`** | **★ v1.4 新增** | **session 生命周期事件流水（8 个 event_type）** |
| 19 | live_orders | 预留 +account_id | 真实委托 |
| 20 | live_trades | 预留 +account_id | 真实成交 |
| 21 | live_positions | 预留 +account_id | 真实持仓 |

### 跨领域

| # | 表名 | 状态 | 说明 |
|---|------|------|------|
| 22 | backtest_runs | v1.0 保留 | 回测任务 |

**总表数 22 张**（其中 3 张预留 live_* + v1.4 新增 3 张 + v1.5 新增 1 张：turtle_trade_process）

**v1.3 → v1.4 → v1.5 表数变化**：
- v1.3 → v1.4：18 → 21（+3：turtle_session_data / session_event_log / strategy_event_log）
- v1.4 → v1.5：21 → 22（+1 turtle_trade_process）
- 领域 2 策略：4 → 5 → 6（v1.4 +strategy_event_log / v1.5 +turtle_trade_process）
- 领域 3 交易账户：8 → 10 → 10（v1.4 +turtle_session_data +session_event_log，v1.5 不变）
- ~~dual_ma_session_data~~ 不预留（v1.4 决策点 2：避免空表占位维护成本）
- ~~dual_ma_trade_process~~ v1.5 不做（决策点 4：等加 dual_ma 时再设计）

---

*本文档由设计阶段产出，待 Alex 审核确认后进入实施阶段。*
