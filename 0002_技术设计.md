# Stock Quant - 技术设计

## 1. 技术选型

### 数据获取
- **akshare**：免费开源的财经数据接口，支持A股、基金、期货等
- **tushare**：可选，国内量化常用的数据源（需要积分）
- **baostock**：免费，无需积分

### 数据分析
- **pandas**：核心数据处理
- **numpy**：数值计算

### 回测框架
- **backtrader**：轻量级回测框架，社区活跃，适合个人研究
- **rqalpha**：聚宽开源回测框架（社区较小，维护不稳定，暂不推荐）
- **VN.py**：完整实盘框架，计划在研究成熟后对接
- **self-contained**：自研（调研阶段暂不需要）

### 因子研究
- **XGBoost/LightGBM**：工业级因子挖掘，效果好，可解释性强
- **scikit-learn**：传统机器学习辅助工具
- **深度学习（Transformer/LSTM）**：研究阶段探索，实盘暂不推荐（过拟合风险高）

### AI/机器学习应用（2024-2026趋势）
- **主流模型**：XGBoost/LightGBM > Transformer > LSTM
- **应用场景**：因子挖掘 > 选股信号 > 择时
- **风险提示**：深度学习实盘衰减严重，建议以机器学习+传统因子为主

### 可视化
- **matplotlib/seaborn**：数据可视化

## 2. 目录结构

```
stock-quant/
├── api/                      # FastAPI 服务（核心入口）
│   ├── __init__.py
│   └── main.py               # API 主程序，端口 8000
├── backtest/                 # 回测引擎模块
│   ├── backtest_engine.py    # Backtrader 封装
│   ├── config.py             # 回测配置
│   └── data_loader.py        # 回测数据加载
├── strategies/               # 策略模块
│   └── turtle/               # 海龟交易策略
│       ├── factors.py        # 因子计算（Unit/ATR/止损公式）
│       ├── position.py       # 仓位管理（Unit/加仓/止损阶梯）
│       └── tests/            # 单元测试
├── data/                     # 数据层
│   └── data_loader.py        # akshare 数据获取 + 缓存
├── scripts/                  # 工具脚本
│   ├── run_backtest.py       # 回测样例脚本
│   └── test_engine.py        # 引擎测试脚本
├── vnpy_strategies/          # VN.py 实盘策略（预留）
│   └── cta_strategies/
├── www/                      # 前端页面（静态 HTML）
│   └── index.html            # 期货行情看板
├── config/                   # 配置文件
│   └── instruments.yaml      # 品种配置（40+ 品种）
├── notebooks/                # Jupyter 研究笔记
│   └── research/             # 调研文档
├── requirements.txt          # Python 依赖
└── *.md                      # 项目文档
```

## 3. 功能模块说明

### 3.1 Web 看板（前端面板）

**入口**：`http://localhost:8000`

**功能**：
- 33 个期货品种实时行情展示
- K 线图（5min/15min/30min/1H）
- 海龟信号时间线（55 日突破入场、20 日反向离市、加仓信号）
- 回测入口（选择品种、时间范围）
- 持仓状态查询

**启动方式**：见 §4.1

### 3.2 FastAPI 服务（API 层）

**入口**：`http://localhost:8000`（与前端同端口）

**主要接口**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 前端页面 |
| `/health` | GET | 健康检查 |
| `/signals/all` | GET | 批量获取所有品种信号 |
| `/signals/{symbol}` | GET | 获取指定品种信号 |
| `/position/{symbol}` | GET | 查询仓位状态 |
| `/position/{symbol}/add` | POST | 手动登记持仓 |
| `/backtest/run` | POST | 提交回测任务 |
| `/backtest/{job_id}` | GET | 查询回测结果 |
| `/minute/{symbol}` | GET | 查询分时数据 |
| `/minute/sync` | POST | 同步分时数据 |
| `/cache/status` | GET | 缓存状态诊断 |
| `/turtle/signals/{symbol}` | GET | 品种历史海龟信号 |
| `/turtle/signals/active` | GET | 当前未离市信号 |
| `/turtle/signals/alerts` | GET | 最近新信号（推送用） |
| `/turtle/scan/{symbol}` | POST | 扫描品种历史信号 |
| `/turtle/scan/all` | POST | 全量扫描所有品种 |

**启动方式**：见 §4.1

### 3.3 数据层（data/data_loader.py）

**功能**：
- akshare 日线/分钟线数据获取
- SQLite 缓存（增量更新）
- ATR/55日高/55日低因子计算
- 分时数据同步（33 品种自动找主力合约）

**关键函数**：

| 函数 | 说明 |
|------|------|
| `get_daily_data_from_cache()` | 获取日线数据 |
| `get_minute_from_cache()` | 获取分钟 K 线数据 |
| `sync_all_minute_data()` | 全量分时同步 |
| `get_daily_signal_from_cache()` | 获取每日信号因子 |
| `cache_status()` | 缓存诊断 |
| `get_contract_info()` | 获取主力合约信息 |
| `get_turtle_signals()` | 查询历史信号 |
| `get_active_signals()` | 查询未离市信号 |
| `detect_signals_for_symbol()` | 检测品种信号 |

### 3.4 海龟策略（strategies/turtle/）

**factors.py** - 因子计算：

| 函数 | 说明 |
|------|------|
| `calc_unit_size()` | 计算每 Unit 手数 |
| `calc_add_interval()` | 计算加仓间隔（0.5N）|
| `calc_stop_loss()` | 计算止损距离（2N）|

**position.py** - 仓位管理：

| 类/函数 | 说明 |
|---------|------|
| `TurtleUnit` | 单个 Unit 数据类 |
| `TurtlePosition` | 单品种持仓类 |
| `PositionManager` | 全局持仓管理器 |

### 3.5 回测引擎（backtest/）

**backtest_engine.py** - Backtrader 封装：

| 类/函数 | 说明 |
|---------|------|
| `BacktestEngine` | 回测引擎类 |

**run_backtest.py** - 回测样例脚本

## 4. 启动方式

### 4.1 Web 看板 + API 服务（推荐）

```bash
cd ~/.openclaw/workspace/projects/stock-quant/api
python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
```

访问 `http://localhost:8000` 打开前端面板。

### 4.2 后台运行

```bash
cd ~/.openclaw/workspace/projects/stock-quant/api
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 > /tmp/stock_quant.log 2>&1 &
```

### 4.3 查看运行状态

```bash
# 检查进程
lsof -i :8000

# 查看日志
tail -f /tmp/stock_quant.log
```

### 4.4 单元测试

```bash
cd ~/.openclaw/workspace/projects/stock-quant
python3 -m pytest strategies/turtle/tests/test_turtle_unit.py -v
```

### 4.5 回测脚本样例

```bash
cd ~/.openclaw/workspace/projects/stock-quant
python3 scripts/run_backtest.py
```

## 5. 数据源对比

| 数据源 | 费用 | 数据范围 | 稳定性 |
|--------|------|----------|--------|
| akshare | 免费 | A股、期货、基金 | 较好 |
| tushare | 需要积分 | 全面 | 稳定 |
| baostock | 免费 | A股为主 | 较好 |
| 聚宽 | 付费 | 全面 | 稳定 |
