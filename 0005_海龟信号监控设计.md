# 海龟信号监控功能设计

## 背景目标

在**分钟K线上实时监控**海龟策略信号，发现命中时：
1. 在 K 线图上打标记（气泡/竖线）
2. 记录到数据库
3. 面板气泡提示

最终接入 web 面板，让用户一眼看到哪些品种当前命中了海龟信号。

---

## 信号类型

| 信号 | 触发条件 | 方向 | 说明 |
|------|---------|------|------|
| **55日突破入场（做多）** | 分钟收盘价 ≥ 55日最高价 | long | 核心信号 |
| **55日突破入场（做空）** | 分钟收盘价 ≤ 55日最低价 | short | 核心信号 |
| **20日反向离市（多头）** | 分钟收盘价 ≤ 20日最低价 | exit_long | 触发全平 |
| **20日反向离市（空头）** | 分钟收盘价 ≥ 20日最高价 | exit_short | 触发全平 |
| **加仓信号** | 价格相对上一持仓移动 ≥ 0.5N | add | 仅在有持仓时 |

---

## 数据库设计

### 新增表：turtle_signals

```sql
CREATE TABLE turtle_signals (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol TEXT NOT NULL,           -- 品种代码 AG
    name TEXT,                      -- 中文名 白银
    contract_code TEXT,              -- 主力合约 ag2605
    signal_type TEXT NOT NULL,       -- 'entry_long' | 'entry_short' | 'exit_long' | 'exit_short' | 'add_unit'
    trigger_price REAL NOT NULL,     -- 触发价格
    reference_price REAL NOT NULL,    -- 参考价格（55日高/低 or 20日高/低）
    reference_n REAL NOT NULL,        -- 当时的 N 值（ATR）
    direction TEXT NOT NULL,         -- 'long' | 'short' | 'flat'
    bar_time TEXT NOT NULL,          -- 触发时的分钟 Bar 时间 2026-05-30 09:35:00
    period TEXT NOT NULL,            -- K线周期 5min
    alert_level TEXT DEFAULT 'normal', -- 'normal' | 'strong'（是否在持仓中）
    created_at TEXT DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(symbol, signal_type, bar_time)
);

CREATE INDEX idx_turtle_signals_symbol ON turtle_signals(symbol);
CREATE INDEX idx_turtle_signals_time ON turtle_signals(bar_time);
```

---

## 核心逻辑

### 信号检测函数

```
检测每一根分钟K线时：

1. 获取55日最高/最低（来自日线缓存，固定一天一变）
2. 获取20日最高/最低（来自日线缓存）
3. 获取当前ATR（N值）

信号判断：
  IF close >= high_55 → 记录 entry_long，reference_price = high_55
  IF close <= low_55  → 记录 entry_short，reference_price = low_55
  IF direction == 'long' AND close <= low_20 → 记录 exit_long
  IF direction == 'short' AND close >= high_20 → 记录 exit_short
  IF 有持仓 AND close移动了0.5N AND 未满4Unit → 记录 add_unit
```

---

## K线标记展示

在 TradingView Lightweight Charts 上叠加**竖线/箭头标记**：

```
entry_long  → 绿色向上箭头 ▲（实心）
entry_short → 红色向下箭头 ▼（实心）
exit_long   → 绿色向下箭头 ▼（空心）
exit_short  → 红色向上箭头 ▲（空心）
add_unit    → 蓝色加号 +
```

---

## 面板展示设计

### 顶部品种网格（不变）
每个品种卡片显示：
- 代码 + 中文名 + 主力合约
- 当前价格 + 涨跌幅
- **信号指示器**：卡片左上角有一个小圆点
  - 绿色：命中做多信号（待入场）
  - 红色：命中做空信号（待入场）
  - 蓝色：当前有持仓
  - 灰色：无信号

### 详情K线图
点击品种展开详情时：
- K线图叠加信号标记竖线
- 鼠标悬停显示信号详情（类型、价格、时间、N值）
- 右侧显示**信号时间线**：该品种历史所有信号列表

### 信号推送（气泡提示）
当实时分钟数据触发新信号时：
- 页面右上角弹出气泡："AG 白银 触发做多信号 @ 18350（55日高18300）"
- 信号入库，品种卡片小圆点变绿色

---

## 实施计划

### Phase 1: 数据层
- [ ] `turtle_signals` 表建表脚本
- [ ] `save_turtle_signal()` 写入信号
- [ ] `get_turtle_signals(symbol, days)` 查询历史信号
- [ ] `get_active_signals()` 查询当前未离市的信号

### Phase 2: 信号检测逻辑
- [ ] `check_minute_bar_signal()` 单根K线信号检测
- [ ] `scan_all_signals()` 全品种扫描（用于回溯历史）
- [ ] 接入现有 `sync_all_minute_data`，在增量写入时顺便检测

### Phase 3: API 接口
- [ ] `GET /turtle/signals/{symbol}` 品种历史信号
- [ ] `GET /turtle/signals/active` 当前所有未离市信号
- [ ] `GET /turtle/signals/alerts` 未读的新信号（用于推送）

### Phase 4: 前端展示
- [ ] 品种网格卡片信号指示点
- [ ] K线图叠加信号标记
- [ ] 信号时间线侧边栏
- [ ] 实时气泡推送

---

## 关键实现细节

### 55日/20日数据的时效性
- 日线数据每天收盘后更新一次
- 分钟信号检测用的 high_55/low_55/high_20/low_20 **当天内固定不变**
- 这意味着：只要收盘价没破前一天算的55日高低，当天就不会产生新信号

### 实时信号 vs 历史回溯
- **实时模式**：每次 `sync_minute_data` 增量写入时，顺便检测最后一根新K线
- **回溯模式**：调用 `scan_all_signals()` 对历史分钟数据扫描（仅需一次）

### 信号去重
同一品种同一信号类型同一分钟时间戳只记录一次（UNIQUE约束）

### 持仓状态与信号强度
- 有持仓时触发的信号 alert_level = 'strong'（蓝色气泡）
- 无持仓时触发的突破信号 alert_level = 'normal'（等待入场）
