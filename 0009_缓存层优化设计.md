# 0009_缓存层优化设计

> 项目：Stock Quant（股票量化）
> 编号：0009
> 创建日期：2026-06-07
> 状态：🔄 设计阶段（用户拍板 7 决策，待执行）
> 关联版本：v0.18.5 → v0.18.6
> 关联需求：sq-0009

---

## 一、背景与现状

### 1.1 用户反馈

> 没有查到各个品类的 K 线的数据缓存,定位下原因,另外优化下缓存设计,
> 应该是有一个后台定时任务可以定期拉取前一天的分时数据和天数据,
> 另外有一个独立页面模块可以展示对应的缓存数据拉取记录,
> 也可以手动在界面拉取指定时间段的日数据和分时数据

### 1.2 现状摸查

| 项 | 状态 | 位置 |
|----|------|------|
| `futures_daily` / `futures_min` 表 | ✅ 已有 | `data/data_loader.py:137, 153` |
| `incremental_update_daily()` 增量更新 | ✅ 已有 | `data/data_loader.py:727` |
| `fetch_minute_data()` 缓存优先 + 懒加载 | ✅ 已有 | `data/data_loader.py:939` |
| `sync_all_minute_data()` 全 38 品种同步 | ✅ 已有 | `data/data_loader.py:1150` |
| `backfill_daily_history()` / `backfill_min_history()` 回填 | ✅ 已有 | `data/data_loader.py:1896, 1983` |
| `cache_status()` 缓存诊断 | ✅ 已有 | `data/data_loader.py:1377` |
| `POST /minute/sync` 后端端点 | ✅ 已有（半成品）| `api/routers/market.py:256` |
| `POST /minute/sync` 前端 service 方法 | ✅ 已有 | `webapp/src/services/market.ts:7` |
| `POST /minute/sync` 前端 view 实际调用 | ❌ 0 处 | — |
| `POST /daily/sync` 后端端点 | ❌ 无 | — |
| 后台定时任务（APScheduler / cron）| ❌ 无 | — |
| 拉取记录持久化（`cache_sync_log`）| ❌ 无 | — |
| 调度任务状态表（`cache_schedule_state`）| ❌ 无 | — |
| 按品种 × 周期覆盖率查询 | ❌ 无 | — |
| 拉取记录页 / 手动拉取页 | ❌ 无 | — |

### 1.3 根因

底层数据层 80% 函数已存在，缺的是「**调度器 + 记录层 + API 暴露 + 前端页**」4 个串联。
属于 v0.18.x 之前只做了"数据层"、缺"基础设施层"的典型遗漏。

---

## 二、7 决策汇总（用户拍板 2026-06-07）

| # | 决策 | 选项 | 含义 |
|---|------|------|------|
| 1 | 调度框架 | **APScheduler** | 轻量、进程内调度，SQLite 持久化作业存储 |
| 2 | 调度任务数 | **只开 daily + 5min** | 2 个常驻任务，不是 5 个 |
| 3 | 手动拉取时段 | **不限时段** | akshare 不限 |
| 4 | 覆盖率告警 | **v1 不做，v2 做** | 仅记录，不告警 |
| 5 | 新 view 命名 | **`MarketDataSync`** | 贴 market router |
| 6 | `/daily/sync` 职责 | **B — 只做拉取** | 信号检测独立（海龟因子是日盘收盘批量算）|
| 7 | 15/30/60min 缓存策略 | **C — 混合** | 调度跑 5min + 手动按需拉任意周期 + 前端降级 resample |

---

## 三、架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                       触发层 (3 路)                          │
├──────────────────────────────────────────────────────────────┤
│  1. APScheduler 定时       2. 手动 API     3. 前端 view    │
│     - daily_sync             POST /daily/sync   按钮触发   │
│     - minute_sync_5min       POST /minute/sync              │
│                              POST /cache/backfill           │
├──────────────────────────────────────────────────────────────┤
│                    核心 SSOT: api/cache_sync.py              │
│   - sync_daily_one() / sync_daily_all()                     │
│   - sync_minute_one() / sync_minute_all()                   │
│   - backfill_daily() / backfill_minute()                    │
│   - get_coverage() / get_sync_logs() / get_schedule_state() │
│   - 入口处统一写 cache_sync_log                              │
├──────────────────────────────────────────────────────────────┤
│                  存储层: data/data_loader.py                 │
│   - futures_daily / futures_min（已有）                      │
│   - cache_sync_log（新增）                                   │
│   - cache_schedule_state（新增）                             │
├──────────────────────────────────────────────────────────────┤
│                       端点层（api/routers/）                 │
│   market.py:    + POST /daily/sync                          │
│   cache.py:     + GET /cache/coverage                       │
│                 + GET /cache/sync/logs                      │
│                 + GET /cache/sync/logs/{id}                 │
│                 + GET /cache/schedule                       │
│                 + POST /cache/schedule/{task}/toggle        │
│                 + POST /cache/backfill                      │
├──────────────────────────────────────────────────────────────┤
│                    前端: webapp/src/views/MarketDataSync.vue│
│   Tab1 覆盖率    Tab2 拉取记录    Tab3 手动拉取    Tab4 调度  │
└──────────────────────────────────────────────────────────────┘
```

---

## 四、数据库设计（新增 2 张表）

### 4.1 `cache_sync_log`（拉取记录）

```sql
CREATE TABLE cache_sync_log (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    symbol          TEXT NOT NULL,           -- 'AG' / 'ALL' / 'ag2608' 等
    period          TEXT NOT NULL,           -- 'daily' / '5min' / '15min' / '30min' / '60min'
    sync_type       TEXT NOT NULL,           -- 'scheduled' / 'manual' / 'backfill'
    status          TEXT NOT NULL,           -- 'running' / 'success' / 'failed' / 'partial'
    start_at        TEXT NOT NULL,
    end_at          TEXT,
    rows_existing   INTEGER DEFAULT 0,
    rows_new        INTEGER DEFAULT 0,
    rows_total      INTEGER DEFAULT 0,
    error_message   TEXT,
    trigger_source  TEXT,                    -- 'cron' / 'api:POST /minute/sync' / 'webapp:manual'
    created_at      TEXT DEFAULT (datetime('now'))
);
CREATE INDEX idx_csl_symbol_at   ON cache_sync_log(symbol, start_at DESC);
CREATE INDEX idx_csl_type_status ON cache_sync_log(sync_type, status, start_at DESC);
```

### 4.2 `cache_schedule_state`（调度任务状态）

```sql
CREATE TABLE cache_schedule_state (
    task_name    TEXT PRIMARY KEY,           -- 'daily_sync' / 'minute_sync_5min'
    last_run_at  TEXT,
    next_run_at  TEXT,
    last_status  TEXT,                       -- 'success' / 'failed' / 'skipped'
    run_count    INTEGER DEFAULT 0,
    fail_count   INTEGER DEFAULT 0,
    enabled      INTEGER DEFAULT 1,
    updated_at   TEXT DEFAULT (datetime('now'))
);
-- 种子数据
INSERT OR IGNORE INTO cache_schedule_state(task_name, enabled) VALUES
    ('daily_sync', 1),
    ('minute_sync_5min', 1);
```

### 4.3 表迁移位置

`data/data_loader.py:init_db()` 内追加，遵循既有"幂等自动迁移"模式（`CREATE TABLE IF NOT EXISTS`）。

---

## 五、后端模块设计

### 5.1 `api/cache_sync.py`（新 — 核心 SSOT）

```python
"""缓存同步核心（sq-0009-p2）

设计原则：
- 调度（APScheduler）和手动（API）共享同一份函数入口
- 入口处统一写 cache_sync_log（status='running'），结束时更新 status
- 单一职责：sync_* 只管拉数据写库，不检测信号
"""
from datetime import datetime
from data.data_loader import (
    get_conn, incremental_update_daily, fetch_minute_data,
    sync_all_minute_data, backfill_daily_history, backfill_min_history,
)


def _log_start(symbol: str, period: str, sync_type: str, trigger_source: str) -> int:
    """写一条 status='running' 日志，返回 log_id"""
    ...


def _log_end(log_id: int, status: str, rows_existing: int, rows_new: int,
             rows_total: int, error_message: str = None):
    """更新日志 status / end_at / rows_*"""
    ...


def sync_daily_one(symbol: str, trigger_source: str) -> dict:
    """单品种日线同步（日线只支持全 38 品种或单品种）"""
    ...


def sync_daily_all(trigger_source: str) -> dict:
    """全 38 品种日线同步"""
    ...


def sync_minute_one(symbol: str, period: str, trigger_source: str) -> dict:
    """单品种分时同步"""
    ...


def sync_minute_all(period: str, trigger_source: str) -> dict:
    """全 38 品种分时同步"""
    ...


def backfill_daily(symbol: str, years: int, trigger_source: str) -> int:
    """回填日线历史（包装 data.data_loader.backfill_daily_history）"""
    ...


def backfill_minute(symbol: str, period: str, days: int, trigger_source: str) -> int:
    """回填分时历史（包装 data.data_loader.backfill_min_history）"""
    ...


# ===== 查询层 =====

def get_coverage() -> dict:
    """按品种 × 周期覆盖率（解决"查不到各品类 K 线缓存"问题）

    返回结构：
    {
      "symbols": ["AG", "AU", ...],
      "periods": ["daily", "5min", "15min", "30min", "60min"],
      "matrix": {                           # symbols × periods
        "AG": {
          "daily":  {"count": 1095, "latest": "2026-06-04", "status": "fresh"},
          "5min":   {"count": 0,    "latest": None,        "status": "missing"},
          ...
        },
        ...
      },
      "summary": {                          # 全局统计
        "total_symbols": 38,
        "total_cells": 190,                 # 38 × 5
        "fresh_cells": 78,
        "stale_cells": 12,
        "missing_cells": 100
      }
    }

    状态判定（按 latest 与今天的距离）：
    - daily:    ≤ 1 天 fresh,  ≤ 3 天 stale, > 3 天 missing
    - 5/15min:  ≤ 30 分钟 fresh, ≤ 2 小时 stale, > 2 小时 missing
    - 30/60min: ≤ 1 小时 fresh,  ≤ 4 小时 stale, > 4 小时 missing
    """
    ...


def get_sync_logs(
    symbol: str = None,
    sync_type: str = None,
    status: str = None,
    page: int = 1,
    page_size: int = 20,
) -> dict:
    """分页查询拉取记录"""
    ...


def get_schedule_state() -> list[dict]:
    """查询所有调度任务状态"""
    ...


def toggle_schedule(task_name: str, enabled: bool) -> bool:
    """启停调度任务（更新 cache_schedule_state.enabled + APScheduler 同步）"""
    ...
```

### 5.2 `api/scheduler.py`（新 — APScheduler 集成）

```python
"""后台调度器（sq-0009-p5）

技术选型：APScheduler 3.x + BackgroundScheduler
- 进程内调度，零依赖
- JobStore 默认内存，SQLite 持久化需 sqlalchemy>=1.4 + apscheduler[sqlalchemy]
- 本项目 v1 用内存 JobStore（重启后从 cache_schedule_state 表读 last_run_at 重算）
"""
from apscheduler.schedulers.background import BackgroundScheduler
from apscheduler.triggers.cron import CronTrigger
from api.cache_sync import sync_daily_all, sync_minute_all, _update_schedule_state
from data.data_loader import get_conn


def create_scheduler() -> BackgroundScheduler:
    scheduler = BackgroundScheduler(timezone="Asia/Shanghai")

    # 任务 1：日线同步（每个交易日 17:00）
    scheduler.add_job(
        func=_run_daily_sync,
        trigger=CronTrigger(day_of_week="mon-fri", hour=17, minute=0),
        id="daily_sync",
        name="日线全量增量同步",
        replace_existing=True,
    )

    # 任务 2：5min 分时同步（交易日 9-15 点每 5 分钟）
    scheduler.add_job(
        func=_run_minute_5min,
        trigger=CronTrigger(day_of_week="mon-fri", hour="9-15", minute="*/5"),
        id="minute_sync_5min",
        name="5min 分时增量同步",
        replace_existing=True,
    )

    return scheduler


def _run_daily_sync():
    """包装层：写 cache_schedule_state 前后状态"""
    _update_schedule_state("daily_sync", "running")
    try:
        result = sync_daily_all(trigger_source="cron:daily_sync")
        _update_schedule_state("daily_sync", "success")
    except Exception as e:
        _update_schedule_state("daily_sync", "failed", str(e))


def _run_minute_5min():
    _update_schedule_state("minute_sync_5min", "running")
    try:
        result = sync_minute_all(period="5min", trigger_source="cron:minute_sync_5min")
        _update_schedule_state("minute_sync_5min", "success")
    except Exception as e:
        _update_schedule_state("minute_sync_5min", "failed", str(e))
```

### 5.3 端点扩展

#### 5.3.1 `api/routers/market.py` — 新增 `POST /daily/sync`

```python
@router.post("/daily/sync")
async def sync_daily_data(symbol: str = "all"):
    """日线同步（只做拉取，不检测信号；信号走 /signals/refresh 端点）

    Args:
        symbol: 品种代码 / 'all'（默认全 38 品种）/ 'incremental'（增量）
    """
    if symbol == "all":
        return sync_daily_all(trigger_source=f"api:POST /daily/sync?symbol={symbol}")
    else:
        return sync_daily_one(symbol, trigger_source=f"api:POST /daily/sync?symbol={symbol}")
```

**注意**：`/minute/sync` 现有版本是"同步 + 检测信号"复合端点；按决策 6，**保持现状**（`/minute/sync` 仍带信号检测，职责不强行拆；`/daily/sync` 模仿但不检测）。

#### 5.3.2 `api/routers/cache.py` — 扩展 7 端点（1 旧 + 6 新）

| Method | Path | 用途 | 调用 |
|--------|------|------|------|
| GET | `/cache/status` | 缓存状态（旧）| `cache_status()` |
| GET | `/cache/coverage` | 38 品种 × 5 周期覆盖率 | `get_coverage()` |
| GET | `/cache/sync/logs` | 拉取记录分页 | `get_sync_logs()` |
| GET | `/cache/sync/logs/{log_id}` | 单条详情 | `get_sync_logs(log_id=...)` |
| POST | `/cache/sync/daily` | 触发日线同步（手动）| `sync_daily_one/all()` |
| POST | `/cache/sync/minute` | 触发分时同步（手动）| `sync_minute_one/all()` |
| POST | `/cache/backfill` | 回填历史 | `backfill_daily/minute()` |
| GET | `/cache/schedule` | 调度任务状态 | `get_schedule_state()` |
| POST | `/cache/schedule/{task_name}/toggle` | 启停调度 | `toggle_schedule()` |

**注意**：`/cache/sync/daily` 和 `/daily/sync` 路径不同——`/cache/sync/*` 走 cache router，`/daily/sync` 走 market router（保留现有命名习惯）。

### 5.4 端点合计

| router | 旧 | 新增 | 合计 |
|--------|------|------|------|
| market.py | 6 | 1（`/daily/sync`）| 7 |
| cache.py | 1 | 6（coverage / logs×2 / sync×2 / schedule×2）| 7 |
| **总计** | **47** | **+7** | **54** |

---

## 六、前端模块设计

### 6.1 `webapp/src/views/MarketDataSync.vue`（新 — 4 Tab）

| Tab | 组件 | 数据源 |
|-----|------|--------|
| **Tab 1：覆盖率** | 38 品种 × 5 周期矩阵，绿/黄/红（fresh/stale/missing）| `GET /cache/coverage` |
| **Tab 2：拉取记录** | 分页表格（symbol / period / sync_type / status / rows_new / start_at / error）| `GET /cache/sync/logs` |
| **Tab 3：手动拉取** | 选品种 + period + 触发按钮（调 `POST /cache/sync/daily\|minute`）| — |
| **Tab 4：调度状态** | 2 个任务卡片（last_run_at / next_run_at / run_count / fail_count / enabled 切换）| `GET /cache/schedule` |

**手动拉取交互**（Tab 3）：
- 选品种（38 + "全部"）
- 选周期（daily / 5min / 15min / 30min / 60min）
- 选时间段（仅分时：start_date / end_date）
- 点"立即拉取" → 调对应端点 → loading → 成功后切到 Tab 2 看记录

### 6.2 `webapp/src/stores/cache.ts`（新 — Pinia store）

```typescript
import { defineStore } from 'pinia'
import { useApi } from '@/composables/useApi'

export const useCacheStore = defineStore('cache', () => {
  const coverage = ref<any>({})
  const syncLogs = ref<any[]>([])
  const schedule = ref<any[]>([])

  async function fetchCoverage() { ... }
  async function fetchSyncLogs(params: any) { ... }
  async function fetchSchedule() { ... }
  async function triggerSync(type: 'daily' | 'minute', params: any) { ... }
  async function toggleSchedule(taskName: string, enabled: boolean) { ... }

  return { coverage, syncLogs, schedule, fetchCoverage, fetchSyncLogs, fetchSchedule, triggerSync, toggleSchedule }
})
```

### 6.3 路由 + 导航

- 新路由：`/market-data-sync` → `MarketDataSync.vue`
- 导航入口：`webapp/src/App.vue` / 侧边栏菜单 + 一行菜单项 "行情数据同步"

### 6.4 15/30/60min 前端降级（决策 7-C）

```typescript
// composables/useKLineData.ts
async function fetchKLineData(symbol: string, period: '5min' | '15min' | '30min' | '60min') {
  // 1. 优先查库
  const cached = await useApi().get(`/minute/${symbol}`, { period })
  if (cached.records?.length > 0) return cached

  // 2. 库为空 → 降级 resample 5min
  if (period !== '5min') {
    const min5 = await useApi().get(`/minute/${symbol}`, { period: '5min' })
    if (min5.records?.length > 0) {
      return resampleKLine(min5.records, period)   // 前端 JS 聚合
    }
  }

  // 3. 都无 → 返回空
  return { records: [] }
}
```

`resampleKLine` 工具：把 5min K 线按 15/30/60min 聚合（OHLCV 重算），前端用 lightweight-charts 渲染。

---

## 七、SSOT 收敛

| 文件 | 角色 | 状态 |
|------|------|------|
| `data/data_loader.py` | 数据层 SSOT | 已有，新增 init_db 2 张表 |
| `api/cache_sync.py` | 同步核心 SSOT | 新增 |
| `api/scheduler.py` | 调度器 SSOT | 新增 |
| `api/routers/market.py` | 端点（消费方）| 扩展 +1 |
| `api/routers/cache.py` | 端点（消费方）| 扩展 +6 |
| `api/schemas/cache.py` | Pydantic schemas | 扩展 6 个 |
| `webapp/src/views/MarketDataSync.vue` | 视图 SSOT | 新增 |
| `webapp/src/stores/cache.ts` | 缓存状态 SSOT | 新增 |
| `webapp/src/services/cache.ts` | API 客户端 | 新增（与 market.ts 对称）|
| `webapp/src/composables/useKLineData.ts` | K 线查询 + resample | 新增 |
| `0009_缓存层优化设计.md` | 设计文档 SSOT | 本文件 |

---

## 八、Phase 拆分

| Phase | 内容 | 估时 | 依赖 |
|-------|------|------|------|
| **P1** | DB 迁移：新增 2 张表 + init_db 接入 | 30 min | — |
| **P2** | `api/cache_sync.py` 核心函数（sync / backfill / 查询 9 函数）| 1.5 h | P1 |
| **P3** | `api/routers/market.py` 新增 `POST /daily/sync` | 20 min | P2 |
| **P4** | `api/routers/cache.py` 扩展 6 端点 + `api/schemas/cache.py` 6 schemas | 1 h | P2 |
| **P5** | `api/scheduler.py` APScheduler 集成 + `main.py` 启动钩子 | 1.5 h | P2 |
| **P6** | 前端 `MarketDataSync.vue`（4 Tab）+ `cache.ts` store + `cache.ts` service + `useKLineData.ts` | 2.5 h | P4 |
| **P7** | pytest 单测（cache_sync 9 函数）+ 端到端冒烟（11 端点回归 + 7 新端点）+ 备份 baseline + commit | 1.5 h | P1-P6 |

**总计约 9 小时**。P6 可与 P3-P5 并行。

### 8.1 实施步骤表

| Phase | 内容 | 状态 | 完成日期 | 备注 |
|-------|------|------|----------|------|
| Phase 1 | DB 迁移 2 表 | ⏳ 待开始 | — | — |
| Phase 2 | api/cache_sync.py | ⏳ | — | — |
| Phase 3 | POST /daily/sync | ⏳ | — | — |
| Phase 4 | cache.py 6 端点 | ⏳ | — | — |
| Phase 5 | api/scheduler.py | ⏳ | — | — |
| Phase 6 | 前端 MarketDataSync.vue | ⏳ | — | — |
| Phase 7 | pytest + 冒烟 + 备份 + commit | ⏳ | — | — |

---

## 九、后续（v2）

1. **覆盖率告警**（决策 4）：某品种连续 2 天没拉到 → 飞书推送
2. **夜盘支持**：21:00-02:30 时段（海龟策略一般不盯夜盘，先不做）
3. **节假日过滤**：接入 chinese_calendar 库，跳过法定节假日
4. **15/30/60min 后端聚合任务**：决策 7-C 选前端降级；如果发现降级体验差，可加后端 resample 任务
5. **回测引擎读 cache**：现在回测自己加载，可在 `backtest/backtest_engine.py` 接入 `cache_sync` 复用缓存

---

## 十、配套变更

执行时一并改：

| 文档 | 变更 |
|------|------|
| `CLAUDE.md` 顶部「项目内规范」表 | 加一行 `缓存层优化 → 0009_缓存层优化设计.md` |
| `0003_变更日志.md` | 追加 `v0.18.6` 记录（每个 Phase 完工 + commit 各一条）|
| `README.md` 47 → 54 端点清单 | 7 新端点补全 |
| `requirements.txt` | 加 `apscheduler>=3.10,<4.0` |
| `webapp/src/App.vue` 导航 | 加 "行情数据同步" 菜单项 |

---

## 执行记录

| 日期 | Phase | 变更内容 | 执行者 | 变更日志版本 |
|------|-------|----------|--------|-------------|
| 2026-06-07 | 设计 | 7 决策拍板，建设计文档 v0.1 | claudecode-alexyuan | 待 v0.18.6 |
