# 0008_后端API模块化拆分设计

> **作者**：Blex  
> **创建时间**：2026-06-04 00:26  
> **状态**：⏳ 待用户审核  
> **优先级**：高（与前端 Vue 迁移并行，不冲突）

---

## 一、问题陈述

### 1.1 现状

- `api/main.py`：**1848 行**，**46 个端点**（`@app.get/post/put/delete` 装饰器），全部扁平堆在同一个文件
- `api/services/`：4 个 service 文件（event_logger 422 / sim_engine 478 / session_lifecycle 362 / line_calculator 334 = 1596 行）已有，但**未按域拆分** router
- **0 个 APIRouter 实例**：`grep APIRouter` 在 main.py 中无结果（全部端点直接挂载到全局 `app`）
- 维护痛点：
  - 单文件 >1800 行，IDE 跳转 / 搜索效率低
  - 不同域端点混在一起（如 `/backtest/run` 和 `/signals/all` 在同一个文件相邻位置）
  - 跨域调用链追踪困难（一个端点可能调 2-3 个 service）
  - 测试 mock 不友好（无法单独 import 某个域的端点）

### 1.2 端点分类（按域，共 46 个）

| 域名 | 端点 | 数量 | main.py 行号 |
|------|------|------|-------------|
| **health** | `/`, `/health` | 2 | L479-489 |
| **backtest** | `/backtest/run`, `/backtest/runs`, `/backtest/runs/{id}`, `/backtest/runs/{id}/trades`, `/backtest/{job_id}` | 5 | L490-631, L873-901 |
| **account** | `/account/overview`, `/account/{id}/positions`, `/account/{id}/units`, `/account/{id}/sessions`, `/account/{id}/trades` | 5 | L632-872 |
| **signal** | `/signals/all`, `/signals/{symbol}` | 2 | L902-924 |
| **position** | `/position/{symbol}`, `/position/{symbol}/add` | 2 | L925-973 |
| **market** | `/contracts/{symbol}`, `/quotes/{code}`, `/main-contract/{symbol}` | 3 | L974-1115 |
| **strategy** | `/strategies`, `/strategies/{id}`, POST/PUT/DELETE `/strategies`, `/strategies/{id}/signals`, `/strategies/{id}/param-history`, `/strategies/{id}/events` | 8 | L1116-1409 |
| **session** | `/session`, `/session/{id}/orders`, `/session/{id}/orders/reduce`, `/session/{id}/close`, `/session/{id}/events`, `/session/{id}/trade-process`, `/sessions`, `/session/{id}/lines`, `/session/{id}/line/{type}` | 9 | L1410-1666 |
| **market_data** | `/minute/{symbol}`, `/daily/{symbol}` | 2 | L1667-1749 |
| **cache** | `/cache/status`, `/minute/sync` | 2 | L1750-1757, L1806-1848 |
| **turtle** | `/turtle/signals/{symbol}`, `/turtle/signals/active`, `/turtle/signals/alerts`, `/turtle/scan/{symbol}`, `/turtle/scan/all`, `/turtle/signal/latest` | 6 | L1758-1805 |
| **辅助函数** | `_fetch_latest_factor`, `_calc_signal`, `_calc_pnl`, `_run_backtest_sync` | 4 | L178-478 |

### 1.3 目标

- **行为零变更**：46 个端点的 URL、参数、返回值、CORS 配置、lifespan 完全不变
- **main.py < 150 行**：仅保留 app 创建 + lifespan + router 注册 + 中间件
- **按域拆 11 个 router**：每个域一个 `routers/<domain>.py`，含自己的 Pydantic schemas
- **共享依赖抽取**：DB 连接、账户默认参数抽取到 `deps.py`
- **现有 82 测试 100% 通过**

---

## 二、目标结构

```
api/
├── __init__.py                        # 空（保持包识别）
├── main.py                            # 80-120 行（app + lifespan + include_router）
├── deps.py                            # 共享依赖：get_db / get_default_account
├── schemas/                           # Pydantic models 按域分组
│   ├── __init__.py                    # re-export
│   ├── common.py                      # HealthResponse, ApiError
│   ├── backtest.py                    # BacktestRequest, BacktestResponse
│   ├── signal.py                      # SignalResponse, PositionResponse
│   ├── market.py                      # ContractInfo, QuoteInfo
│   ├── strategy.py                    # StrategyCreate, StrategyUpdate, StrategyDetail
│   ├── session.py                     # SessionCreate, OrderAdd, OrderReduce, SessionClose
│   └── account.py                     # AccountOverview, AccountPosition, ...
├── routers/                           # 11 个域 router
│   ├── __init__.py                    # re-export
│   ├── health.py                      # 2 端点
│   ├── backtest.py                    # 5 端点
│   ├── account.py                     # 5 端点
│   ├── signal.py                      # 2 端点
│   ├── position.py                    # 2 端点
│   ├── market.py                      # 3 端点（contracts/quotes/main-contract）
│   ├── strategy.py                    # 8 端点
│   ├── session.py                     # 9 端点
│   ├── market_data.py                 # 2 端点（minute/daily）
│   ├── cache.py                       # 2 端点
│   └── turtle.py                      # 6 端点
├── helpers/                           # 4 个跨域辅助函数
│   ├── __init__.py
│   ├── factor.py                      # _fetch_latest_factor
│   ├── signal_calc.py                 # _calc_signal
│   ├── pnl.py                         # _calc_pnl
│   └── backtest_runner.py             # _run_backtest_sync
└── services/                          # 已有，保留
    ├── __init__.py
    ├── event_logger.py
    ├── line_calculator.py
    ├── session_lifecycle.py
    └── sim_engine.py
```

**行数预估**：

| 文件 | 当前 | 目标 | 减少 |
|------|------|------|------|
| main.py | 1848 | ~120 | **-93%** |
| 单个最大 router | - | ~250 (session) | 0 |
| 单个最大 schema | - | ~150 (account) | 0 |

---

## 三、拆分原则

### 3.1 端点归属规则

- **path 第一个 segment 一致 → 同一个 router**：
  - `/account/...` → `routers/account.py`（包含 `/account/overview`，即使没 `{account_id}`）
  - `/strategies/...` → `routers/strategy.py`（单数，匹配业务域）
  - `/turtle/...` → `routers/turtle.py`（独立子域）
- **path 含具体资源 ID 才归属**：如 `/backtest/{job_id}` 虽不匹配前缀模式，但因属于 backtest 域，放 `routers/backtest.py`
- **辅助函数（_xxx）不挂在 router 上**：抽到 `helpers/`，被 router 通过 `from api.helpers import xxx` 引用

### 3.2 依赖顺序（先低后高）

1. `health`（无依赖）
2. `market`（仅用 data_loader）
3. `signal`（用 helpers.signal_calc + market）
4. `market_data`（用 data_loader）
5. `cache`（用 data_loader）
6. `position`（用 sim_engine）
7. `turtle`（用 sim_engine + data_loader）
8. `strategy`（用 sim_engine + services）
9. `session`（用 sim_engine + session_lifecycle + line_calculator + event_logger）
10. `backtest`（用 backtest_engine + helpers.backtest_runner）
11. `account`（用几乎所有 services，依赖最重）

### 3.3 Schemas 拆分原则

- **每个域的 Pydantic model 跟 router 走**：router 文件直接 `from api.schemas.session import ...`，不二次跳目录
- **跨域共享的 model 放 common.py**：如 `HealthResponse`
- **不破坏现有 import 路径**：如果 main.py 顶层有 `BacktestRequest = ...` 这种运行时引用，需在 `schemas/__init__.py` 中 re-export 兼容

### 3.4 共享依赖（deps.py）

```python
# api/deps.py
from typing import Generator
from fastapi import Depends

def get_db_path() -> str:
    return "data/futures_akshare.db"

def get_default_account_id() -> str:
    return "sim_default"
```

仅当原 main.py 中**多处重复出现**同一逻辑时才抽到 deps.py。避免过度抽象。

---

## 四、执行阶段（5 阶段 / 子任务）

| 阶段 | 子任务 ID | 内容 | 风险 | 验收 | 限时 |
|------|----------|------|------|------|------|
| **1. 准备** | `sq-0008-p1-prep` | 创建 `api/{schemas,helpers,routers}/` 目录骨架；写 `schemas/__init__.py` re-export；保留 main.py 不动 | 低 | 目录存在，main.py 可正常 import | 15 min |
| **2. helpers 抽取** | `sq-0008-p2-helpers` | 把 4 个 `_xxx` 函数抽到 `api/helpers/`；main.py 改为 import | 中 | 单元测试全过 | 20 min |
| **3. schemas 抽取** | `sq-0008-p3-schemas` | 把所有 Pydantic model 按域归类到 `api/schemas/<域>.py`；main.py 改 import | 中 | 46 端点全 200（行为不变） | 30 min |
| **4. routers 抽取** | `sq-0008-p4-routers` | 按依赖顺序逐个抽 11 个 router；每个 router 内联 schema import；main.py 改为 `app.include_router(...)` | 高 | 46 端点全 200 + 路径不变 | 90 min |
| **5. 清理验证** | `sq-0008-p5-cleanup` | main.py 缩到 < 150 行；跑全量 82 测试；端到端冒烟；文档更新 | 低 | 82 测试全过 + main.py < 150 行 | 30 min |

**总计 ~3 小时**，分 5 个串行子任务。

### 关键约束（每阶段子任务都要遵守）

1. **零行为变更**：URL 路径、参数、返回值的 JSON 结构完全不变
2. **零破坏性 import**：现有 `from api.main import ...` 引用保持兼容（如有）
3. **每阶段独立验收**：上一个子任务完成 → 跑 46 端点冒烟 + 82 测试 → 通过 → 才派下一个
4. **失败回滚**：每个子任务完成后 git commit 一次（`v1.6-backend-phase-X`），出问题可 `git revert`
5. **必须真改文件**（MEMORY 教训）：每个子任务必须明确"创建 N 个文件 + 修改 M 个文件"，验收以 `git diff --stat` 为准

---

## 五、关键风险 & 应对

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| **path 顺序冲突**：如 `/backtest/run` 和 `/backtest/{run_id}` 同 router 内，FastAPI 顺序敏感 | 中 | 高 | 子任务逐 router 写完后用 curl 验证 200 |
| **include_router 的 prefix 不一致**：原端点无 prefix（裸挂在 app 上），新 router 默认加 prefix | 中 | 中 | 全部 router 用 `APIRouter()`（无 prefix），端点路径保持原样 |
| **Pydantic model 跨域引用**：如 `BacktestResponse` 在 account endpoint 里返回 | 低 | 中 | `schemas/__init__.py` re-export 兼容旧 import |
| **lifespan 中途失败**：db 初始化、akshare 配置被漏移 | 低 | 高 | 保留 `lifespan` 完整逻辑在 main.py，**不抽** |
| **测试 fixture 依赖 main.py 内部符号**：如 `test_main.py` 直接 import 端点函数 | 低 | 中 | 阶段 5 验证：跑 82 测试全过 |
| **uvicorn 启动失败**：路由注册顺序错误导致 path 冲突 | 中 | 高 | 阶段 4 子任务结束后必跑 `curl localhost:8000/health` + 6 个代表端点 |
| **uvicorn exec SIGKILL（60s 超时）** | 高 | 中 | 阶段 4 验收用 `nohup uvicorn ... > /tmp/uvicorn-restart.log 2>&1 &` 后台启动 |

---

## 六、决策点（待用户拍板）

| # | 决策项 | 推荐 | 备选 |
|---|--------|------|------|
| 1 | **是否保留 `api/services/`** 现有 4 个 service 文件？ | ✅ 保留（不重新组织） | 抽到 `api/services/<域>/` 子包（**过度**） |
| 2 | **schemas 是放 `api/schemas/`** 还是 **每个 router 内联**？ | `api/schemas/<域>.py`（**独立目录**） | router 内联（文件稍大但减少 import 链） |
| 3 | **router 用 `prefix`** 还是 **裸路径**？ | **裸路径**（`APIRouter()` 无 prefix，保持原 URL 不变） | 统一 prefix（如 `/api/v1`）—— 但**会破坏当前前端调用** |
| 4 | **执行节奏**：5 阶段子任务**串行** vs **并行**？ | **串行**（依赖顺序强，每阶段验收） | 部分并行（如 p2 helpers + p3 schemas 可并行，因互不依赖） |
| 5 | **是否同时重构 service**（按域再拆 service 文件）？ | ❌ **不重构**（本任务只拆 router，service 是另一回事） | 同时拆（**会扩大范围**，本次否决） |
| 6 | **与 Vue 迁移是否并行**？ | ✅ **并行**：Phase C/D/E 在飞，p1-p5 串行互不干扰 | 暂停 Vue，先做后端（**不必要**，并发能力足够） |
| 7 | **是否生成 `api/__init__.py` re-export** 让 `from api import X` 工作？ | ✅ 保留兼容：把 main.py 的公共符号 re-export 到 `api/__init__.py` | 不做（**如果有外部脚本 import 就会破**） |

**默认**：以上 7 项**全部按推荐**。如你同意，我立即派 Phase 1 子任务。

---

## 七、验收标准

### 7.1 每阶段子任务

| 阶段 | 验收 |
|------|------|
| P1 | 目录结构存在；main.py 仍 1848 行可启动；curl `/health` 200 |
| P2 | 4 个 helper 文件存在；main.py 顶部 import 改了；82 测试全过 |
| P3 | 7 个 schema 文件存在；main.py 改 import；46 端点全 200 |
| P4 | 11 个 router 文件存在；main.py 改用 `include_router`；46 端点路径全 200 |
| P5 | main.py < 150 行；82 测试全过；端到端冒烟 8 项全 200 |

### 7.2 最终（5 阶段后）

| 项 | 目标 |
|----|------|
| `api/main.py` | < 150 行 |
| `api/routers/*.py` | 11 个文件，每个 < 250 行 |
| `api/schemas/*.py` | 7 个文件 |
| `api/helpers/*.py` | 4 个文件 |
| 46 端点 | 全部 200，URL 路径 0 变化 |
| 82 测试 | 100% 通过 |
| CORS | 正常 |
| lifespan | 启动/关闭无错 |

### 7.3 端到端冒烟（p5 必跑）

```bash
# 1. health
curl -o /dev/null -w "%{http_code}\n" http://localhost:8000/health    # 200

# 2. 7 大域各 1 端点
for ep in /signals/all /strategies /sessions /cache/status /turtle/signals/active /backtest/runs /account/overview; do
  curl -o /dev/null -w "$ep: %{http_code}\n" "http://localhost:8000$ep?account_id=sim_default"
done
```

预期 7/7 全 200。

---

## 八、依赖关系

```
P1 (prep) → P2 (helpers) → P3 (schemas) → P4 (routers) → P5 (cleanup)
                                                       ↓
                                              验收 + 文档同步
```

P2 和 P3 互不依赖，理论上可并行。但**保守起见串行**（MEMORY 教训：API 重试 ≤1 min 失败就换路径，但子任务不冒险）。

---

## 九、飞书同步

完成后同步至飞书：
- 父节点：`CAmhwOvBLiXhaUkVidccCluDnxg` (股票量化)
- 父节点：`AVjtwHVbDi3KMek4me9c50NUnLg` (实施过程)
- 现有：`JqIowcMDjiYQ4XkUdHXc3amsnyb` (数据模型重构)
- 新建子节点：`后端API模块化拆分`（与"数据模型重构"同级）
  - `01-设计` ← 本文档
  - `02-计划` ← 执行计划
  - `03-执行` ← 实施日志
  - `04-验收` ← 验收报告

---

## 十、变更日志

完成后追加 `0003_变更日志.md`：
- v0.18.0（P1 准备）
- v0.18.1（P2 helpers 抽取）
- v0.18.2（P3 schemas 抽取）
- v0.19.0（P4 routers 抽取，跨越大版本）
- v0.19.1（P5 清理验证）

每阶段一个版本号，**变更日志即时记录**（SOUL.md 铁律）。

---

## 待用户决策

**7 项决策点 + 1 个总开关**：
- **总开关**：是否同意开始 P1 子任务？（同意后立即派，不同意则按你的意见调整设计）
