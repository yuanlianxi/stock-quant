# Stock Quant - 变更日志

## [v0.9.0] - 2026-06-01 (设计阶段 · 待审核)

### 新增

**数据模型与界面重构设计文档**：`0006_数据模型与界面重构设计.md`
- 现状分析：4 张表的局限性 + 界面与数据表对应关系矩阵
- 新增 10 张表的设计：合约层（2）+ 策略层（2）+ 信号层（1）+ 模拟交易层（4）+ 持仓单元（1）+ 回测层（2）+ 真实持仓层预留（3）
- 核心原则：维度建模 / 模拟与真实分离 / 策略可插拔 / 持仓单元持久化
- 界面改造：顶部策略切换器 / 品种网格合约下钻 / 详情四 Tab / 账户中心 / 回测中心
- 关键处理流程：实时信号→模拟下单、撮合引擎、回测落盘
- 与 0016 通达信海龟策略字段对齐（entry_basis_price / entry_atr / is_gap）
- 实施计划：7 个 Phase，约 17-19 工作日
- 8 项待确认项（资金/费率/撮合/多账户等）

**飞书同步**
- 同步至飞书知识库：股票量化/0006_数据模型与界面重构设计
- 节点 Token：`OMwUwjCDAi0QOyk6gINcDZVUnlc`
- Obj Token：`U3hPdGEOdo0DJ6xESDKcCkWonvc`
- 同步方式：feishu_doc write 一次性写入 647 blocks
- 同步耗时：单次成功（28KB 文档未触发分块阈值）

**本地 Token 表更新**
- `0001_项目设计.md` Token 清单增加 0006 节点行

### v1.1 修订（2026-06-01）

**5 项关键修订**：

1. **表注释补全**：所有表加 CREATE TABLE 块注释（用途/写入方/读取方/生命周期）+ 字段级行注释
2. **策略版本字段**：`strategies` 加 `version` + `state_table` 字段；新增 `strategy_param_history` 记录参数迭代历史
3. **信号主表**：新增 `strategy_signal_states`（状态表）—— 区别于 `strategy_signals`（流水表）；UPSERT vs INSERT
4. **交易会话**：`strategy_signals` 加 `trade_session_id` + `session_phase`；同一次交易的 entry/add/exit 用同一 UUID 串联
5. **策略状态表拆分**：
   - 删除 `strategy_contracts`（主力合约运行时识别）
   - 策略状态按"类型"分表：turtle_states / dual_ma_states
   - 每表带 `phase` 字段：idle / signal_detect / in_position / exit_pending / drawdown_check

**表数量**：10 → 11（-1 strategy_contracts，+1 strategy_param_history，+1 strategy_signal_states，+2 turtle_states/dual_ma_states）

**新增界面元素**：
- 顶部策略下拉显示版本号 "海龟T1 v1"
- 策略历史页 `/strategy/{id}/history`
- 账户中心增加"策略状态"卡片（phase/locked_atr/basis_price/current_units）

**飞书同步**：
- 同步方式：write 覆盖（先删 160 旧 block + 写 1051 新 block）
- 文档链接未变：`https://feishu.cn/wiki/OMwUwjCDAi0QOyk6gINcDZVUnlc`

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.1（43KB / 1300+ 行）

### v1.2 重大重构（2026-06-01 下午）— 三大基础领域划分

**背景**：用户审视 v1.1 后指出领域边界不清——`turtle_states` 同时承担"策略阶段"和"建仓锁定事实"，违反 DDD 限界上下文。

**核心修订**：

1. **明确划分 3 大基础领域**：
   - 领域 1 合约行情（futures_contracts / futures_quotes / K线）
   - 领域 2 策略（strategies / strategy_signals / strategy_signal_states）
   - 领域 3 交易/账户（trade_sessions / sim_* / position_units）
2. **删除**：`turtle_states` / `dual_ma_states`（状态属于交易事实，不属于策略）
3. **新增**：`trade_sessions`（交易领域主表，承载一次完整交易的元数据）
4. **调整**：`trade_session_id` 从策略领域外键引用 → 提升为交易领域核心字段；重命名为 `signal_session_id` 在策略领域
5. **核心原则**：**策略是无状态规则引擎**，所有"锁定值"（基准价/锁定 ATR/跳空标志）都在建仓瞬间由撮合引擎写死到交易领域的事实表
6. **phase 字段位置变更**：v1.1 在 turtle_states（已删除）→ v1.2 在 strategy_signal_states（策略层判断字段）

**表数量**：11 → 16（-2 turtle_states/dual_ma_states，+1 trade_sessions，+1 sim_orders 的 session_id，+1 sim_trades 的 session_id，+1 sim_positions 的 session_id，+1 position_units 的 session_id 必填）

**新增章节**：
- §3 领域划分（v1.2 核心）
- §3.3 为什么策略不能有独立状态表（关键决策说明）
- §4.5 跨领域协同关系
- §4.6 trade_session 生命周期（v1.2 核心流程）
- §4.8 表关系总图（按领域重画）

**实施计划优化**：
- v1.1：8 个 Phase（含 Phase 3 单独做策略状态表）
- v1.2：7 个 Phase（删除 Phase 3，建表工作量合并到 Phase 3 交易层）

**飞书同步**：
- 同步方式：write 主块（228 blocks） + append 5 批（累计 ~1023 blocks）= 1251 blocks
- 首次 write 触发飞书服务 400 错误（临时故障），间隔 10s 后重试成功
- 文档链接未变：`https://feishu.cn/wiki/OMwUwjCDAi0QOyk6gINcDZVUnlc`

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.2（53KB / 1300+ 行）

### v1.3 修订（2026-06-01 晚）

**背景**：用户审视 v1.2 后提出 3 项重要修订：
1. 交易领域表需要 account_id 关联
2. trade_sessions 需要与账户 ID 关联
3. 信号是"价格线集合"而非单点；用户可手动调价；价格线不持久化

**核心修订**：

1. **多账户支持**：所有交易表加 `account_id` 字段
   - `sim_account.account_id` 承担主账户身份（`sim_default` / `sim_user1` 等）
   - `sim_account` 新增 `user_id` / `display_name` / `account_type` 字段
   - `sim_orders` / `sim_trades` / `sim_positions` / `position_units` / `trade_sessions` / `live_*` 都加 `account_id`（必填）
   - 所有联合唯一约束调整

2. **价格线实时计算**（不持久化）：
   - **不建 `trade_session_lines` 表**（避免双写一致性问题）
   - 价格线由基础事实实时计算：trade_sessions 锁定值 + position_units 实际成交价 + price_overrides_json + 策略参数 + K线
   - 新增 API：`GET /session/{id}/lines?account_id=...` 实时计算返回所有价格线

3. **用户调价支持**：
   - `trade_sessions.price_overrides_json` TEXT 字段（轻量级 JSON）
   - 存用户调整后的埋点价格，如 `{"add_unit_1": 18830, "stop_loss": 18750}`
   - 调价 API：`PUT /session/{id}/line/{line_type}?account_id=...`
   - 优先级：override > 算法值；override 仅对"未成交的信号线"生效

4. **策略代码组织约定**（实施层约束）：
   - 目录结构：`strategies/<type>/V<版本号>/strategy.py`（如 `strategies/turtle/V1/strategy.py`）
   - 主类命名：`<Type>V<N>Strategy`（如 `TurtleV1Strategy`）
   - strategy_id 命名：`<type>_v<n>`（如 `turtle_v1`）
   - 实现 `strategies/loader.py`：根据 `strategy_id` 动态导入
   - V1 / V2 完全物理隔离，避免代码交叉影响

**表数量**：16 → 18（新增 live_orders/trades/positions 三张预留表的 account_id 字段从抽象变成具体；含预留 live_ 实际表数 = 16 + 3 = 19，v1.3 与 v1.2 表数一致 16 张主表 + 3 张预留）

**关键调整清单**：
- `sim_account`：`account_id` 改为有意义的命名（`sim_default` / `sim_user1`），新增 `user_id` / `display_name`
- `trade_sessions`：加 `account_id` + `price_overrides_json`
- `sim_orders` / `sim_trades` / `sim_positions` / `position_units`：加 `account_id`
- `live_*` 三张预留表：加 `account_id`

**实施计划调整**：
- Phase 2 策略领域：增加"代码组织重构"任务（把现有 strategies/turtle/ 移到 V1/，实现 loader）
- Phase 3 交易领域：增加"价格线计算 API"和"用户调价 API"
- 合计 ~19-22 工作日

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.3（45KB / 1300+ 行）

**飞书同步**：
- 状态：✅ **已完整同步**（2026-06-02 09:40 验证）
- 同步方式：lark SDK 路径（write 失败后降级为 BatchDelete + 单 block 循环写，参见下方"v1.3 飞书同步补记"）
- 飞书 doc 终态：14 个 Heading2（章节） + 50 个 Heading3（子章节） + 15 飞书原生 Sheet（表清单） + 35 Code 块（SQL schema） + 143 Bullet + 19 Ordered + 15 Divider = 352 顶层块
- 章节对齐：本地 14 个 ## 章节、50 个 ### 子章节 = 飞书 100% 匹配（一～十三章齐全）
- 文档链接未变：`https://feishu.cn/wiki/OMwUwjCDAi0QOyk6gINcDZVUnlc`

#### v1.3 飞书同步补记（2026-06-02 上午）

**背景**：22:08-22:33 飞书 doc / drive / wiki 三套 API 持续 400 错误（约 25 分钟），22:14 通过 `feishu_app_scopes` 验证 token 正常（283 granted, 0 pending），确认是飞书服务端故障。22:23 wiki get 恢复但 doc write 仍 400。23:30-23:33 期间本地 5 项编辑（futures_daily 加 main_contract_code / strategy_signals 精简 / 删除 strategy_signal_states / 订单交易表加外部平台字段 / 修订记录追加）按部就班完成。故障恢复后某次重试成功写入完整 v1.3 内容。

**2026-06-02 09:40 验证结果**：
- 飞书 doc 状态：14 H2 + 50 H3 + 15 Sheet + 35 Code = 352 顶层块 ✅
- 与本地 v1.3（70KB / 1642 行 / 14 ## / 50 ###）100% 章节对齐
- 14 个 ## 章节一一对应：修订记录、一～十三（含 1.1/1.2/2.1/2.2/3.2/3.3/4.1-4.6/5.1-5.7/6.1-6.6/7.1-7.4/8.1-8.2/9-13）
- **结论**：v1.3 已完整同步，无需重写

**经验沉淀**：
- 经验已写入 `~/.openclaw/plugin-skills/feishu-doc/SKILL.md` 的"Write Strategy by Document Size"章节
- 核心结论：feishu_doc `write` 在 >200 块后开始不稳定；>500 块应直接 lark SDK 批删+单 block 写
- 昨晚 22:08+ 的 400 不是文档大小问题（v1.3 45KB 也不算特别大），是飞书服务端临时故障

### 状态
- ⏳ 待用户审核（v1.3 内容已落地飞书，等 Alex 审设计）
- 审核通过后按 Phase 1→7 实施
- 旧 4 张表保留，新表增量创建

---

### v1.4 重大扩展（2026-06-02）

**背景**：用户在 v1.3 基础上指出 2 块缺失：
1. 缺子策略特有字段的存储（v1.3 的通用表被海龟字段污染）
2. session_id 触发与复用规则没说清楚

**核心修订**：
1. 新增 `turtle_session_data`：海龟 session 级特有事实（7 字段）
2. 新增 `session_event_log`：session 生命周期审计（8 个 event_type）
3. 新增 `strategy_event_log`：策略执行引擎事件流水
4. `trade_sessions` 注释调整 + open session 部分索引
5. 新增 §7.5 Session 生命周期与触发源（核心新增）
6. 新增 §7.6 用户操作流程（建仓/加仓/减仓/平仓 4 个流程）
7. 改造 §6 界面：5 个 Tab + 手动操作模态框
8. 6 个新 API（POST /session / POST /session/{id}/orders / POST /session/{id}/close 等）

**表数量**：18 → 21（+3：turtle_session_data / session_event_log / strategy_event_log）
**决策点 8 项**：全部按推荐默认（字段范围 / 占位 / event_type / 归档 / position_units / 并发 / 入口 / 版本）

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.4（~100KB / 2201 行 / 14 ## / 54 ###）
- 主 agent 修复：4.3.5 章节顺序（移到 4.3 之后、4.4 之前）+ §13 总表数 18 → 21

**飞书同步**：
- 文档链接未变：https://feishu.cn/wiki/OMwUwjCDAi0QOyk6gINcDZVUnlc
- 同步状态：🔄 多次子任务尝试（详见下方 v1.4 飞书同步补记）
- 截至 16:40 仍在抢救中

#### v1.4 飞书同步补记

**v1.4 飞书同步经过 4 次尝试**：

| # | runId | taskName | 路径 | 跑时 | 结果 |
|---|-------|----------|------|------|------|
| 1 | 5e3baf29 | stock-quant-0006-v14-sync | 混合（本地 + lark SDK）| 超 1h | timeout 无完成 |
| 2 | 4f1858df | stock-quant-0006-v14-feishu-only | feishu_doc + lark SDK | 20m03s | failed |
| 3 | c6f99be6 | stock-quant-0006-v14-feishu-recover | lark SDK 单干 | 51m+ | 调试 API bug 后实际写中 |
| 4 | fd0b371d | stock-quant-0006-v14-feishu-write | feishu_doc write 单干 | 2m | done 但没写入（read 400 立即放弃）|

**关键问题**：
- 3b4ccc65 死磕 lark SDK，47 分钟才修复 API bug（UpdateTextElementsRequest 不是 list）
- d2029df3 太老实：看到 feishu_doc read 400 立即放弃（read 400 ≠ write 不可用）
- 多个子任务并发写同一飞书 doc 风险高

**用户决策（16:39）**：方案 C = kill lark SDK 路径子任务，让 feishu_doc write 路径子任务跑到底

**经验教训**：
- 项目区大文档同步（>100KB）**首选 feishu_doc write**（一次性成功率高）
- **不碰 lark SDK**（OpenClaw 环境下 lark_oapi 不稳定，30+ 分钟调试 API 调用错误）
- 子任务**拆任务**（改写 / 飞书同步 / 变更日志 拆 3 个子任务）
- 子任务**限时**（25 分钟超时立即降级或放弃）
- 子任务**不准看 read 状态决定是否 write**（read 400 和 write 400 是不同 API 路径）

### v1.5 重大扩展（2026-06-02）— 海龟交易过程数据子表

**背景**：用户在 v1.4 基础上指出 1 块缺失：
1. 缺"子策略交易过程数据"子表（`turtle_session_data` 是 session 级事实快照，但**过程数据**——逐根 K 线决策流：何时检测加仓信号、跳空跳过、止损触发——无表存储）

**核心新增**：
1. 新增 `turtle_trade_process` 表（§4.3.6）：海龟策略逐根 K 线决策流水，10 个 `event_type` 枚举
2. 新增 §7.7 海龟交易过程流程：on_bar 流程图 + 4 Unit 加仓模型 + 退仓优先顺序（止损 > 加仓）
3. §4.5.5 补充 `turtle_trade_process` vs `session_event_log` / `strategy_event_log` 边界对比表
4. §8.1 新增 API：`GET /session/{id}/trade-process`
5. Phase 3 新增 4 子任务（3.15-3.18）：新表 + on_bar 改造 + check 类 C 方案 + 新 API
6. §10 新增 3 项待确认（数据量归档 / check 5 根够不够 / dual_ma 何时做）
7. §11 新增 2 项风险（数据量预估 / check 类调试够用性）
8. §13 表清单更新：22 张（v1.4 = 21 + v1.5 新增 1）

**4 项决策**（用户全部按推荐默认）：
| # | 决策点 | 决定 | 备选 |
|---|--------|------|------|
| 1 | 加仓次数 | 4 Unit（1 首仓 + 3 加仓）| 5 Unit / 6 Unit |
| 2 | 退仓优先顺序 | 止损优先（同根 K 线同时触发先止损）| 加仓优先 / 并行 |
| 3 | check 类事件存储 | C 方案（触发存前后 5 根，平时不存）| 全部存 / 完全不存 / 5+10 根 |
| 4 | `dual_ma_trade_process` | v1.5 不做 | v1.5 一起做 |

**命名变更**：
- ~~`turtle_trade_log`~~ → **`turtle_trade_process`**
- 理由：避免和 `session_event_log` 命名冲突；"过程数据"比"log"更规范

**10 个 event_type 枚举**：
1. `entry_signal` / 2. `entry_filled` / 3. `add_signal` / 4. `add_filled` / 5. `add_skipped_gap` / 6. `stop_loss_check` / 7. `stop_loss_triggered` / 8. `exit_20_check` / 9. `exit_20_triggered` / 10. `unit_closed`

**check 类 C 方案详解**：
- `stop_loss_check` / `exit_20_check` 是每根 K 线都检查
- 平时不写 `turtle_trade_process`（避免数据量爆炸）
- **触发时**：存触发前 5 根 + 触发 + 触发后 5 根 = 共 10 条 check 事件
- 触发事件本身（`stop_loss_triggered` / `exit_20_triggered`）正常写
- **效果**：定位"为何触发"够用（看前 5 根价格走势），数据量不爆炸

**表数变化**：21 → 22（+1 turtle_trade_process）

**本地文档**：
- `0006_数据模型与界面重构设计.md` v1.5（预计 110KB / 2400+ 行 / 14 ## / 60+ ###）
- 新增章节：§4.3.6 / §7.7；新增子任务：3.15-3.18

**飞书同步**：
- 本轮**未同步飞书**（按用户明确要求：只改本地）
- 后续同步策略待定

#### v1.5 飞书同步补记（2026-06-02 下午 18:21）

**同步方式**：lark SDK（复用 v1.4 `/tmp/sync_v14_v4.py` → `/tmp/sync_v15.py`，改 `skipped_delete=False`）

**飞书 doc 终态**（实测，2026-06-02 18:21 验证）：
- 总顶层 block 数：**521**（= 1 H1 + 14 H2 + 69 H3 + 30 Table + 50 Code + 14 Quote + 157 Bullet + 46 Ordered + 125 Text + 15 Divider）
- 顶层表格数：30（22 个原始表 + 8 个被 MAX_ROWS=9 切分后多出的子表）
- 表格单元格填充：**842 个**（tables_created=30，cells_filled=842）
- H2: 14 ✅（期望 14）
- H3: 69 ✅（期望 57，差 12 = `####` 4 级标题被 parser 合并到 H3，与 v1.4 行为一致）
- Code 块: 50 ✅（期望 ≥48）

**v1.5 关键内容验证**（✅ 全部出现）：
- 标题层：`4.3.6 海龟交易过程数据` / `7.7 海龟交易过程流程` / `v1.5（2026-06-02）— 海龟交易过程数据子表`
- Code 块：`CREATE TABLE turtle_trade_process` / `4 Unit` / `idx_ttp_session` / `10 个 event_type` / `turtle_session_data`
- 关键词：`海龟交易过程` / `turtle_trade_process` / `4.3.6` / `7.7` / `4 Unit`

**同步耗时**：769.6 秒（12 分 50 秒）分解：
- Step 1 parse: 0.5s（80686 字符 → 513 blocks）
- Step 2 get root: 0.7s
- Step 3 delete 旧 v1.4 内容：9.9s（475 blocks / 50 per batch × 0.5s sleep = 10 batches）
- Step 4 create 新 v1.5 内容：757.5s（521 blocks / 18 batches × 0.5s sleep + 30 tables × 28 cells × 0.4s API + 0.05s sleep = 表填充占大头）
- Step 5 verify: 1.0s
- errors: **0** ✅

**脚本路径**：`/tmp/sync_v15.py`（v1.4 = `/tmp/sync_v14_v4.py`）
**沿用规范**：不准调试 SDK / 不看 read 状态 / 5 分钟 SDK 限制

**对比 v1.4 同步**（2026-06-02 上午 09:00-16:40）：
- v1.4 第一次同步历经 4 次子任务，3b4ccc65 死磕 lark SDK 调试 47+ 分钟才修复 `UpdateTextElementsRequest` 不是 list
- v1.5 同步**复用 v1.4 脚本**只改 1 个常量（`skipped_delete=True` → `False`）+ LOG_FILE / result.json 路径 → **零 SDK 调试时间**
- v1.5 总耗时 12:50（v1.4 5h+）；其中 v1.5 主流程（parse + delete + create + verify）= ~770s，相比 v1.4 同步执行耗时（v1.4 阶段 4 跑通时 8 分钟）几乎一致
- **核心经验**：v1.4 调试死磕沉淀出的 `/tmp/sync_v14_v4.py` **成为 v1.5 资产**，v1.5 同步一气呵成

**v1.5 同步子任务**：agent:main:subagent:036025a8-019b-40d8-9571-65db6af66f4e
**主任务 session**：agent:main:feishu:group:oc_050e2f94af46a6482ef18f807eb8dbcb

---

---

### v0.10.0（2026-06-02 晚）— _requirements 目录规范建立

**背景**：原模式"过程记录直接写在主设计文档"导致 0006 文档膨胀到 100KB+。改为目录结构管理。

**核心变更**：
1. 新建 `_requirements/data-model-redesign/` 目录结构（5 阶段 + _process / _iterations）
2. 4 个核心设计文档（总体/后端/前端/整合）
3. 5 个 _process 过程记录（v1.0 / v1.3 / v1.4 / v1.5 / 命名微调）
4. 4 个 _iterations 快照
5. 5 阶段占位 README（02-04 + 01-design）
6. 根 README.md 状态汇总

**协作规范更新**：
1. PROJECT_ZONE.md §2.1 追加 _requirements/ 标准结构
2. PROJECT_ZONE.md 新增 §八、_requirements 目录规范
3. AGENTS.md 新增"九、_requirements 协作规范"章节

**当前状态**：
- 01-design/ 阶段：🔄 进行中（80%）
- 02-plan / 03-execution / 04-verification：⏳ 待开始

**飞书同步**：本次**未同步飞书**（_requirements 是本地存档，不污染飞书）

## [v0.8.5] - 2026-05-29

### 新增

**FastAPI 接口实际逻辑对接**
- `api/main.py` 全部接入实际逻辑：
  - `GET /signals/{symbol}`：akshare 真实行情 + ATR/55 日突破信号计算
  - `GET /position/{symbol}`：`position.py` PositionManager 内存状态
  - `POST /position/{symbol}/add`：手动登记持仓（测试用）
  - `POST /backtest/run`：Backtrader + akshare 真实回测
  - `POST /minute/sync`：全量分时同步（33 品种自动找主力合约）
  - `GET /minute/{symbol}`：查询某品种分时数据
  - `GET /cache/status`：缓存诊断
- `backtrader` 安装，版本 1.9.78.123

**增量数据层**（`data/data_loader.py`）
- `incremental_update_daily()`：增量更新日线（最多回溯 7 天）
- `sync_all_minute_data()`：全量分时同步（枚举 33 品种，自动识别当前主力合约）
- `cache_status()`：缓存诊断
- `DAILY_RETENTION_DAYS = 14`：日线保留上限
- `MIN_RETENTION_DAYS = 7`：分时保留上限
- 自动清理过期缓存
- 表结构自动迁移（ALTER TABLE）
- Sina 品种代码映射 + 交割月配置（33 品种全覆盖）

**全量分时同步结果**
- 33/33 品种成功（33759 条记录）
- 主力合约自动识别：AG→ag2607、RB→rb2610、TA→ta2609 等
- 5 分钟分时数据，品种覆盖：贵金属(AG/AU/CU/AL/ZN/PB/NI/SN)、黑色(RB/HC/I/J/JM)、建材(FG)、能化(TA/MA/BU/EG/V/L/PP/RU)、农产品(M/Y/RM/OI/P/SR/CF/A/C/JD/CS)

### Bug 修复
- `api/main.py` `_fetch_latest_factor`：`row["date"]` → `row.name`
- `api/main.py` 回测日期格式：直接传 YYYY-MM-DD
- `api/main.py` `_TurtleStrategy`：Backtrader API 兼容（`data.close[0]`）

### 下一步
- 回测结果自动写入 PositionManager，接入 `/position` 接口
- 分时增量同步：每日定时任务自动更新

## [v0.8.4] - 2026-05-26

### 变更
- **§6.3 待确认项全部确认**（Alex 2026-05-22）：
  1. 热卷（HC）并入螺纹钢（RB）关联组
  2. 股指/国债不纳入回测品种池
  3. 日内突破回测：无分钟线时用「日 high/low 触达即成交」近似
  4. 名义资金 = initial_equity（净值口径）
- **0015 §6.3 更新**：文档已更新确认状态
- **P1 单元测试框架完成**：`strategies/turtle/tests/test_turtle_unit.py`
  - 14 个测试用例，覆盖 §8.1 全部 8 个场景
  - ATR EMA 递推 / Unit floor / 止损阶梯 / 跳空加仓 / 20日离市 / 70%清盘 / 90%风险度 / 组合约束

### Bug 修复
- **P2: position.py skip_adjust 止损计算修复**
  - 旧：`latest.stop_price = new_atr * 2`（多头空头相同，忽略 entry_price）
  - 新：`latest.stop_price = latest.entry_price - direction * 2 * new_atr`
  - 修复后跳空加仓的止损计算与文档 §3.6 跳空例外条款一致

### 新增
- **FastAPI 服务框架**：`api/main.py`
  - `POST /backtest/run` — 提交回测任务
  - `GET /backtest/{job_id}` — 查询回测结果
  - `GET /signals/{symbol}` — 获取交易信号
  - `GET /position/{symbol}` — 查询仓位状态
  - `GET /health` — 健康检查
  - 依赖：`fastapi>=0.100.0`, `uvicorn[standard]>=0.23.0`, `pydantic>=2.0.0`

### 已知问题
- FastAPI 接口待接入实际回测/信号/仓位计算逻辑（TODO 标注）

### 下一步
- P1：Sheet4 黄金样本回归测试（AG1912 成交序列回放）
- FastAPI：接入 `backtest_engine.py` 回测逻辑与 `position.py` 仓位管理

## [v0.8.3] - 2026-05-20

### 变更
- **P0 数据接口完成**：akshare 日线数据 + 因子计算模块正式启用
  - `data/data_loader.py`：akshare 日线获取 + ATR/55日最高/55日最低/均值成交量因子计算
  - `strategies/turtle/factors.py`：Unit/止损/加仓点公式（Excel 直译）
  - `strategies/turtle/position.py`：仓位状态机（Excel 逻辑）
  - `config/instruments.yaml`：40+ 品种静态配置（Sheet3 基础资料）
- **测试数据验证通过**：
  - AG 白银：3410 条历史（2012至今），close=18708, ATR=1036.20, 55日最高=24500
  - RB 螺纹钢：4160 条历史，close=3204, ATR=30.26
  - TA PTA：4714 条历史，ATR=234.17, 55日高低=7270/5182

### 下一步
- P1：接入 `backtest/` 做回测验证，跑通 0015 Sheet4 黄金样本回归测试

## [v0.8.2] - 2026-05-19

### 变更
- **文档目录重组**：调研文档（0010～0014）从根目录迁移至 `notebooks/research/`
  - 原：`0010_调研计划.md` → 新：`notebooks/research/0010_调研计划.md`
  - 原：`0011_回测系统详细设计.md` → 新：`notebooks/research/0011_回测系统详细设计.md`
  - 原：`0012_VN.py实盘对接详细设计.md` → 新：`notebooks/research/0012_VN.py实盘对接详细设计.md`
  - 原：`0013_T1海龟交易系统描述.md` → 新：`notebooks/research/0013_T1海龟交易系统描述.md`
  - 原：`0014_T1海龟交易系统设计.md` → 新：`notebooks/research/0014_T1海龟交易系统设计.md`
- **原始资料归档**：`notebooks/turtle-strategy/`（4个文件：2×docx + 2×xlsx）
- **版本记录同步**：变更日志新增 v0.8.2 条目

### 新增
- **0015_T1海龟交易系统完整项目设计.md**（v1.0）：整合 0013/0014/0005 + 原始资料的 SSOT 文档
  - T1 完整策略规格、入市/加仓/止损/出场规则详解
  - 基金风控体系（I.V、名义资金三档、70%清盘、90%风险度）
  - 领域模型、目标目录结构、与 0011 回测集成方案
  - P0～P6 实施计划与测试验收标准

## [v0.8.1] - 2026-05-17

### 新增
- **0015_T1海龟交易系统完整项目设计.md**：基于 `notebooks/turtle-strategy/` 原始资料（2×docx、2×xlsx）与 0013/0014/0005 整合的项目 SSOT，涵盖：
  - 经典海龟 vs T1 参数对照表
  - 完整算法规格（N/Unit/55入市/0.5N加仓/2N止损/20退出/组合Unit上限）
  - 基金风控（I.V、名义资金三档、70%清盘、90%风险度砍仓、交割月）
  - 目标目录结构、领域模型、与 0011 回测集成方案
  - P0～P6 实施计划与测试验收标准
  - §6.3 待确认项（关联组、股指纳入、日内回测近似、名义资金口径）

### 说明
- 原始资料归档目录：`notebooks/turtle-strategy/`（交易系统规则、风控方案、头寸计算器、2019 台账）

## [v0.8.0] - 2026-05-06

### 新增
- **0013_T1海龟交易系统描述.md**：海龟交易系统完整描述文档，涵盖：
  - 入市规则（55日突破、跳空处理）
  - 加仓规则（N值/ATR计算、1/2N间隔、4单位上限）
  - 止损规则（2N上限、动态上移1/2N）
  - 离市规则（20日反向突破）
  - T1基金风控体系（名义资金3档调整、I.V计算、70%清盘线）
  - 账户风控细则（90%风险度减仓、80%重新开仓）
- **0014_T1海龟交易系统设计.md**：海龟交易系统完整技术设计文档，涵盖：
  - 系统架构（六层：数据层→因子层→仓位层→信号层→执行层→风控层→回测层）
  - 数据层设计（market_data、portfolio_data、account_data）
  - 因子计算层（ATR/突破/离市信号完整Python实现）
  - 仓位管理层（TurtlePositionManager完整代码）
  - 信号与订单层（Order/Signal生成逻辑）
  - 风控层（单笔2%止损、账户风险度、净值管理）
  - 回测引擎设计 + 绩效分析指标
  - VNpy对接接口说明

### 变更
- **飞书文档读取**：三个飞书文档来源——交易系统描述（docx）、风控方案（docx）、交易品种表格（sheet）
  - 前两个docx已完整读取并整合
  - 表格文档暂无法直接读取（bitable API格式限制），品种列表/每点价值等参数待后续补充

### 待处理
- [ ] 补充品种表格文档（Enbwss1CBhWaoMtwfkHcGGBBnsc）中各品种的每点价值和保证金比例
- [ ] 0013/0014两个文档同步到飞书知识库

## [v0.7.5] - 2026-04-30

### 变更
- **0011_回测系统详细设计.md 实施计划更新**：
  - Phase 1-3 状态更新为 ✅ 已完成
  - 新增已完成交付物清单（backtest/目录5个文件）
- **0012_VN.py实盘对接详细设计.md 实施计划更新**：
  - Phase 3 状态更新为 ✅ 已完成（双均线策略转译）
  - 新增已完成交付物清单（vnpy_strategies/目录4个文件）

### 当前进度状态
| 模块 | 阶段 | 状态 |
|------|------|------|
| 调研文档 | 阶段一 | ✅ 已完成 |
| 回测系统 | 阶段二-A | 🔄 部分完成（Phase 1-3 已完成）|
| VN.py实盘 | 阶段二-C | 🔄 部分完成（Phase 3 已完成）|

## [v0.7.2] - 2026-04-29

### 新增
- **调研文档同步**：9份调研文档全部同步到飞书知识库（调研文档文件夹）
  - 0001_量化技术调研 ~ 0009_VN.py实盘对接补充调研
- **顶层文档同步**：0002_技术设计、0003_变更日志、0010_调研计划、0012_VN.py实盘对接详细设计 同步到飞书
- **调研文档文件夹**：`AtECwond6iLTnHkUoJ4cvLy9nUd` 作为父节点，9份调研文档在其下建立wiki节点

### 变更
- **项目设计文档 v0.7.1**：`0001_项目设计.md` 完整飞书节点映射表已更新
  - 包含17个文档的完整Token映射
  - 知识库结构树已更新

### 待处理
- [ ] 删除两个空占位节点（需在飞书知识库手动删除）
  - `V96UwmyORiW1rnkruKpcDqi1nGh` - Stock Quant - 股票量化（空文档）
  - `FwwkwaaGliAUOCkwmzIcULOLnhd` - Stock Quant - 股票量化（空文档）

## [v0.7.1] - 2026-04-29

### 变更
- **飞书知识库整合**：将分散在 AI个人博客 和 首页 下的4个股票量化节点，统一移入 Stock Quant 股票量化顶级节点

## [v0.7.0] - 2026-04-29

### 新增
- `vnpy_strategies/`：VN.py 策略实现目录（原 `vnpy/` 重命名以避免与安装包冲突）
  - `__init__.py`：模块初始化
  - `cta_strategies/__init__.py`：策略子模块导出
  - `cta_strategies/dual_ma_strategy.py`：DualMAStrategy 双均线策略实现
  - `cta_strategies/ctp_config_guide.md`：CTP 连接配置指南（SimNow + 实盘期货公司参数）

### 策略说明
- DualMAStrategy 基于 vnpy_ctastrategy.CtaTemplate（注意：非 CtaStrategy，后者不存在于 v4.3.0）
- 核心逻辑：快线MA上穿慢线MA → 金叉做多；快线MA下穿慢线MA → 死叉平多
- 参数：fast_ma=10, slow_ma=20, fixed_size=1
- 变量：pos, fast_ma_value, slow_ma_value

### 技术细节
- vnpy 4.3.0 中策略基类为 `CtaTemplate`（非 `CtaStrategy`）
- Direction 枚举从 `vnpy_ctastrategy` 直接导入
- ArrayManager 同样从 `vnpy_ctastrategy` 导入
- 命名冲突：本地目录 `vnpy/` 与安装包冲突，已重命名为 `vnpy_strategies/`

### 下一步
- 使用 SimNow 仿真账户运行策略
- 确认实盘期货公司 Broker ID 和服务器地址

---

## [v0.5.3] - 2026-04-28

### 新增
- `notebooks/research/0006_T1基金交易系统调研.md` v1.0：T1基金交易系统V1.0完整调研文档，涵盖：
  - 系统概述（定位、特点、设计意图）
  - 入市规则（55日突破、日内直接建仓、跳空处理）
  - 加仓规则（N/ATR系统、0.5N间隔、跳空市价追入）
  - 止损规则（2N上限、1/2N动态上移、跳空特殊处理）
  - 退出规则（20日反突破）
  - 交易品种（35个国内商品期货 + 强关联品种提示 + 交割月规则）
  - 风控方案（基金级完整体系：名义总资金3档调整、清盘线70%、风险度90%减仓、静默期）
  - 与原版海龟8大维度逐项对比（入市周期/ATR周期/出场周期/止损机制等）
  - 与后续策略开发关联建议

---

## [v0.5.4] - 2026-04-28

### 新增
- `notebooks/research/0007_文华财经交易软件调研.md`：WH8/WT9 功能详解
  - WH8（麦语言）：趋势策略 + 自动交易
  - WT9（宽语言）：量化策略开发平台（趋势/订单流/算法）
  - 与自建系统对比
  - 飞书已同步

### 同步
- 飞书知识库：https://feishu.cn/docx/HKVfd2xCQolLmhxftQ9cEpubnEb

---

## [v0.5.4] - 2026-04-28

### 新增
- `notebooks/research/0008_程序化期货交易开源软件调研.md`：程序化自动期货交易开源软件汇总
  - 国内主流框架（VN.py、EasyTrader、XTP等）
  - 国外框架（Backtrader、Zipline等）
  - VN.py 详细分析
  - 与自建系统对比
  - 飞书已同步

### 同步
- 飞书知识库：https://feishu.cn/docx/ZLfud8s33o88cTxPUndcLmcYn6f

---

## [v0.6.1] - 2026-04-29

### 新增
- `notebooks/research/0009_VN.py实盘对接补充调研.md`：VN.py 实盘对接详细设计补充调研文档，涵盖：
  - CTP 接口配置详解（地址、经纪商、认证信息）
  - CtaStrategy 策略模板源码解析（含 ArrayManager 使用）
  - DataFeed 数据配置方案（RQData/XTP/本地数据库）
  - Mac 系统 VN.py 安装 + 动态库信任配置
  - 多策略/多账号管理方案
  - 实盘账户开户指引
  - VN.py 安装命令汇总

### 同步
- 调研结论：建议将 6 Phase 扩展为 7 Phase（增加 Mac 兼容性验证）

---

## [v0.6.0] - 2026-04-28

### 新增
- `0012_VN.py实盘对接详细设计.md`：Phase 2-C VN.py 实盘对接完整设计
  - 对接架构（Backtrader → VN.py → CTP → 期货公司）
  - 策略转译方案（Backtrader → VN.py 代码对比）
  - 目录结构
  - 实施计划（6个Phase）
  - SimNow 模拟账户说明
  - 前提条件 + 风险提示

### 同步
- 飞书知识库：https://feishu.cn/docx/ZCbUdCGeLoIJ7Jx6joOco7mrnmh

---

## [v0.5.2] - 2026-04-28

### 新增
- `backtest/backtest_engine.py`：BacktestEngine 回测引擎封装
  - Cerebro 实例创建
  - 数据源添加
  - 策略添加
  - 回测运行
  - 内置分析器（TradeAnalyzer、SharpeRatio、DrawDown、AnnualReturn）

### 更新
- `backtest/__init__.py`：导出 BacktestEngine、create_engine
- `scripts/test_engine.py`：回测引擎测试脚本

---

## [v0.5.1] - 2026-04-28

### 新增
- `backtest/` 目录：Phase 1 回测系统数据层实现
  - `backtest/__init__.py`：模块初始化
  - `backtest/config.py`：配置（数据路径、数据库路径、默认参数、数据源配置）
  - `backtest/data_loader.py`：DataLoader 类，统一数据获取接口，支持 SQLite 本地缓存 + akshare 在线获取
- `scripts/run_backtest.py`：回测运行样例脚本
- `requirements.txt`：追加 akshare、baostock 依赖

### 功能
- `DataLoader.get_stock_daily()`：获取股票日线数据（支持 SQLite 缓存 + akshare 获取）
- `DataLoader.get_etf_daily()`：获取 ETF 日线数据（支持 SQLite 缓存 + akshare 获取）
- `DataLoader._init_db()`：自动创建 stock_daily、etf_daily 表
- `get_data_loader()`：便捷函数，返回 DataLoader 单例

### 状态
- Phase 1（数据层）：✅ 已完成

---

## [v0.5.0] - 2026-04-28

### 新增
- `0011_回测系统详细设计.md`：阶段二-A 回测系统完整设计文档
  - 系统架构（数据层→回测引擎→绩效评估→报告生成）
  - 目录结构
  - 5大模块详细设计（DataLoader、BacktestEngine、StrategyBase、Performance、Report）
  - 3套预置策略（双均线、海龟、网格）
  - 实施计划（8个Phase）

### 同步
- 飞书知识库同步：https://feishu.cn/docx/VaJvdxC6coKMtNxe406c63klnSb

### 更新
- `0010_调研计划.md`：同步阶段一执行状态，补充阶段二（工具开发）实施计划
  - 阶段一：0001-0005 调研文档状态更新为已完成
  - 阶段二：新增回测系统/多因子选股/实盘工具三方向实施计划
  - 阶段三：新增验证与优化阶段概览

---

## [v0.4.0] - 2026-04-28

### 新增
- `notebooks/research/0005_海龟交易策略深度调研.md` v1.0：海龟交易策略完整深度调研文档，涵盖：
  - 策略起源与背景（理查德·丹尼斯的故事）
  - 核心理念与原则（五大原则、心理偏差克服）
  - 完整交易规则详解（入场/加仓/止损/出场）
  - Unit 系统与仓位管理（含股票/期货/原油完整计算示例）
  - ATR 计算方法（True Range 定义 + Python 实现）
  - 参数设置与优化（含网格搜索、Walk-Forward 分析）
  - 品种筛选条件（A股ETF/商品期货适配度评级）
  - Python 完整代码实现（Backtrader 框架 + 纯 Python 两套方案）
  - 回测分析（沪深 300ETF 2018-2023 分年数据）
  - 实盘注意事项（交易前准备、执行要点、仓位管理）
  - 常见误区与解决方案（6 大误区及解决方案）
  - 参考文献 + 附录（ATR 计算代码、绩效评估模板）

---

## [v0.3.2] - 2026-04-23

### 更新
- `notebooks/research/0004_策略回测报告.md` 海龟交易策略章节补充三项技术细节：Unit 计算公式详解（含股票/期货示例）、品种筛选条件（含适配度评级）、参数优化建议（含市场环境适配表）；文档版本升至 v1.2

---

## [v0.3.1] - 2026-04-23

### 新增
- `notebooks/research/0004_策略回测报告.md` 新增第7章「更多交易策略技术详解」：涵盖 RSI 顺势策略、KDJ 金叉策略、布林带均值回归、CCI 超卖策略、区间突破策略、ATR 止损突破策略、唐奇安通道策略、肯特纳通道策略、放量突破策略、缩量回调策略、OBV 顺势策略、MFI 资金流量策略、多指标确认综合策略等 12 种进阶策略的原理、参数和完整代码

---

## [v0.3.0] - 2026-04-23

### 新增
- 创建策略回测报告 `notebooks/research/0004_策略回测报告.md`：涵盖 4 种策略（双均线、布林带、MACD、网格交易）在 3 个标的上的回测结果，含综合对比分析和实盘注意事项
- 创建技术栈实操手册 `notebooks/research/0003_技术栈实操手册.md`：涵盖数据获取（akshare/baostock/tushare）、Backtrader 回测框架、数据存储（SQLite/PostgreSQL）、实盘对接（VN.py/XTP/EasyTrader）、环境配置及目录结构
- 创建调研计划文档 `0010_调研计划.md`：规划8个调研模块，9份输出文档
- 量化投资案例调研 `notebooks/research/0002_量化投资案例调研.md`：成功/失败案例、实操策略详解

---

## [v0.2.0] - 2026-04-23

### 新增
- 创建量化技术调研文档：`notebooks/research/0001_量化技术调研.md`

### 更新
- 0002_技术设计.md：补充 AI/机器学习技术栈推荐（XGBoost/LightGBM、Backtrader）
- 策略方向：明确多因子选股 + 趋势跟踪为推荐组合
- 数据源：确认为 akshare + baostock 组合

---

## [v0.1.0] - 2026-04-23

### 新增
- 项目结构初始化
- 创建 0001_项目设计.md
- 创建 0002_技术设计.md
- 创建 0003_变更日志.md
- 创建 requirements.txt

#### v1.5 命名微调（2026-06-02 晚）

**用户反馈**：`exit_20_check` 命名不够简洁，调整为 `check_exit`

**修改**：
- `exit_20_check` → **`check_exit`**
- 影响范围：4 处（§v1.5 修订记录 event_type 列表 / §4.3.6 表格 / §4.3.6 文字描述 / §7.7 流程图注释）
- 同步调整：
  - §4.3.6 表格行 8：`exit_20_check` → `check_exit`
  - §7.7 流程图："20日反向检查（exit_20_check）" → "20日反向检查（check_exit）"
  - event_type 列表顺序不变（仍然在位置 8）

**`stop_loss_check` 保持不变**（用户没要求改）

**飞书同步**：本轮**未同步飞书**（按 v1.4 经验：客户端缓存 + 子任务 12 分钟重写，性价比不高）。等用户明确要求再做。


---

## [v0.10.1] - 2026-06-03

### _requirements/data-model-redesign/01-design/ 设计填实

**子任务**：`stock-quant-design-fillout-v2`（深 1/1）

**背景**：v0.10.0 拆分 4 个文档时仅完成骨架，v1 子任务只动 01-overall.md 一点点就超时失败。本次重新填实。

**4 文档填实**（在原结构上补充，不复制）：

| 文档 | 起点 | 终点 | 增量 | 重点新增内容 |
|------|------|------|------|------------|
| 01-overall.md | 369 行 | 553 行 | +50% | §7 现状分析 + §8 领域总览/设计原则/价格线"实时计算"反例 + §9 风险与回退 + §10 界面与数据表对应 + §11 9 个交互流清单 |
| 02-backend.md | 586 行 | 1247 行 | +113% | §8 22 张表精简 SQL + §9 价格线计算算法 + §10 用户调价 API + §11 Session 生命周期状态机 + §12 跨领域协同 |
| 03-frontend.md | 640 行 | 1043 行 | +63% | §9 9 个核心交互流（补全 4 个缺失） + §10 K 线图集成 + §11 前后端数据格式 + §12 轮询详细决策 + §13 可访问性/性能/国际化 |
| 04-integration.md | 488 行 | 923 行 | +89% | §10 详细测试用例 + §11 详细部署步骤 + §12 详细安全性 + §13 链路追踪 + §14 上线检查清单 |

**新增文档**：

- `05-project-structure.md`：项目结构（4 大功能模块：数据层 / API 层 / 策略层 / 前端层）+ 完整目录树 + 依赖关系 + 演进路径

**3 README 同步**：

- `01-design/README.md`：进度 80% → 100%，子任务 5/6/7/8 标 ✅，新增完成时间 2026-06-03
- `02-plan/README.md`：进入条件 4 个全部勾选
- 根 `README.md`：阶段状态汇总 设计 80% → 100%，填入完成时间 2026-06-03

**飞书同步**：本次**未同步飞书**（设计文档本地存档，按 v0.10.0 规范不同步飞书）

**主设计文档 0006**：本次**未修改**（0006 是事实源，4 个拆分文档从 0006 提炼）

---

## [v0.10.2] - 2026-06-03

### 02-plan/plan.md 执行计划规划

**背景**：v0.10.1 设计填实完成后，进入计划阶段。基于 0006 §9 实施计划 + 04-integration.md §四 Phase 1-7 + §10 18 项待确认 + §11 9 项风险，整合为完整 plan.md。

**产出**：
- `_requirements/data-model-redesign/02-plan/plan.md`（9973 字节）
  - 7 个 Phase（6 必做 + 1 可选）
  - 时间线：23-27 工作日 / 5-6 周
  - 9 项风险 + 4 项回退
  - 18 项待确认（全部采纳默认）
  - 22 张表按 Phase 分布清单
  - 资源分配 + 每 Phase 验收标准 + 进入条件

**状态**：
- 02-plan/README.md：⏳ 待开始 → 🔄 已规划，待用户审核
- 根 README.md：阶段状态汇总 设计 100% + 计划 100%（待审核）
- 维护责任人：计划 → Alex + Blex

**飞书同步**：本次**未同步飞书**（plan.md 是执行层文档，按规范不同步飞书）

**下一步**：等用户审核 plan.md → 拍板"可以执行" → 派 Phase 1 子任务开始实施

---

## [v0.11.0] - 2026-06-03

### Phase 1 完工：合约行情领域（4 子任务全部完成）

**背景**：plan.md v0.1.0 审核通过后，按"先实施第一个 Phase"指令，拆 4 个 25 分钟子任务完成 Phase 1 全部工作（3-4 天工作量压缩到 25 分钟级别）。

**4 个子任务汇总**：

| 子任务 | runId 前缀 | 耗时 | 关键产物 |
|--------|-----------|------|---------|
| Phase 1.1 schema | c9facaa8 | ~2m | 4 张表（futures_contracts / futures_quotes / daily +atr / min +date）+ 6 索引 + init_db 幂等迁移 |
| Phase 1.2 retention | 7d09baf4 | ~6m | retention 62→1095 / 7→180 + 4 函数（backfill + identify_main + sync_all）|
| Phase 1.3 API | a08a2231 | ~5m | 3 新 API（/contracts / /quotes / /main-contract）+ 2 改写（/daily +atr / /minute +date）|
| Phase 1.4 前端 | c04d5958 | ~4m | 品种卡片 📋 角标 + 合约抽屉 8 列表格 + 主力行高亮 + 3 种关闭方式 |

**Phase 1 验收（04-integration.md §Phase 1 全部达标）**：
- ✅ 点 AG 卡片能弹出 4 个合约的报价列表（合约抽屉 4 行：ag2607⭐主力 + ag2609/11/2701）
- ✅ 日线 retention 3 年（1095 天）+ 分时 retention 半年（180 天）
- ✅ 主力合约高亮（绿色 ⭐）

**文件变更**：

| 文件 | 起点 | 终点 | 增量 |
|------|------|------|------|
| data/data_loader.py | 1320 行 | 1771 行 | +451（4 表 + 4 函数 + retention）|
| api/main.py | 634 行 | 784 行 | +150（3 新 API + 2 改写）|
| www/index.html | 1130 行 | 970 行* | +CSS/HTML/JS（合约抽屉 + 角标）|

\* 注：原 index.html wc -l 1130 是含 35057 字节，新版 970 行 + 9529 字节新增（具体行数因 JS 整合调整而变化，字节净增）

**DB 现状**：
- 5 张表：futures_contracts（4 行 AG）/ futures_daily（2901 行）/ futures_min（34733 行）/ futures_quotes（0 行待 Phase 2 实时同步）/ turtle_signals（0 行）
- futures_contracts AG 4 行：ag2607（is_main=1）/ ag2609/2611/2701

**下一步**：等用户决策 Phase 2 启动（策略领域：6 张表 + 7 API + V1/V2 代码隔离 + 策略历史）


## [v0.10.3] - 2026-06-03

### Phase 1.2 实施：retention 调整 + 数据回填函数 + 主力合约识别

**背景**：Phase 1.1（4 张表 schema）已完成。本 Phase 完成 retention 调整、回填函数、主力识别。

**目标**（来自 04-integration.md §Phase 1）：
1. retention 常量调整：futures_daily 62→1095（3 年）、futures_min 7→180（半年）
2. 实现 4 个新函数：`backfill_daily_history` / `backfill_min_history` / `identify_main_contract` / `sync_all_contracts_and_main`
3. AG 品种跑通测试

**变更详情**：

| 位置 | 变更类型 | 内容 |
|------|---------|------|
| `data/data_loader.py` L51-52 | retention 常量调整 | `DAILY_RETENTION_DAYS = 1095`（3 年）`MIN_RETENTION_DAYS = 180`（半年）|
| `data/data_loader.py` `_make_contract_codes` | Bug 修复 | 去重 + 按交割月顺序递进（修前会生成 ag2607/ag2607 重复，错过 ag2608 真实主力）|
| `data/data_loader.py` 新增 | `SYMBOL_EXCHANGE` | 品种→交易所映射（shfe/dce/czce/ine/gfex）|
| `data/data_loader.py` 新增 | `_get_instrument_info()` | 从 instruments.yaml 读品种静态信息 |
| `data/data_loader.py` 新增（~370 行）| 4 个新函数 | `backfill_daily_history` / `backfill_min_history` / `identify_main_contract` / `sync_all_contracts_and_main` |

**4 个新函数签名**：
- `backfill_daily_history(symbol, years=3) -> int`：ak.futures_zh_daily_sina 拉主力连续合约历史，过滤比缓存更新的行写入 futures_daily
- `backfill_min_history(symbol, days=180, period="5min") -> int`：ak.futures_zh_minute_sina 拉近 4 合约，自动派生 date 字段
- `identify_main_contract(symbol) -> Optional[str]`：遍历近 4 合约，按 hold 降序，写 is_main=1, main_rank=1
- `sync_all_contracts_and_main() -> int`：遍历 instruments.yaml 所有品种

**AG 测试结果**：

| 测试 | 结果 |
|------|------|
| 主力识别 | `ag2607`（oi=14920），全部 4 合约: ag2607/09/11/2701 |
| 日线回填 1 年 | 3 行新增（2026-05-29 ~ 2026-06-02），缓存已最新 |
| 分时回填 7 天 | 233 行新增（2026-06-01 ~ 2026-06-03 09:55），全部带 date 字段 |
| futures_contracts 表 | AG 4 行（1 行 is_main=1 + 3 行 is_main=0）|

**最终数据库状态**：
- futures_daily: 2904 行（v1.4 2901 → +3）
- futures_min: 34966 行（v1.4 34733 → +233，全为 AG 新增）
- futures_contracts: 4 行（新增）

**验证清单**（9 项全部通过）：
- [✅] retention 常量已改（1095/180）
- [✅] init_db + cleanup 不报错
- [✅] 4 个新函数定义存在
- [✅] AG 主力识别返回 'ag2607'
- [✅] 日线回填函数可调用
- [✅] 分时回填函数可调用，date 字段有值（233 行）
- [✅] futures_contracts 表 4 行（AG）
- [✅] 现有 5 个 API 不破坏（get_futures_daily / get_minute_data / get_minute_from_cache / get_daily_data_from_cache / get_daily_ref_levels）
- [✅] futures_daily AG 行数 = 728（>= 60）

**遗留风险 / 后续 Phase**：
1. Phase 1.2 数据回填仅 AG 品种，其他 32 个品种未回填（按规范"不全量回填"）
2. `_make_contract_codes` 修复后，`sync_all_minute_data` 等其他内部调用方也会使用修复后版本（行为变更，但更正确）
3. instruments.yaml 中 AG 等品种的 `delivery_months` 与 SHFE 实际合约月份不符（AG 实际每月都有合约，但 yaml 写的是 [1,3,5,7,9,11]）。本次未改 yaml。
4. Phase 1.3+ 才做 5 个 API 实现

**文件变更**：
- `data/data_loader.py`：1399 → 1770 行（+371 行）
- 飞书同步：本次**未同步飞书**（按 v0.10.0 规范）

**耗时**：约 6 分钟


---

## v1.5 - Phase 1.3：5 个 API 端点（2026-06-03）

**变更类型**：功能新增
**执行任务**：stock-quant-phase-1-3-api
**前置条件**：Phase 1.2 已完成（retention + 回填函数 + 主力识别）

### 背景

`api/main.py` 之前只有 16 个端点，无法支持前端查合约、主力、行情、K 线（带 atr）。本阶段新增 3 个、改写 2 个。

### 新增 API（3 个）

| 端点 | 功能 | 实现 |
|------|------|------|
| `GET /contracts/{symbol}` | 查询某品种所有合约（含主力标记）| `SELECT * FROM futures_contracts WHERE symbol=? ORDER BY is_main DESC, main_rank ASC` |
| `GET /quotes/{contract_code}` | 查询某合约实时行情 | `SELECT * FROM futures_quotes WHERE contract_code=?` |
| `GET /main-contract/{symbol}` | 快速查主力合约 | `SELECT * FROM futures_contracts WHERE symbol=? AND is_main=1 LIMIT 1` |

### 改写 API（2 个）

| 端点 | 新增字段 | 实现 |
|------|---------|------|
| `GET /daily/{symbol}` | `atr` + `main_contract_code` + `include_atr` 参数 | data_loader 已算 atr；DB 读 main_contract_code |
| `GET /minute/{symbol}` | `date` 字段（NULL 时从 datetime 派生）| 自动补齐，老数据兼容 |

### 关键技术点

- **/contracts/{symbol}**：返回 `count` / `main_contract` / `contracts` 列表，主力（is_main=1）排第一
- **/quotes/{contract_code}**：暂未填充数据（需实时同步任务），返回 `note` 提示
- **/main-contract/{symbol}**：单条 SQL 限 1 行，响应 < 5ms
- **/daily**：保留原 `open/high/low/close/volume/datetime/date` 7 字段，新增 `atr` + `main_contract_code`
- **/minute**：保留原 6 字段，新增 `date`；DB 中 date 为 NULL 时从 datetime 前 10 位派生
- **错误处理**：用 `HTTPException(500, detail=...)` 统一；空数据用 `note` 字段提示

### 验证结果（8 项全部通过）

| 验证项 | 结果 |
|--------|------|
| 5 个新/改 API 全部 200 | ✅ 全部 200 |
| 现有 16 个 API 不破坏 | ✅ 12/12 抽样 200 |
| /daily/AG?days=30 records 含 atr | ✅ 9 字段（atr + main_contract_code 都在）|
| /daily/AG?days=30 records 含 main_contract_code | ✅ AG 为 null（DB 旧行未填）|
| /minute/AG?days=3 records 含 date | ✅ 234 行全部有 date |
| 老数据（date=NULL）派生 | ✅ 58 行 2026-05-18 数据全部派生为 2026-05-18 |
| /main-contract/AG 响应 < 0.05s | ✅ 5 次平均 4.0ms（min=0.6ms, max=17.2ms）|
| data_loader 15 个核心函数可导入 | ✅ 全部 OK |
| include_atr=false 参数 | ✅ 关闭后 records 不含 atr |
| 多品种合约查询 | ✅ AG 返回 4 合约 + 主力 ag2607；AU/CU 返回空 + note |
| 旧字段（open/high/low/close/volume/datetime/date）保留 | ✅ 全部保留 |

### 端点清单（21 个 = 16 旧 + 5 新/改）

```
新增（3）：
  /contracts/{symbol}
  /quotes/{contract_code}
  /main-contract/{symbol}

改写（2）：
  /daily/{symbol}            ← 增加 atr + main_contract_code
  /minute/{symbol}           ← 增加 date 字段
```

### 文件变更

| 文件 | 行数变化 | 字节变化 |
|------|---------|---------|
| `api/main.py` | 634 → 784（+150）| 21738 → 27263（+5525）|

### 不破坏性保证

- ✅ data/data_loader.py 未动
- ✅ 4 张表 schema 未动
- ✅ 16 个旧 API 端点行为保持（仅 /daily 和 /minute 内部增强）
- ✅ 旧字段全部保留（向后兼容）
- ✅ 新字段通过 include_atr=false 等参数可选关闭
- ✅ 未 git commit（按规范）

### 耗时

约 5 分钟

### 状态

[✅] 完整完成

---

## [v1.6.0] - 2026-06-03 — Phase 2.1 策略领域表 schema

### 背景

Phase 1（4 子任务：1.1 init_db 迁移 / 1.2 子表补齐 / 1.3 API 端点 / 1.4 前端 v1.3）已完工。
Phase 2.1 落地策略领域 5 张表 + 7 索引 + 2 种子策略。

### 新增（5 张表 + 7 索引 + 2 种子行）

| # | 表名 | 阶段 | 字段数 | 关键字段 |
|---|------|------|-------|---------|
| 1 | `strategies` | v1.3 +version | 10 | strategy_id, name, version, type, params_json, is_active, is_paper, UNIQUE(name, version) |
| 2 | `strategy_param_history` | v1.3 | 9 | strategy_id, version, params_json, changed_at, changed_by, change_reason |
| 3 | `strategy_signals` | v1.3 精简 | 14 | strategy_id, signal_type, trigger_price, bar_time, UNIQUE(strategy_id, symbol, signal_type, bar_time) |
| 4 | `strategy_event_log` | v1.4 | 7 | strategy_id, event_type, event_at, context_json |
| 5 | `turtle_trade_process` | v1.5 | 23 | session_id（无 FK，留给 Phase 3）, event_type（10 枚举）, bar OHLC, unit_id |

#### 7 个索引

| 索引名 | 表 | 字段 |
|-------|----|------|
| `idx_sph_strategy` | strategy_param_history | (strategy_id, version) |
| `idx_ss_strategy` | strategy_signals | (strategy_id, bar_time) |
| `idx_ss_active` | strategy_signals | (strategy_id, symbol, direction, alert_level) |
| `idx_sel_strategy_at` | strategy_event_log | (strategy_id, event_at) |
| `idx_ttp_session` | turtle_trade_process | (session_id, bar_time) |
| `idx_ttp_type` | turtle_trade_process | (event_type, bar_time) |
| `idx_ttp_bar` | turtle_trade_process | (bar_time) |

#### 2 种子策略（INSERT OR IGNORE 幂等）

| strategy_id | name | version | type | is_active | params_json |
|------------|------|---------|------|-----------|-------------|
| `turtle_v1` | 海龟T1 | 1 | turtle | 1 | `{entry_n:55, exit_n:20, add_interval:0.5, stop_loss:2, atr_n:20, max_units:4, risk_per_trade:2000}` |
| `dual_ma_v1` | 双均线 | 1 | dual_ma | 0 | `{fast:10, slow:20}` |

### 关键设计决策

1. **turtle_trade_process 不带外键**：因为 trade_sessions 是 Phase 3 才建。
   用 SQL 注释明确标记 `session_id TEXT NOT NULL, -- FK to trade_sessions (Phase 3 会加)`
2. **种子数据时间戳**：在 Python 端用 `datetime.now().isoformat()` 计算后传入，
   避免 SQLite 中 `datetime('now').isoformat()` 语法错误（`.isoformat()` 不是 SQLite 函数）
3. **datetime 格式**：所有 created_at/updated_at 都用 ISO 8601（`2026-06-03T10:40:xx.xxx`），
   方便 Python json 序列化时直接 parse
4. **strategies 不创建额外索引**：用 UNIQUE(name, version) 约束代替，避免冗余
5. **CREATE TABLE IF NOT EXISTS + CREATE INDEX IF NOT EXISTS + INSERT OR IGNORE**：
   三重幂等保证，重复跑 init_db() 不会报错

### event_type 枚举（10 个，记到表注释和 docstring）

```
entry_signal / entry_filled
add_signal / add_filled / add_skipped_gap
stop_loss_check / stop_loss_triggered
check_exit / exit_20_triggered
unit_closed
```

### 验证

| 验证项 | 结果 |
|--------|------|
| 5 张新表都存在 | ✅ strategies / strategy_param_history / strategy_signals / strategy_event_log / turtle_trade_process |
| 种子数据 turtle_v1 + dual_ma_v1 都有 | ✅ 2 行（turtle_v1 is_active=1, dual_ma_v1 is_active=0）|
| 7 索引全部创建 | ✅ idx_sph_strategy / idx_ss_active / idx_ss_strategy / idx_sel_strategy_at / idx_ttp_bar / idx_ttp_session / idx_ttp_type |
| turtle_trade_process 无外键约束 | ✅ pragma_foreign_key_list 返回空（Phase 3 再加 FK）|
| 幂等测试：init_db() 跑 3 次不报错 | ✅ OK |
| 现有 5 张表数据保留 | ✅ 4 / 2904 / 34966 / 0 / 0（与 Phase 1 一致；注：dataline/min 比 spec 略多，是因 DB 缓存自然增长，cleanup 正常工作）|
| 现有 21 个 API 端点全部 200 | ✅ 抽样 7 个：/、/health、/contracts/AG、/main-contract/AG、/daily/AG?days=5、/minute/AG?days=1、/signals/all |
| data_loader 现有函数不破坏 | ✅ init_db / get_futures_daily / get_minute_from_cache / backfill_daily_history / identify_main_contract 全部可导入 |

### 文件变更

| 文件 | 行数变化 | 说明 |
|------|---------|------|
| `data/data_loader.py` | 1771 → 1897（+126）| init_db() 内新增 5 张表 + 7 索引 + 2 种子行；docstring 更新 |

### 不破坏性保证

- ✅ Phase 1 已实施的 5 张表（futures_contracts / daily / min / quotes / turtle_signals）完全未动
- ✅ api/main.py（Phase 1.3 已完成）未动
- ✅ www/index.html（Phase 1.4 已完成）未动
- ✅ 21 个现有 API 端点行为保持
- ✅ strategies/ 目录未建（Phase 2.2 才做）
- ✅ 7 个新 API 未实现（Phase 2.3 才做）
- ✅ 策略执行器 on_bar 未写（Phase 2.4 才做）
- ✅ 未调 lark SDK
- ✅ 未 git commit（按规范）

### 备份

- DB 备份：`data/futures_akshare.db.bak-phase2.1-20260603-103908`（4.7 MB）
- 备份时间：2026-06-03 10:39:08（修改前一刻）

### 耗时

约 3 分钟（备份 + 代码改动 + 10 项验证）

### 状态

[✅] 完整完成

---

## [v0.12.0] - 2026-06-03

### Phase 2 完工：策略领域（5 子任务全部完成）

**背景**：plan.md v0.1.0 审核通过后 Phase 1（4 子任务）完工，继续 Phase 2（5 子任务）。

**5 个子任务汇总**：

| 子任务 | runId 前缀 | 耗时 | 关键产物 |
|--------|-----------|------|---------|
| Phase 2.1 schema | 9a325187 | ~4m | 5 张策略表（strategies/strategy_param_history/strategy_signals/strategy_event_log/turtle_trade_process）+ 7 索引 + 2 行种子 |
| Phase 2.2 代码 | f78e71ff | ~8m | strategies/base.py + loader.py + V1/strategy.py 继承 + position.py 软链 + dual_ma 占位 |
| Phase 2.3 API | d6c76e5a | ~4m | 7 个端点（4 CRUD + 1 signal + 1 param-history）|
| Phase 2.4 执行器 | 2596cafd | ~5m | strategies/executor.py（389 行）+ TurtleStrategy.on_bar（55日/20日）|
| Phase 2.5 前端 | 000484ee | ~8m | 顶部栏策略切换器 + 详情区策略历史面板（3 Tab） + events 端点 |

**Phase 2 验收（plan.md §Phase 2 全部达标）**：
- ✅ 6 张表创建 + 索引（5 张新建 + 1 张已删的 signal_states 不建）
- ✅ 7 个 API 端点全部正常
- ✅ 策略切换器能切换海龟 V1（默认选中）
- ✅ 策略执行器能生成 entry_long / entry_short 信号
- ✅ turtle_trade_process 表创建（空，Phase 3 才有数据）

**文件变更**：

| 文件 | 起点 | 终点 | 增量 |
|------|------|------|------|
| data/data_loader.py | 1771 | 1900 | +129（5 表 + 7 索引）|
| strategies/base.py | 0 | 33 | +33（新建）|
| strategies/loader.py | 0 | 76 | +76（新建）|
| strategies/turtle/V1/strategy.py | 256 | 326 | +70（on_bar 实现）|
| strategies/executor.py | 0 | 389 | +389（新建）|
| api/main.py | 784 | 1061 | +277（7+1 端点 + 2 Pydantic）|
| www/index.html | 970 | 1267 | +297（CSS + HTML + JS）|

**DB 现状**：
- 10 张表：5 旧（futures_contracts/daily/min/quotes + turtle_signals）+ 5 新（strategies/strategy_param_history/strategy_signals/strategy_event_log/turtle_trade_process）
- strategies：3 行（turtle_v1 active + dual_ma_v1 active=0 + test_v1 active=0 测试残留）
- strategy_event_log：12 行（engine_started/stopped + signal_detected）
- strategy_param_history：2 行（test_v1 创建+更新）
- strategy_signals：0 行（4 品种无 55日突破）
- turtle_trade_process：0 行（Phase 3 才有数据）

**端点总数**：29 个（16 旧 + 5 Phase 1.3 + 7 Phase 2.3 + 1 Phase 2.5 events）

**下一步**：等用户决策 Phase 3 启动（交易 / 账户领域：10 张表 + 12 API + 4 模态框 + 调价 + 4 Unit + 跳空，10-12 天）

---

## [v0.13.0] - 2026-06-03

### Phase 3 完工：交易 / 账户领域（9 子任务全部完成）

**背景**：plan.md v0.1.0 审核通过后 Phase 1 + 2 完工（10 子任务），继续 Phase 3（9 子任务）。

**9 个子任务汇总**：

| 子任务 | runId 前缀 | 耗时 | 关键产物 |
|--------|-----------|------|---------|
| Phase 3.1 schema | 96413401 | ~4m | 11 张表（sim_account/trade_sessions/orders/trades/positions/units/turtle_session_data/session_event_log + 3 live_*）+ 5 FK + 7 索引 + turtle_trade_process 修复 FK |
| Phase 3.2 sim_engine | 65628624 | ~6m | api/services/sim_engine.py（388 行，5 核心函数：submit_order/open_session/add_unit/close_unit/close_session）|
| Phase 3.3 lifecycle | 505d3a54 | ~3m | session_lifecycle.py（362 行，状态机+锁+列表+详情+12 公开符号）|
| Phase 3.4 line_calculator | 34aef331 | ~4m | line_calculator.py（334 行，3 色价格线+override+缓存）|
| Phase 3.5 event_logger | 7a0ae347 | ~4m | event_logger.py（422 行，9 log_* + log_batch + 2 查询）|
| Phase 3.6 API | fbc405fd | ~12m | 7 新端点（POST /session/orders/orders-reduce/close + GET events/sessions/lines + PUT line）|
| Phase 3.7 modals | 1db03f3b + 83703edd | ~7m | 4 模态框 + 顶部操作组 + 调价集成（v2 重试 1 次，v1 失败）|
| Phase 3.8 trade_process | 6a9302db | ~10m | trade_process_logger.py（267 行）+ on_bar 改造 + C 方案 + 3.18 trade-process API |
| Phase 3.9 chart+4unit | 18022c53 | ~6m | 价格线 bar + 4 Unit 面板 + 跳空标志 + 交易过程时间线（v1.5 完整集成）|

**Phase 3 验收（plan.md §Phase 3 + 04-integration.md 全部达标）**：
- ✅ 8 张活跃表 + 3 张预留（live_*）+ 5 FK 关联
- ✅ 5 服务模块（sim_engine / session_lifecycle / line_calculator / event_logger / trade_process_logger）
- ✅ 12 个新 API（含 1 个 v1.5 trade-process 端点）
- ✅ 4 模态框（建仓/加仓/减仓/全平）+ 顶部操作组 + 调价集成
- ✅ K 线图集成价格线 + 4 Unit 详情 + 跳空标志
- ✅ v1.5 完整：3.15 trade_process_logger + 3.16 退仓优先 + 3.17 C 方案 + 3.18 trade-process API
- ✅ 9 个 v1.5 事件类型完整（entry_signal/entry_filled/add_signal/add_filled/add_skipped_gap/stop_loss_check/stop_loss_triggered/check_exit/exit_20_triggered/unit_closed）

**文件变更汇总**：

| 文件 | 起点 | 终点 | 增量 |
|------|------|------|------|
| data/data_loader.py | 1900 | 2200 | +300（11 表 + 7 索引）|
| strategies/base.py | 33 | 33 | 0（Phase 2.2）|
| strategies/loader.py | 76 | 76 | 0（Phase 2.2）|
| strategies/turtle/V1/strategy.py | 256 | 326 | +70（Phase 2.2 + Phase 3.8 on_bar 改造）|
| strategies/executor.py | 389 | 389 | 0（Phase 2.4，加注释）|
| strategies/trade_process_logger.py | 0 | 267 | +267（新建，Phase 3.8）|
| api/services/__init__.py | 0 | 0 | 0（空）|
| api/services/sim_engine.py | 0 | 388 | +388（新建，Phase 3.2）|
| api/services/session_lifecycle.py | 0 | 362 | +362（新建，Phase 3.3）|
| api/services/line_calculator.py | 0 | 334 | +334（新建，Phase 3.4）|
| api/services/event_logger.py | 0 | 422 | +422（新建，Phase 3.5）|
| api/main.py | 1061 | 1340 | +279（7 端点 + 4 Pydantic）|
| www/index.html | 1267 | 1682 | +415（4 模态框 + 顶部操作 + 价格线 + 4 Unit + 时间线）|

**DB 现状**：
- 21 张表全部存在
- sim_account：3 行种子（sim_default / sim_user1 / sim_user2）
- trade_sessions：7+ 行（含 1 个 4-unit session，0 跳空记录 + 1 stop_loss_check C 方案 11 根）
- sim_orders / sim_trades / sim_positions / position_units / turtle_session_data / session_event_log：Phase 3 测试遗留
- strategy_signals：0 行（4 品种无 55日突破）
- turtle_trade_process：18+ 行（包含 1 个 is_gap=1 跳空数据）
- futures_contracts / futures_daily / futures_min / futures_quotes：Phase 1 数据保留
- strategies / strategy_param_history / strategy_event_log：Phase 2 数据保留

**端点总数**：**37 个**（16 旧 + 5 Phase 1.3 + 7 Phase 2.3 + 1 Phase 2.5 events + 7 Phase 3.6 + 1 Phase 3.8 trade-process）

**模块矩阵**：
- 数据层：data/data_loader.py
- 策略层：strategies/{base.py, loader.py, executor.py, V1/strategy.py, trade_process_logger.py}
- 服务层：api/services/{sim_engine, session_lifecycle, line_calculator, event_logger}.py
- API 层：api/main.py（37 端点）
- 前端：www/index.html（1682 行，4 模态 + 价格线 + 4 Unit + 时间线）

**v1.5 完整覆盖**：
- 3.15 turtle_trade_process 表写入（open_session/add_unit/close_unit 集成）
- 3.16 退仓优先顺序（on_bar 先止损 → 后加仓 → 最后反向）
- 3.17 check 类事件 C 方案（前后 5 根 K 线共 11 根）
- 3.18 trade-process API + 前端时间线展示

**下一步决策点**：
- Phase 4（回测持久化，1 个子任务）
- git commit + 飞书同步
- 部署验证
- 调整细节

---

## [v0.14.0] - 2026-06-03

### Phase 5 完工：账户中心页（2 子任务全部完成）

**背景**：plan.md v0.1.0 审核通过后 Phase 1-4 完工（20 子任务），继续 Phase 5（2 子任务）。

**2 个子任务汇总**：

| 子任务 | runId 前缀 | 耗时 | 关键产物 |
|--------|-----------|------|---------|
| Phase 5.1 API | 2d5ab473 | ~4m | api/main.py 5 端点（/account/overview + /positions + /units + /sessions + /trades，247 行）|
| Phase 5.2 前端 | 7749897a | ~8m | www/index.html 账户切换器 + 8 summary 卡片 + 5 Tab 模态框（+339 行）|

**Phase 5 验收（plan.md §Phase 5 全部达标）**：
- ✅ `/account/overview` 聚合 5 表（sim_account + sim_positions + trade_sessions + position_units + sim_trades）
- ✅ 价格线集成（open_sessions 每条含 lines 字段，复用 line_calculator.compute_lines）
- ✅ 4 Unit 明细按 session_id 分组（by_session 字段）
- ✅ Session 列表 + 持仓 + 成交 4 个子端点
- ✅ 顶部"账户切换器"（sim_default / sim_user1 / sim_user2 + 账户中心入口）
- ✅ 8 个 summary 卡片（余额/可用/持仓/浮动盈亏/已实现/Open Sessions/Open Units/近 N 天成交）
- ✅ 5 Tab 模态框（总览/持仓/Unit 明细/Session 列表/成交记录）
- ✅ ESC 关闭 + 点击外部关闭
- ✅ switchAccount 联动 loadOpenSessions + loadAccountSummary

**文件变更**：

| 文件 | 起点 | 终点 | 增量 |
|------|------|------|------|
| api/main.py | 1601 | 1848 | +247（5 端点 + 区块标题 + 注释）|
| www/index.html | 1780 | 2119 | +339（CSS + HTML + JS）|

**DB 现状**：22 张表 + sim_account 8 行（3 种子 + 5 backtest_）

**端点总数**：**47 个**（42 + 5 新）

**累计全局**：
- 22 张表（5 旧 + 5 策略 + 11 交易 + 1 回测）
- 6 服务（sim_engine / session_lifecycle / line_calculator / event_logger / trade_process_logger / BacktestEngine）
- 47 个 API 端点
- 2119 行前端（4 模态 + 顶部操作 + 策略切换 + 合约抽屉 + 4 Unit + 价格线 + 跳空 + 时间线 + 账户中心）
- 累计 22 个子任务

**下一步决策点**：
- Phase 6（单测 + 集成测试，1 个子任务）
- git commit + 飞书同步
- 部署验证
- 调整细节

---

## [v0.15.0] - 2026-06-03

### Phase 6 完工：单测 + 集成测试 + 性能测试（2 子任务全部完成）

**背景**：plan.md v0.1.0 审核通过后 Phase 1-5 完工（22 子任务），继续 Phase 6（2 子任务）。

**2 个子任务汇总**：

| 子任务 | runId 前缀 | 耗时 | 关键产物 |
|--------|-----------|------|---------|
| Phase 6.1 单测 | 7590dbf8 | ~17m | pytest 框架 + 7 单测文件（test_sim_engine/test_line_calculator/test_session_lifecycle/test_multi_account/test_strategy_loader/test_event_logger/test_trade_process_logger）|
| Phase 6.2 集成 + 性能 | 7858c281 | ~8m | test_integration.py（13 集成用例）+ locustfile.py（6 task 压测）|

**Phase 6 验收（plan.md + 04-integration.md §Phase 6 全部达标）**：
- ✅ **82 个测试全通过**（69 单测 + 13 集成，0 失败）
- ✅ **5 服务层覆盖率平均 71.6%**：
  - sim_engine 80% / loader 98% / base 77% / session_lifecycle 73% / line_calculator 72% / event_logger 66% / trade_process_logger 67%
  - **总覆盖率 53%**（核心服务超过 60% 目标）
- ✅ **性能基线**（实测）：
  - 100 session 建仓：0.84s（< 30s 阈值）
  - 100 调价：0.38s（< 5s 阈值）
  - 50 session × 4 unit 全平仓：1.81s（< 15s 阈值）
  - locust 5s 压测：20.52 RPS / 100 请求 / 0 真实失败
- ✅ **端到端测试覆盖**：信号 → 建仓 → 加仓 4 unit → 调价 → 减仓 → 全平仓
- ✅ **v1.5 C 方案测试**：11 根 K 线上下文（trigger 前后各 5）
- ✅ **多账户隔离测试**：3 账户独立，互不影响
- ✅ **与 0016 通达信字段对齐**：v1.3 简化 signal_type（entry_long / entry_short）
- ✅ **状态机测试**：5 个状态转换合法/非法组合

**新增文件**：
- `tests/__init__.py`（空）
- `tests/conftest.py`（125 行：临时 DB fixture + 5 fixture）
- `pytest.ini`（testpaths / 命名约定 / -v --tb=short）
- 7 个单测文件（~40 KB）
- `tests/test_integration.py`（504 行 / 17 KB / 13 集成测试）
- `tests/locustfile.py`（80 行 / 2.3 KB / 6 task）
- 依赖：pytest 9.0.3 + pytest-cov 7.1.0 + locust 2.44.0

**关键技术决策**：
- **零侵入** 测试设计：不动 5 service / 22 表 / 47 端点 / production DB
- **临时文件 DB**：每个测试独立 `tempfile.mkstemp` + 复制 production DB + monkey-patch 所有 `_conn` 指向临时文件
- **覆盖重点**：5 service 的公共 API + 错误路径（不存在账户/不合法状态/超 4 unit 等）
- **性能基线**：只记录当前值，不追求极限优化
- **集成测试**：5 大类（端到端流 / 价格线 override / 字段对齐 / C 方案 / 性能基线）
- **locust 压测**：5 task 按权重分配（3/2/1/1/1/1），反映实际用户行为

**端到端流程测试覆盖**：
1. 建仓（open_session）→ 4 加仓到 4 units
2. 价格线（多单 17100/17600 止损/加仓）
3. 调价（stop_loss 17000）
4. v1.5 turtle_trade_process 写入（4 事件）
5. 减仓 unit 1
6. 全部平仓（close_session）
7. session_event_log status_changed 验证

**累计全局**：
- 22 张表（5 旧 + 5 策略 + 11 交易 + 1 回测）
- 6 服务（sim_engine / session_lifecycle / line_calculator / event_logger / trade_process_logger / BacktestEngine）
- 47 个 API 端点
- 2119 行前端
- **82 个测试（69 单测 + 13 集成）通过，0 失败**
- **24 个子任务全部完工**

**下一步决策点**：
- Phase 7（可选 CTP 接入，按 plan.md 不在 6 必做范围）
- git commit + 飞书同步
- 部署验证
- 调整细节
