# 数据源架构设计

## 现状问题
- 分时数据只有当日缓存，隔日需重新同步
- 无实时价格推送，靠缓存+手动触发同步
- 品种中文名和主力合约信息未存储在缓存中

## 数据源总览

| 数据类型 | 数据源 | 实时性 | 用途 |
|---------|--------|--------|------|
| 实时行情 | Sina (`Market_Center.getHQFuturesData`) | ~100ms 轮询 | 信号计算、面板价格 |
| 分钟K线 | Sina (`FC_MinKLineService.GetMinKLineData`) | 日间实时 | K线图、回测 |
| 日线数据 | East Money (`futures_zh_daily_sina`) | 日结算后 | 回测、因子计算 |
| 主力合约 | Sina (`futures_display_main_sina`) | 日间实时 | 合约映射 |

## 实时行情 API

**接口**: `https://vip.stock.finance.sina.com.cn/quotes_service/api/json_v2.php/Market_Center.getHQFuturesData`

**参数**:
- `node`: 品种标识（如 `pta_qh`, `by_qh`, `tks_qh`）
- `sort`: 排序字段（`position`=持仓量）
- `asc`: 升序/降序
- `base`: 固定 `futures`

**返回字段**:
```
symbol, exchange, name, trade(最新价), settlement, presettlement,
open, high, low, close,
bidprice1/askprice1(买一/卖一价),
bidvol1/askvol1(买一/卖一量),
volume(成交量), position(持仓量),
ticktime(最新更新时间), tradedate,
preclose, changepercent(涨跌幅%)
```

**接口延迟**: 约 71ms（实测）

**品种标识映射**（来自 `futures_symbol_mark`）:
```
郑州商品: pta_qh, by_qh, czp_qh, ...
大连商品: pvc_qh, tks_qh, dp_qh, ...
上海期货: by_qh, hj_qh, lwg_qh, ...
中金所:   qz_qh, gz_qh, sngz_qh, ...
```

## 实时信号计算

当前：每日收盘后根据日线数据计算一次
目标：日间每分钟刷新，根据实时价格计算55日突破信号

```
signal = {
    direction: close >= high_55 ? 1 : close <= low_55 ? -1 : 0,
    current_price: realtime.trade,
    change_pct: realtime.changepercent,
    atr: daily_atr (不变),
    high_55: daily.high_55 (不变),
    low_55: daily.low_55 (不变),
    volume: realtime.volume,
    position: realtime.position,
    ticktime: realtime.ticktime,
}
```

## 分钟K线数据

**接口**: `https://stock.finance.sina.com.cn/futures/api/json_v2.php/FC_MinKLineService.GetMinKLineData`

**参数**:
- `symbol`: 合约代码（如 `ag0`, `cu0`）
- `scale`: 分钟周期（5, 15, 30, 60）

**返回**:
```
datetime, open, high, low, close, volume, hold(持仓)
```

**存储策略**（不变）:
- 增量同步：只写入比缓存最新时间更新的数据
- 保留7天，自动清理过期数据

## 数据库表设计

### futures_min（含变更）
```sql
CREATE TABLE futures_min (
    symbol TEXT,           -- 品种代码 AG
    datetime TEXT,         -- 时间 2026-05-30 09:35:00
    open REAL, high REAL, low REAL, close REAL,
    volume REAL,            -- 成交量
    hold REAL,             -- 持仓量（新增）
    period TEXT,            -- 5min/15min/30min/60min
    cached_at TEXT,         -- 缓存日期
    contract_code TEXT,     -- 主力合约代码 ag2605
    name TEXT,              -- 中文名 白银
    PRIMARY KEY (symbol, datetime)
);
```

### futures_realtime（新增）
```sql
CREATE TABLE futures_realtime (
    symbol TEXT PRIMARY KEY,  -- 品种代码
    exchange TEXT,            -- 交易所
    name TEXT,                -- 中文名
    contract_code TEXT,       -- 主力合约代码
    trade REAL,              -- 最新价
    change_pct REAL,         -- 涨跌幅%
    change_abs REAL,         -- 涨跌额
    open REAL, high REAL, low REAL,
    bidprice REAL,            -- 买一价
    askprice REAL,            -- 卖一价
    bidvol REAL,              -- 买一量
    askvol REAL,              -- 卖一量
    volume REAL,              -- 当日成交量
    position REAL,            -- 持仓量
    settlement REAL,          -- 结算价
    presettlement REAL,       -- 前结算价
    preclose REAL,            -- 前收盘价
    ticktime TEXT,            -- 更新时间 HH:MM:SS
    tradedate TEXT,           -- 交易日期
    updated_at TEXT,          -- 数据获取时间
);
```

## 实施计划

### Phase 1: 实时行情采集
- [ ] 新增 `fetch_futures_realtime_all()` 函数，轮询所有品种实时行情
- [ ] 新增 `save_futures_realtime()` 存入 `futures_realtime` 表
- [ ] API 新增 `GET /realtime/all` 返回所有品种实时行情
- [ ] API 新增 `GET /realtime/{symbol}` 返回单个品种实时行情

### Phase 2: 实时信号刷新
- [ ] 新增 `GET /signals/realtime/{symbol}` 基于实时行情计算信号（价格用trade，high_55/low_55仍从日线缓存）
- [ ] 前端面板新增"实时信号"切换：日线信号 / 实时信号
- [ ] 前端面板自动刷新（每30秒轮询一次）

### Phase 3: 持仓量+买卖盘
- [ ] `futures_realtime` 表增加 bid/ask 相关字段
- [ ] 前端详情页展示买卖盘口

## 东方财富实时性说明

东方财富期货行情页面使用 **WebSocket 长连接**推送数据，延迟约 100-300ms。
Sina 的轮询 API 延迟约 71ms，两者均可满足交易信号需求。

对于分钟K线，Sina 提供日间连续实时推送，
但目前 `futures_zh_minute_sina` 使用的是 HTTP 轮询模式（非 WebSocket），
实际为每隔一段时间刷新一次，适合缓存策略。
