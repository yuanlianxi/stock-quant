# 0007 Stock Quant 前端模块化重构设计（Vue 3）

> 版本：v0.1.0
> 创建：2026-06-03
> 状态：✅ 决策已批准（2026-06-03 23:20），开始执行

---

## 一、背景与目标

### 1.1 现状

| 指标 | 值 |
|------|---|
| 文件 | `www/index.html`（单文件 2120 行）|
| 代码组织 | HTML + CSS + JS 全内联 |
| 函数数量 | 60+ 函数，全局作用域 |
| API 调用 | 35 处 `fetch()`，无统一封装 |
| 全局变量 | 12 个 `let` 散落在文件各段 |
| 第三方库 | 仅 `lightweight-charts@4.1.0` |
| 构建工具 | 无（直接用 `python3 -m http.server 8080`） |

### 1.2 痛点

1. **可维护性差**：单文件 2120 行，新增/修改一个功能需要滚动大量无关代码
2. **无组件复用**：建仓/加仓/减仓模态框结构 90% 相似，代码重复
3. **无类型约束**：全 JS 无 TS，函数参数/返回类型靠注释
4. **状态混乱**：12 个全局变量跨函数共享，作用域不清晰
5. **CSS 污染**：所有样式在一个 `<style>` 块，靠注释分段
6. **测试空白**：纯函数逻辑（格式化、计算）无单测

### 1.3 目标

- **可维护性**：单个文件 < 300 行；按"功能模块 + 组件"组织
- **可复用性**：模态框、表格、卡片抽象为通用组件
- **可测试性**：核心 composable / store 工具可单测
- **零功能丢失**：v1.5 全部 8 大功能 100% 保留
- **零破坏升级**：可灰度切换，不影响现有使用

---

## 二、技术选型

### 2.1 核心栈

| 项 | 选型 | 版本 | 理由 |
|------|------|------|------|
| 框架 | **Vue 3** | ^3.4 | Composition API + `<script setup>`，与你点名的技术栈对齐 |
| 构建工具 | **Vite** | ^5.0 | 启动快（< 1s）、HMR 丝滑、原生支持 Vue 3 |
| 状态管理 | **Pinia** | ^2.1 | 替代 Vuex，是 Vue 3 官方推荐；12 个全局变量天然映射成 store |
| 路由 | **不引入 vue-router** | — | 当前是单页 tab 切换，无 URL 路由需求；引入会增加复杂度 |
| 图表库 | **lightweight-charts** | ^4.1 | 保留（已用），包装成 Vue 组件 `<PriceChart>` |
| HTTP 客户端 | **`fetch` + 统一封装** | — | 不引入 axios；用 `composables/useApi.ts` 统一拦截/错误处理 |
| 类型 | **可选 TypeScript** | ^5.4 | 见决策点 ① |

### 2.2 组件库决策（**待你拍板**）

| 选项 | 优势 | 劣势 | 推荐度 |
|------|------|------|--------|
| **A. 不引入组件库（自研 + Tailwind 工具类）** | 体积小（无打包），样式完全可控，与现有暗色主题一致 | 表单/对话框/下拉需手写 | ⭐⭐⭐⭐⭐ |
| **B. Element Plus** | 组件齐全，文档好，Vue 3 适配完整 | 体积大（~500KB gzip），与现有暗色风格冲突需深度定制 | ⭐⭐ |
| **C. Naive UI** | 轻量（~150KB），主题化好，TypeScript-first | 文档略简，生态比 Element 小 | ⭐⭐⭐ |

> **建议**：选 A。当前界面是自研暗色主题，引入组件库反而要重写主题适配。

### 2.3 CSS 方案（**待你拍板**）

| 选项 | 优势 | 劣势 |
|------|------|------|
| **A. 单文件 `<style>` + CSS 变量** | 与现有风格一致，迁移成本低 | 无 scoping，需手写 BEM 命名 |
| **B. `<style scoped>`（Vue 原生）** | 天然样式隔离，组件化彻底 | 不能跨组件复用全局变量 |
| **C. Tailwind CSS** | 原子化、生产体积小 | 学习曲线、暗色主题适配需自定义 config |

> **建议**：选 A + B 混合。全局变量在 `assets/styles/variables.css`（CSS 变量），组件内用 `<style scoped>`。

---

## 三、目录结构

```
projects/stock-quant/
├── www/                          # ⚠️ 旧版（保留作为对照，灰度期保留访问）
│   └── index.html                # 2120 行（不动）
├── webapp/                       # 🆕 新版（Vue 3 + Vite）
│   ├── index.html                # Vite 入口 HTML
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json             # 决策点 ①：是否用 TS
│   ├── public/
│   │   └── favicon.ico
│   └── src/
│       ├── main.ts               # Vue 应用入口
│       ├── App.vue               # 根组件
│       ├── assets/
│       │   └── styles/
│       │       ├── variables.css # CSS 变量（颜色/间距）
│       │       └── global.css    # 全局重置
│       ├── components/           # 通用组件（无业务逻辑）
│       │   ├── Modal.vue
│       │   ├── Toast.vue
│       │   ├── Drawer.vue
│       │   ├── Tabs.vue
│       │   ├── Badge.vue
│       │   ├── StatusBadge.vue
│       │   ├── PriceChart.vue    # 包装 lightweight-charts
│       │   └── DataTable.vue
│       ├── composables/          # 业务逻辑（可复用 hook）
│       │   ├── useApi.ts         # fetch 统一封装
│       │   ├── usePolling.ts     # 轮询工具
│       │   ├── useToast.ts
│       │   └── useModal.ts
│       ├── stores/               # Pinia 状态
│       │   ├── market.ts         # marketData, activeSignals, curSym, curEx
│       │   ├── chart.ts          # curPeriod, curDate, curDays, curIsDaily
│       │   ├── strategy.ts       # currentStrategy, strategyList
│       │   ├── account.ts        # currentAccount, accountSummary
│       │   └── session.ts        # openSessionsBySym
│       ├── services/             # API 调用层
│       │   ├── market.ts         # /signals/*, /minute/*, /daily/*
│       │   ├── strategy.ts       # /strategies/*
│       │   ├── account.ts        # /account/*
│       │   ├── session.ts        # /session/*, /sessions
│       │   ├── backtest.ts       # /backtest/*
│       │   ├── contracts.ts      # /contracts/*, /quotes/*
│       │   ├── cache.ts          # /cache/status
│       │   └── position.ts       # /position/*
│       ├── views/                # 页面级组件（与原页面 1:1 映射）
│       │   ├── MarketGrid.vue            # 顶部交易所 Tab + 品种网格
│       │   ├── SymbolDetail.vue          # 选中品种的 3 栏详情
│       │   ├── PriceLineBar.vue          # Phase 3.9 4 色价格线
│       │   ├── UnitDetail.vue            # 4 Unit 详情面板
│       │   ├── TradeProcessTimeline.vue  # v1.5 交易过程流水
│       │   ├── ContractDrawer.vue        # Phase 1.4 合约抽屉
│       │   ├── AccountCenterModal.vue    # 账户中心模态框
│       │   ├── ActionModalHost.vue       # 手动交易模态框宿主
│       │   ├── BacktestPanel.vue         # 快速回测
│       │   ├── StrategyHistoryPanel.vue  # 策略历史面板
│       │   └── SignalTimeline.vue        # 海龟信号时间线
│       ├── modals/               # 手动交易类模态框
│       │   ├── OpenSessionModal.vue
│       │   ├── AddUnitModal.vue
│       │   ├── ReduceUnitModal.vue
│       │   ├── CloseAllModal.vue
│       │   └── LineOverrideModal.vue
│       └── config/
│           ├── symbols.ts        # EX_MAP, ALL_SYMBOLS（从原文件迁移）
│           └── colors.ts         # COLORS 常量
└── ...（后端代码不动）
```

---

## 四、模块拆分映射（2120 行 → 25 个文件）

### 4.1 旧版功能 → 新版组件

| 旧版（L 行号）| 新版组件 / 位置 | 预估行数 |
|---------|---------|---------|
| L677-700 全局常量 | `config/{symbols,colors}.ts` | 30+10 |
| L702-815 行情/信号/Toast | `services/market.ts` + `stores/market.ts` + `components/Toast.vue` | 80+50+40 |
| L816-885 日期 Tab + 网格渲染 | `views/MarketGrid.vue` | 150 |
| L886-944 选品种/信号/详情 | `views/SymbolDetail.vue` + `stores/market.ts` | 180 |
| L946-1010 K线 + lightweight-charts | `components/PriceChart.vue` | 120 |
| L1014-1040 信号时间线 | `views/SignalTimeline.vue` | 60 |
| L1041-1058 持仓 | `views/SymbolDetail.vue`（嵌入） | 30 |
| L1061-1100 快速回测 | `views/BacktestPanel.vue` | 90 |
| L1103-1135 历史回测 | `views/BacktestPanel.vue`（嵌入） | 70 |
| L1137-1180 回测详情 | `views/BacktestPanel.vue`（嵌入） | 60 |
| L1165-1180 缓存状态 | `views/MarketGrid.vue`（嵌入） | 30 |
| L1190-1295 合约抽屉 | `views/ContractDrawer.vue` | 220 |
| L1280-1370 价格线 + 4 Unit | `views/PriceLineBar.vue` + `views/UnitDetail.vue` | 50+80 |
| L1331-1530 策略切换器 + 历史 | `components/StrategySelector.vue` + `views/StrategyHistoryPanel.vue` | 60+150 |
| L1538-1740 账户中心 | `views/AccountCenterModal.vue` | 250 |
| L1747-1945 手动交易模态框 | `modals/{OpenSession,AddUnit,ReduceUnit,CloseAll,LineOverride}Modal.vue` | 60×5 |
| L1948-2055 交易过程时间线 | `views/TradeProcessTimeline.vue` | 90 |

### 4.2 全局状态 → Pinia store

| 旧全局变量 | 新 store | 字段 |
|---------|---------|------|
| `curEx`, `curSym`, `marketData`, `activeSignals`, `lastAlertTime` | `stores/market.ts` | 同名 + actions: `setSym`, `refreshSignals` |
| `curPeriod`, `curDate`, `curDays`, `curIsDaily` | `stores/chart.ts` | 同名 + actions: `setPeriod`, `setDays` |
| `currentStrategy`, 策略列表 | `stores/strategy.ts` | `current`, `list`, `select(id)` |
| `currentAccount`, 账户汇总 | `stores/account.ts` | `current`, `summary`, `setCurrent` |
| `openSessionsBySym` | `stores/session.ts` | `bySymbol`, `loadOpen()` |
| `currentHistTab`, `currentAcctTab` | 各自组件 local state | 临时 UI 状态不全局化 |
| `chart`（lightweight-charts 实例）| 组件内 `ref` | 不进 store |

### 4.3 API 调用 → services 层

| 旧 `fetch()` 位置 | 新 `services/` |
|---------|---------|
| `/signals/*`, `/turtle/*`, `/cache/status` | `services/market.ts` |
| `/strategies/*` | `services/strategy.ts` |
| `/account/*` | `services/account.ts` |
| `/session/*`, `/sessions` | `services/session.ts` |
| `/backtest/*` | `services/backtest.ts` |
| `/contracts/*`, `/quotes/*` | `services/contracts.ts` |
| `/position/*` | `services/position.ts` |
| `/main-contract/*` | `services/contracts.ts`（合并）|

**统一封装**（`composables/useApi.ts`）：
```ts
export function useApi() {
  const baseUrl = import.meta.env.VITE_API_BASE || ''
  return {
    get: <T>(path: string, params?: Record<string, any>) =>
      request<T>('GET', path, undefined, params),
    post: <T>(path: string, body?: any) => request<T>('POST', path, body),
    put:  <T>(path: string, body?: any) => request<T>('PUT', path, body),
    // 统一错误处理：toast + 日志
  }
}
```

---

## 五、构建与部署

### 5.1 开发环境

```bash
cd webapp
npm install
npm run dev
# 默认 http://localhost:5173（Vite 默认端口）
# 通过 vite.config.ts 配置代理：/api/* → http://localhost:8000
```

### 5.2 生产构建

```bash
npm run build
# 输出 webapp/dist/（静态文件）
# 部署方式二选一（**待你拍板**）：
#   A. 由 FastAPI 挂载 dist/ 静态目录（推荐，单服务）
#   B. 保留 python http.server，但端口改 3000
```

### 5.3 灰度策略

| 阶段 | 持续 | 操作 |
|------|------|------|
| **Phase A 脚手架** | 1 天 | 建 `webapp/`，Vite 跑通，渲染"Hello World" |
| **Phase B 基础组件** | 1 天 | 实现 `useApi`、`Toast`、`Modal`、`Drawer` |
| **Phase C 视图迁移** | 3-4 天 | 按依赖顺序迁移：MarketGrid → SymbolDetail → Backtest → Strategy → Account → Session → Drawer |
| **Phase D 联调 + 灰度** | 2 天 | 新旧两版并行，nginx 按 path 路由（`/` 旧，`/v2` 新）|
| **Phase E 切换 + 清理** | 1 天 | 切主流量到新版，旧版归档 |

---

## 六、验证与验收

### 6.1 功能验收（必须 100% 保留）

| 验收项 | 验证方式 |
|--------|---------|
| 8 大功能模块正常 | 浏览器手动点一遍，对照旧版截图 |
| 35 个 API 调用全 200 | `grep -c 'await useApi' src/` ≥ 35 |
| 12 个全局变量迁移 | `grep -E '^let ' src/views src/stores` = 0 |
| 单文件 < 300 行 | `find src -name '*.vue' -o -name '*.ts' | xargs wc -l | awk '$1 > 300'` = 空 |
| `lightweight-charts` 正常 | 选中品种 → K线图渲染 |
| 手动建仓/加仓/减仓/平仓 | 通过 UI 跑 1 遍 |
| 调价（红/蓝/紫线 override）| UI 跑 1 遍 |
| 账户切换（3 个 sim 账户）| UI 跑 1 遍 |
| 回测运行 + 历史回测 | 跑一次完整回测 |
| 合约抽屉 | 点 AG → 看主力合约列表 |
| 价格线 + 4 Unit + 交易过程 | open session 存在时显示 |

### 6.2 工程验收

| 项 | 标准 |
|------|------|
| Lint | `npm run lint` 0 错 |
| Type check | `npm run type-check`（如果启用 TS） 0 错 |
| Dev server 启动 | < 3s |
| 生产构建 | `npm run build` < 10s |
| 单元测试（可选）| composables/store 覆盖率 > 60% |
| 包大小 | gzip < 200KB（不含 lightweight-charts）|

---

## 七、风险与缓解

| 风险 | 等级 | 缓解 |
|------|------|------|
| 2120 行一次迁移易丢功能 | 🟡 中 | 按模块灰度，每模块迁移后立即对照旧版截图验证 |
| Vite 与 Python http.server 端口冲突 | 🟢 低 | 5173 vs 8080，配置文件注释说明 |
| Pinia store 滥用导致新"全局变量" | 🟡 中 | 规范：跨 view 用 store，view 内部用 `ref`；Code review 卡 |
| TypeScript 学习曲线 | 🟡 中 | 决策点 ①：先纯 JS，待团队熟悉再上 TS |
| lightweight-charts 包装泄漏 | 🟢 低 | `PriceChart.vue` 严格 props/events 边界 |
| 与现有 v1.5 数据流不一致 | 🟢 低 | API 路径和返回结构完全不变 |

---

## 八、决策（已批准，2026-06-03 23:20）

> 用户回复「按你推荐的来就行」，5 项决策全部按推荐项拍板。

| # | 决策项 | **决定** | 备注 |
|---|--------|----------|------|
| ① | **TypeScript vs JavaScript** | ✅ **TypeScript** | 严格类型，提升长期可维护性 |
| ② | **组件库选型** | ✅ **不引入** | 自研 + 全局 CSS 变量，与现有暗色主题一致 |
| ③ | **CSS 方案** | ✅ **scoped + 全局 CSS 变量** | 组件 scoped，全局变量在 `assets/styles/variables.css` |
| ④ | **新版部署方式** | ✅ **FastAPI 挂载 dist/（单服务）** | 8000 端口同时提供 API + 静态资源 |
| ⑤ | **新版访问端口** | ✅ **5173（Vite dev）/ 8000（prod 同源）** | 生产环境无跨域问题 |

---

## 九、变更日志

| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1.0 | 2026-06-03 23:20 | 初稿 + 5 项决策已批准（TS/自研/scoped/FastAPI挂载/默认端口） |
| v0.1.0 | 2026-06-03 23:20 | Phase A 执行中（脚手架） |

